---
phase: 02-critic-refactor-with-commit-0-spike
plan: 05
type: execute
wave: 2
depends_on: [4]
files_modified:
  - agents/gsd-critic-plan.md
  - agents/gsd-critic-code.md
  - agents/gsd-critic-scope.md
  - agents/gsd-critic-verify.md
  - agents/gsd-critic-discuss.md
autonomous: true
requirements: [CRIT-03, CRIT-04, CRIT-05]
must_haves:
  truths:
    - "User can run `wc -l agents/gsd-critic-{plan,code,scope,verify,discuss}.md` and observe each ≤ 100 lines."
    - "User can run `wc -l agents/_shared/critic-base.md agents/gsd-critic-*.md | tail -1` and observe total ≤ 700 lines."
    - "User can run `node --test tests/critic-line-budget.test.cjs` and ALL FOUR sub-tests pass (CRIT-02 base ≤250, CRIT-03 each ≤100, CRIT-04 total ≤700, CRIT-03 reachability @-import on every critic)."
    - "User can run `node --test tests/critic-no-base-shadowing.test.cjs` and observe ZERO violations across all 6 critics."
  artifacts:
    - path: agents/gsd-critic-plan.md
      provides: "Trimmed plan-lens addendum, ≤100 lines, @-import + <lens> + <plan_specific_checklist> + <plan_calibration_examples>."
      min_lines: 30
    - path: agents/gsd-critic-code.md
      provides: "Trimmed code-lens addendum, ≤100 lines (down from 341 — biggest pre-cut)."
      min_lines: 30
    - path: agents/gsd-critic-scope.md
      provides: "Trimmed scope-lens addendum, ≤100 lines (down from 263)."
      min_lines: 30
    - path: agents/gsd-critic-verify.md
      provides: "Trimmed verify-lens addendum, ≤100 lines (down from 271)."
      min_lines: 30
    - path: agents/gsd-critic-discuss.md
      provides: "Trimmed discuss-lens addendum, ≤100 lines (down from 301)."
      min_lines: 30
  key_links:
    - from: agents/gsd-critic-{plan,code,scope,verify,discuss}.md
      to: agents/_shared/critic-base.md
      via: "leading @-import line on every critic"
      pattern: "@~/.claude/get-shit-done/agents/_shared/critic-base.md"
    - from: agents/gsd-critic-plan.md
      to: tests/critic-line-budget.test.cjs (CRIT-04 total cap)
      via: "wc -l sum of all 7 files (base + 6 critics) ≤ 700"
      pattern: "total critic lines"
---

<objective>
Apply the pilot pattern from Plan 04 to the remaining 5 critics — `gsd-critic-{plan,code,scope,verify,discuss}.md`. Each goes from 263–341 lines down to ≤100, beginning with the canonical `@`-import to `agents/_shared/critic-base.md` and containing only `<lens>`, `<{lens}_specific_checklist>`, and `<{lens}_calibration_examples>`. After this plan, all four `critic-line-budget.test.cjs` sub-tests pass (CRIT-02 + CRIT-03 + CRIT-04 + @-import reachability), and `critic-no-base-shadowing.test.cjs` reports zero violations across the 6 critics.

Purpose: Close CRIT-03 (each ≤100), CRIT-04 (total ≤700), and CRIT-05 (no shadowing) for the full critic surface. This is the bulk trim — mechanical, but each lens's surviving content is unique and must be preserved (parity hard-fails in Plan 07 if we drop a critical-tier lens-specific check).

Output: 5 trimmed critic files. Wave-0 static guards GREEN across the board.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/users/dan-halem/gsd-slim-and-integrate/REQUIREMENTS.md
@.planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-RESEARCH.md
@.planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-PATTERNS.md
@.planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-04-SUMMARY.md
@agents/_shared/critic-base.md
@agents/gsd-critic-strategy.md
@agents/gsd-critic-plan.md
@agents/gsd-critic-code.md
@agents/gsd-critic-scope.md
@agents/gsd-critic-verify.md
@agents/gsd-critic-discuss.md
@tests/critic-line-budget.test.cjs
@tests/critic-no-base-shadowing.test.cjs

<interfaces>
The post-trim shape is identical to Plan 04's strategy critic, with the lens token swapped:

```
 ---
<existing frontmatter — preserve verbatim>
 ---

@~/.claude/get-shit-done/agents/_shared/critic-base.md

<lens>
**Primary lane:** <lens-specific summary>
**Finding ID prefix:** `<lens_prefix>-`   # plan- | code- | scope- | verify- | discuss-
**Output file:** `{phase_dir}/CRITIQUE-<lens>.md`. Frontmatter `critique_type: <lens>`.
**Primary input:** <what this critic reviews>
</lens>

<{lens}_specific_checklist>
### Critical-tier (<lens>-only)
- [ ] <items>
### Warning-tier (<lens>-only)
- [ ] <items>
</{lens}_specific_checklist>

<{lens}_calibration_examples>
GOOD: <one specific>
BAD: <one specific REJECT case>
</{lens}_calibration_examples>
```

Lens-specific lane summaries (from RESEARCH §Pattern-1 / existing critic files):

| Lens | Primary lane (1-line summary) | Finding ID prefix | Output file | Primary input |
|------|-------------------------------|-------------------|-------------|---------------|
| plan | Plan quality, requirement coverage, scope estimation, task specificity, dependency correctness, must_haves derivation | `plan-` | `CRITIQUE-plan.md` | `*-PLAN.md` files for the phase |
| code | Source-code quality, security smells, performance hot-paths, idiom drift, test coverage gaps | `code-` | `CRITIQUE-code.md` | files-changed across the phase's commits + relevant src files |
| scope | Scope creep, hidden assumptions, deferred items leaking back in, plan tasks that exceed phase boundaries | `scope-` | `CRITIQUE-scope.md` | PLAN.md + CONTEXT.md (locked decisions + deferred ideas) |
| verify | VERIFICATION.md gaps, missing automated checks, missing must-haves coverage, manual-only items that could be automated | `verify-` | `CRITIQUE-verify.md` | VERIFICATION.md + must_haves frontmatter |
| discuss | CONTEXT.md decision quality, evidence depth, locked-decision contradictions, gray-area decisions left without rationale | `discuss-` | `CRITIQUE-discuss.md` | CONTEXT.md (Decisions, Deferred Ideas, Claude's Discretion sections) |

Pre-trim line counts (verified `wc -l`): plan=299, code=341, scope=263, verify=271, discuss=301. Sum + base + strategy ≤ 700 budget breakdown:
- base ~200 (Plan 02 sets actual)
- strategy ~70 (Plan 04 sets actual)
- 5 critics × ~80 average = ~400
- Total ~670 → headroom ~30 lines

Whitelist (per `tests/critic-no-base-shadowing.test.cjs`): `<lens>`, `tag.endsWith('_specific_checklist')`, `tag.endsWith('_calibration_examples')`. Any other XML tag that ALSO appears in `critic-base.md` is a violation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trim agents/gsd-critic-plan.md to ≤100-line lens addendum</name>
  <files>agents/gsd-critic-plan.md</files>
  <read_first>
    - agents/gsd-critic-plan.md (full file, 299 lines — RESEARCH §Code-Example-1 used this as canonical model)
    - agents/_shared/critic-base.md (Plan 02 — the source-of-truth for what is base-owned)
    - agents/gsd-critic-strategy.md (Plan 04 — the proven template for the post-trim shape)
    - .planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-04-SUMMARY.md (Plan 04's keep-list and any base-shadowing fixes encountered)
    - .planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-PATTERNS.md §agents/gsd-critic-{plan,...}.md (lines 110–158)
    - .planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-RESEARCH.md §Code-Example-1 (lines 458–501)
  </read_first>
  <behavior>
    - YAML frontmatter preserved verbatim.
    - First non-frontmatter line is `@~/.claude/get-shit-done/agents/_shared/critic-base.md`.
    - Contains exactly: `<lens>`, `<plan_specific_checklist>`, `<plan_calibration_examples>`.
    - ≤100 lines.
    - Lane summary: "Plan quality, requirement coverage, scope estimation, task specificity, dependency correctness, must_haves derivation."
    - Finding ID prefix: `plan-`. Output file: `CRITIQUE-plan.md`. Primary input: `*-PLAN.md` files.
    - Critical checklist items survive: requirement coverage, no CONTEXT.md contradiction, dependencies acyclic, deferred ideas absent, no dead code plans, task files exist or are created. Warning items: task action specificity, scope budget, must_haves user-observable, key_links wiring, runnable verify, measurable done.
  </behavior>
  <action>
Use the Write tool. Replace the entire file. Preserve the existing YAML frontmatter exactly (lines 1–7 of current file). The body uses the EXACT calibration example from RESEARCH §Code-Example-1 since it was already vetted:

```markdown
 ---
<existing frontmatter — preserve verbatim>
 ---

@~/.claude/get-shit-done/agents/_shared/critic-base.md

<lens>
**Primary lane:** Plan quality, requirement coverage, scope estimation, task specificity, dependency correctness, must_haves derivation.

**Finding ID prefix:** `plan-`

**Output file:** `{phase_dir}/CRITIQUE-plan.md`. Frontmatter `critique_type: plan`.

**Primary input:** `*-PLAN.md` files for the phase being reviewed.
</lens>

<plan_specific_checklist>
### Critical-tier (plan)
- [ ] Requirement coverage complete: every phase requirement has ≥1 covering task (cross-reference `requirements:` frontmatter against ROADMAP.md requirement IDs)
- [ ] No contradiction with locked CONTEXT.md decisions (each `## Decisions` entry honored by some task)
- [ ] Dependencies acyclic and valid (`depends_on` targets exist, no cycles, wave numbers consistent)
- [ ] Deferred ideas not included (CONTEXT.md `## Deferred Ideas` items absent from tasks)
- [ ] No dead code plans (every task artifact referenced by another task or existing code)
- [ ] Task `<files>` paths exist OR are created by the task

### Warning-tier (plan)
- [ ] Task actions specific enough to execute without questions
- [ ] Scope within budget (2-3 tasks target, ≥4 warning, ≥5 critical-cost)
- [ ] `must_haves.truths` user-observable
- [ ] `must_haves.key_links` cover wiring (component-to-API, etc.)
- [ ] `<verify>` commands are runnable
- [ ] `<done>` criteria measurable
</plan_specific_checklist>

<plan_calibration_examples>
GOOD: "Task 2 at 05-01-PLAN.md:38 says 'implement auth' without specifying mechanism, hash algorithm, or token strategy. Per CONTEXT.md line 12 the user decided JWT with refresh rotation. Task action should reference this decision."

BAD: "Plan looks incomplete." (no evidence, no file:line, no fix — REJECT per base finding-format rules)
</plan_calibration_examples>
```
  </action>
  <verify>
    <automated>wc -l agents/gsd-critic-plan.md; head -10 agents/gsd-critic-plan.md; grep -c "@~/.claude/get-shit-done/agents/_shared/critic-base.md" agents/gsd-critic-plan.md; grep -c "<role>\|<severity_rubric>\|<finding_format>\|<output_contract>" agents/gsd-critic-plan.md</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l agents/gsd-critic-plan.md` returns ≤ 100.
    - `grep -c "@~/.claude/get-shit-done/agents/_shared/critic-base.md" agents/gsd-critic-plan.md` returns 1.
    - `grep -c "<lens>" agents/gsd-critic-plan.md` returns 1.
    - `grep -c "<plan_specific_checklist>" agents/gsd-critic-plan.md` returns 1.
    - `grep -c "<plan_calibration_examples>" agents/gsd-critic-plan.md` returns 1.
    - `grep -c "<role>\|<severity_rubric>\|<finding_format>\|<cross_flag_rules>\|<evidence_requirements>\|<output_contract>\|<success_criteria>\|<context_loading>" agents/gsd-critic-plan.md` returns 0.
    - Output of `node --test tests/critic-no-base-shadowing.test.cjs 2>&1` does NOT contain `gsd-critic-plan.md re-defines`.
  </acceptance_criteria>
  <done>
    plan critic trimmed to spec.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2a: Trim agents/gsd-critic-code.md (highest-risk: 341 → ≤100 lines)</name>
  <files>agents/gsd-critic-code.md</files>
  <read_first>
    - agents/gsd-critic-code.md (full file, 341 lines — biggest pre-cut, most aggressive trim ratio in Phase 2)
    - agents/_shared/critic-base.md (Plan 02 — source-of-truth for what is base-owned)
    - agents/gsd-critic-strategy.md (Plan 04 — proven template)
    - agents/gsd-critic-plan.md (Task 1 of THIS plan — second example of the template)
    - .planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-04-SUMMARY.md
    - .planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-PATTERNS.md §agents/gsd-critic-{plan,...}.md (lines 110–158)
  </read_first>
  <behavior>
    - File trimmed to ≤100 lines following the proven template (strategy critic from Plan 04 + plan critic from Task 1 of this plan).
    - Begins (post-frontmatter) with `@~/.claude/get-shit-done/agents/_shared/critic-base.md`.
    - Contains exactly: `<lens>` + `<code_specific_checklist>` + `<code_calibration_examples>`.
    - Frontmatter preserved verbatim.
    - Why isolated as its own task: gsd-critic-code is the most aggressive trim (341 → ≤100, ratio ~3.4×). Risk of dropping a load-bearing critical-tier check is highest here. Isolating this trim from the other 3 critics means a code.md failure does not contaminate the scope/verify/discuss trims, and a parity regression (caught later by Plan 07's CRIT-10 test) can be debugged without spanning multiple files.
  </behavior>
  <action>
Use the Write tool to replace `agents/gsd-critic-code.md` entirely with the lens-addendum template. Use the lens-specific values:

**For agents/gsd-critic-code.md:**
- Lane: "Source-code quality, security smells, performance hot-paths, idiom drift, test coverage gaps."
- Finding ID prefix: `code-`. Output file: `CRITIQUE-code.md`. Primary input: "Files changed across the phase's commits, plus the relevant src/test files."
- Critical-tier items (extract from existing checklists, drop base duplicates): security violations (input validation, auth, injection), correctness regressions, broken existing tests, missing tests for new behavior.
- Warning-tier: code style drift, performance hot-paths without measurement, missing error handling, magic numbers, deeply nested conditionals.
- Calibration: GOOD = specific file:line citing CWE-XXX. BAD = "Code looks fine." rejected.

After writing, run the per-file checks (see <acceptance_criteria>). If `wc -l` exceeds 100, tighten the calibration examples or merge similar checklist items — DO NOT drop a critical-tier item without recording the drop in SUMMARY (Plan 07's parity test will catch a regression).
  </action>
  <verify>
    <automated>wc -l agents/gsd-critic-code.md; head -10 agents/gsd-critic-code.md; grep -c "@~/.claude/get-shit-done/agents/_shared/critic-base.md" agents/gsd-critic-code.md; grep -c "<code_specific_checklist>" agents/gsd-critic-code.md; grep -c "<code_calibration_examples>" agents/gsd-critic-code.md; grep -c "<role>\|<severity_rubric>\|<finding_format>\|<cross_flag_rules>\|<evidence_requirements>\|<output_contract>\|<success_criteria>\|<context_loading>" agents/gsd-critic-code.md</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l agents/gsd-critic-code.md` returns ≤ 100.
    - `grep -c "@~/.claude/get-shit-done/agents/_shared/critic-base.md" agents/gsd-critic-code.md` returns 1.
    - `grep -c "<lens>" agents/gsd-critic-code.md` returns 1.
    - `grep -c "<code_specific_checklist>" agents/gsd-critic-code.md` returns 1.
    - `grep -c "<code_calibration_examples>" agents/gsd-critic-code.md` returns 1.
    - `grep -c "<role>\|<severity_rubric>\|<finding_format>\|<cross_flag_rules>\|<evidence_requirements>\|<output_contract>\|<success_criteria>\|<context_loading>" agents/gsd-critic-code.md` returns 0.
    - Output of `node --test tests/critic-no-base-shadowing.test.cjs 2>&1` does NOT contain `gsd-critic-code.md re-defines`.
  </acceptance_criteria>
  <done>
    code critic trimmed to spec; the highest-risk trim is isolated and complete before Task 2b touches the other 3 critics.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: Trim agents/gsd-critic-{scope,verify,discuss}.md (3 critics, mechanical trim — total 835 → ≤300 lines across 3 files)</name>
  <files>agents/gsd-critic-scope.md, agents/gsd-critic-verify.md, agents/gsd-critic-discuss.md</files>
  <read_first>
    - agents/gsd-critic-scope.md (full file, 263 lines)
    - agents/gsd-critic-verify.md (full file, 271 lines)
    - agents/gsd-critic-discuss.md (full file, 301 lines)
    - agents/_shared/critic-base.md (Plan 02)
    - agents/gsd-critic-strategy.md (Plan 04 — proven template)
    - agents/gsd-critic-plan.md (Task 1 of THIS plan — template example)
    - agents/gsd-critic-code.md (Task 2a of THIS plan — template example AND a check that the highest-risk trim succeeded before propagating the pattern)
    - .planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-04-SUMMARY.md
    - .planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-PATTERNS.md §agents/gsd-critic-{plan,...}.md (lines 110–158)
  </read_first>
  <behavior>
    - Each of the 3 files trimmed to ≤100 lines following the same template as Plan 04 (strategy), Task 1 (plan), and Task 2a (code).
    - Each begins (post-frontmatter) with `@~/.claude/get-shit-done/agents/_shared/critic-base.md`.
    - Each contains exactly: `<lens>` + `<{lens}_specific_checklist>` + `<{lens}_calibration_examples>`.
    - Frontmatter preserved verbatim per file.
    - Combined total (base + 6 critics including code from Task 2a) MUST be ≤700 — verify after writing all 3.
  </behavior>
  <action>
For each of the 3 files, use the Write tool to replace the entire file with the lens-addendum template. Use the lens-specific values from the `<interfaces>` table in this PLAN.

**For agents/gsd-critic-scope.md:**
- Lane: "Scope creep, hidden assumptions, deferred items leaking back in, plan tasks that exceed phase boundaries."
- Finding ID prefix: `scope-`. Output file: `CRITIQUE-scope.md`. Primary input: "PLAN.md + CONTEXT.md (locked decisions and deferred ideas)."
- Critical-tier: deferred-idea reintroduction, locked-decision violation, cross-phase scope leak, requirement creep without ROADMAP edit.
- Warning-tier: hidden assumptions in task actions, scope ambiguity in must_haves, optimism bias in estimates.
- Calibration: GOOD = "Task 3 at PLAN.md:42 implements feature X marked '## Deferred Ideas' in CONTEXT.md:14." BAD = "Scope might be off." rejected.

**For agents/gsd-critic-verify.md:**
- Lane: "VERIFICATION.md gaps, missing automated checks, must-haves not covered, manual-only items that could be automated."
- Finding ID prefix: `verify-`. Output file: `CRITIQUE-verify.md`. Primary input: "VERIFICATION.md + must_haves frontmatter from each PLAN.md in the phase."
- Critical-tier: must_have not verified, manual-only check that has a CLI alternative, missing automated test for a primary truth, evidence-of-test absent.
- Warning-tier: vague verify command, verify command exits 0 but doesn't actually check the behavior, missing artifact-existence check.
- Calibration: GOOD = "must_haves.truths[2] 'user can log in' has no automated verify; corresponding `<verify>` block at 04-PLAN.md:67 is 'manual'." BAD = "Verify is weak." rejected.

**For agents/gsd-critic-discuss.md:**
- Lane: "CONTEXT.md decision quality, evidence depth, locked-decision contradictions, gray-area decisions left without rationale."
- Finding ID prefix: `discuss-`. Output file: `CRITIQUE-discuss.md`. Primary input: "CONTEXT.md (Decisions, Deferred Ideas, Claude's Discretion sections)."
- Critical-tier: decision lacks rationale, decision contradicts another locked decision, deferred-idea boundary unclear, "Claude's Discretion" item that should be a locked decision.
- Warning-tier: thin evidence on a non-trivial decision, conflated decisions, missing alternatives-considered note.
- Calibration: GOOD = "Decision D-04 'use library X' lacks rationale; D-02 also picks library Y for the same purpose. Reconcile." BAD = "Decisions look hasty." rejected.

After writing all 3 files, run the totals check:

```bash
wc -l agents/_shared/critic-base.md agents/gsd-critic-{plan,code,scope,verify,discuss,strategy}.md
```

If total > 700, the most likely overshoot at this point is one of the 3 files just trimmed. Tighten the calibration examples or merge similar checklist items.
  </action>
  <verify>
    <automated>for f in scope verify discuss; do echo "--- gsd-critic-$f ---"; wc -l agents/gsd-critic-$f.md; head -10 agents/gsd-critic-$f.md; done; echo "--- TOTAL ---"; wc -l agents/_shared/critic-base.md agents/gsd-critic-*.md | tail -1; node --test tests/critic-line-budget.test.cjs; node --test tests/critic-no-base-shadowing.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - For each of `scope`, `verify`, `discuss`: `wc -l agents/gsd-critic-$lens.md` returns ≤ 100.
    - For each: `grep -c "@~/.claude/get-shit-done/agents/_shared/critic-base.md" agents/gsd-critic-$lens.md` returns 1.
    - For each: `grep -c "<${lens}_specific_checklist>" agents/gsd-critic-$lens.md` returns 1.
    - For each: `grep -c "<${lens}_calibration_examples>" agents/gsd-critic-$lens.md` returns 1.
    - For each: `grep -c "<role>\|<severity_rubric>\|<finding_format>\|<cross_flag_rules>\|<evidence_requirements>\|<output_contract>\|<success_criteria>\|<context_loading>" agents/gsd-critic-$lens.md` returns 0.
    - `wc -l agents/_shared/critic-base.md agents/gsd-critic-*.md | tail -1` returns a total ≤ 700.
    - `node --test tests/critic-line-budget.test.cjs` exits 0 (all 4 sub-tests GREEN — CRIT-02, CRIT-03, CRIT-04, @-import reachability).
    - `node --test tests/critic-no-base-shadowing.test.cjs` exits 0 (zero violations across all 6 critics).
  </acceptance_criteria>
  <done>
    All 5 critics trimmed across Tasks 1, 2a, 2b. CRIT-03, CRIT-04, CRIT-05 fully GREEN. The trimmed surface (~700 lines down from 1,731) is the new authoritative state for Wave 2 orchestrator wiring (Plan 06) and live tests (Plan 07).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| existing critic content → trimmed addendum | Risk: dropping a load-bearing critical-tier check unique to a lens, leaving the critic weaker than baseline. Mitigated by parity test in Plan 07. |
| addendum tag set → no-base-shadowing whitelist | Risk: introducing a non-whitelisted XML tag that shadows base. Mitigated by acceptance-criteria grep + the no-base-shadowing test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-A | Tampering | dropping load-bearing lens-specific check during trim | mitigate | Plan 07 CRIT-10 parity test (N=5 median, ≥85% finding overlap, no missing critical) catches behavioral regressions per-lens. If the bulk trim causes parity FAIL on any lens, restore the dropped item to that addendum. |
| T-02-B | Spoofing | leading @-import line typo | mitigate | The line is identical across all 6 critics (and matches the regex in `tests/critic-line-budget.test.cjs`); a single visual diff against `gsd-critic-strategy.md` verifies it. |
| T-02-C | Information Disclosure | preserved frontmatter contains nothing sensitive | accept | Frontmatter is `name`, `description`, `tools`, `color`. |
| T-02-D | Tampering | total exceeding 700-line budget | mitigate | `wc -l ... | tail -1` acceptance check; Plan 04 SUMMARY documents headroom. |
</threat_model>

<verification>
1. `wc -l agents/gsd-critic-*.md` shows each ≤ 100.
2. `wc -l agents/_shared/critic-base.md agents/gsd-critic-*.md | tail -1` shows total ≤ 700.
3. `node --test tests/critic-line-budget.test.cjs` exits 0.
4. `node --test tests/critic-no-base-shadowing.test.cjs` exits 0.
5. Every critic begins (post-frontmatter) with the canonical @-import.
6. No critic contains any base-owned XML tag.
</verification>

<success_criteria>
1. CRIT-03 (each ≤100): GREEN.
2. CRIT-04 (total ≤700): GREEN.
3. CRIT-05 (no base-shadowing): GREEN.
4. The trim surface (≤700 lines) preserves enough lens-specific content per critic that Plan 07's CRIT-10 parity test will pass.
</success_criteria>

<output>
After completion, create `.planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-05-SUMMARY.md` documenting:
- Final per-critic line counts (table) and total
- Per-critic keep-list of critical/warning checklist items (so Plan 07's parity-failure debugging has a quick reference)
- Any base-shadowing violations encountered + how they were resolved (informs whether base needs further trim)
</output>
