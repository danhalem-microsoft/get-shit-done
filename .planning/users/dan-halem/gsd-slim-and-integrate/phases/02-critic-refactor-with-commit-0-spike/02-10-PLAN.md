---
phase: 02-critic-refactor-with-commit-0-spike
plan: 10
type: execute
wave: 5
depends_on: [9]
gap_closure: true
files_modified:
  - tests/BUILD.bazel
autonomous: true
requirements: [CRIT-03, XCUT-03]
must_haves:
  truths:
    - "`bazel query //tests:cull-no-orphan-references` resolves to a `js_test` target tagged `phase-2-critic` AND `phase-1-cull`."
    - "`bazel query //tests:gsd-sdk-query-registry-integration` resolves to a `js_test` target tagged `phase-2-critic`."
    - "Both tests appear in the count returned by `bazel query 'attr(tags, phase-2-critic, //tests/...)'`."
  artifacts:
    - path: tests/BUILD.bazel
      provides: "Two new `js_test` targets registered with correct tag + data attributes."
---

<objective>
Phase 2 exit-gate gap closure #4 (per Wave 6 status check): two test files exist on disk but have no `js_test` entries in `tests/BUILD.bazel`:
- `tests/cull-no-orphan-references.test.cjs` — Phase 1 cull primary gate test, extended in Plan 02-03 to cover `agents/_shared/` (cross-tagged phase-1-cull + phase-2-critic).
- `tests/gsd-sdk-query-registry-integration.test.cjs` — drift guard introduced by Plan 02-06 for the `critic-aggregate` handler registration.

The bazel filter `--test_tag_filters=phase-2-critic` undercounts coverage by 2 targets until these are wired in. This plan adds the entries with correct tags and data deps. Actual `data` plumbing for sandbox file access is owned by Plan 02-11.
</objective>

<context>
File deps inferred from the test sources:
- `cull-no-orphan-references.test.cjs` reads `tests/fixtures/cull-deletion-list.cjs` (same-package), globs `agents/`, `commands/`, `get-shit-done/`, `hooks/`, `scripts/`, `sdk/`, and `get-shit-done/bin/lib/install-manifest.json`.
- `gsd-sdk-query-registry-integration.test.cjs` reads `sdk/src/query/index.ts` and scans `commands/`, `agents/`, `get-shit-done/`, `hooks/`, `scripts/`, `sdk/`.

Both effectively need `//:project_sources` as data — Plan 02-11 adds that. This plan only adds BUILD ENTRIES so the targets exist and pick up the `data` from 02-11.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add js_test entries for cull-no-orphan-references and gsd-sdk-query-registry-integration</name>
  <files>tests/BUILD.bazel</files>
  <action>
Extend the Phase 2 list-comprehension in `tests/BUILD.bazel` to include the two missing test files. The cull-no-orphan-references test is dual-tagged (phase-1-cull AND phase-2-critic) — it covers both. The SDK registry test is phase-2-critic only.

After editing, verify with:
```bash
bazel query '//tests:cull-no-orphan-references' '//tests:gsd-sdk-query-registry-integration'
bazel query 'attr(tags, "phase-2-critic", //tests/...)' | wc -l   # expect 9 (was 7)
```
  </action>
  <verify>
    <automated>bazel query '//tests:cull-no-orphan-references' '//tests:gsd-sdk-query-registry-integration' 2>&1 | grep -c "^//tests:"</automated>
  </verify>
  <acceptance_criteria>
    - `bazel query //tests:cull-no-orphan-references` returns the target name.
    - `bazel query //tests:gsd-sdk-query-registry-integration` returns the target name.
    - `bazel query 'attr(tags, "phase-2-critic", //tests/...)'` returns ≥9 targets (was 7).
    - `bazel query 'attr(tags, "phase-1-cull", //tests/...)'` includes `cull-no-orphan-references`.
  </acceptance_criteria>
  <done>
    Two BUILD entries added; bazel resolves both targets; ready for 02-11 data-deps fix.
  </done>
</task>

</tasks>

<output>
After completion, create `02-10-SUMMARY.md` documenting: BUILD entries added; target count before/after; followup note that tests will still fail at runtime until Plan 02-11 adds `data` attrs.
</output>
</content>
