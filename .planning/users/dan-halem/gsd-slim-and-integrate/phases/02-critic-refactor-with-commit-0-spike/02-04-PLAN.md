---
phase: 02-critic-refactor-with-commit-0-spike
plan: 04
type: execute
wave: 2
depends_on: [1, 2]
files_modified:
  - agents/gsd-critic-strategy.md
autonomous: true
requirements: [CRIT-03, CRIT-05]
must_haves:
  truths:
    - "User can read `agents/gsd-critic-strategy.md` and find that line 1 (after the YAML frontmatter `---`) is exactly `@~/.claude/get-shit-done/agents/_shared/critic-base.md`."
    - "User can run `wc -l agents/gsd-critic-strategy.md` and observe ≤ 100 lines (down from 256)."
    - "User can run `node --test tests/critic-line-budget.test.cjs --test-name-pattern='@-import'` and the @-import-reachability sub-test passes for gsd-critic-strategy."
    - "User can run `node --test tests/critic-no-base-shadowing.test.cjs` and observe NO violation referencing `gsd-critic-strategy.md` in EITHER sub-test (tag-name scan AND H9 Jaccard content-overlap scan from Plan 01 Task 2)."
  artifacts:
    - path: agents/gsd-critic-strategy.md
      provides: "Pilot lens-addendum critic — proves the post-trim shape works on a real critic before Plan 05's bulk trim. Line 1 (post-frontmatter) is the @-import; body has <lens> + <strategy_specific_checklist> + <strategy_calibration_examples> only."
      min_lines: 30
  key_links:
    - from: agents/gsd-critic-strategy.md
      to: agents/_shared/critic-base.md
      via: "leading @-import line resolved by Claude Code at agent-load time"
      pattern: "@~/.claude/get-shit-done/agents/_shared/critic-base.md"
    - from: agents/gsd-critic-strategy.md
      to: tests/critic-line-budget.test.cjs (@-import reachability sub-test)
      via: "first-non-frontmatter-line regex match"
      pattern: "^@~?\\/?\\.claude\\/get-shit-done\\/agents\\/_shared\\/critic-base\\.md"
    - from: agents/gsd-critic-strategy.md
      to: get-shit-done/workflows/critique.md (Plan 06 — B4 dependency)
      via: "the `<strategy_specific_checklist>` tag name in this addendum is REFERENCED by the Task prompt for gsd-critic-strategy in workflows/critique.md (Plan 06 depends_on=[1,2,3,4,5] cascades to wave 4 specifically because of this tag-name dependency)"
      pattern: "<strategy_specific_checklist>"
---

<objective>
Pilot the lens-addendum trim by refactoring the smallest critic (`agents/gsd-critic-strategy.md` at 256 lines) to ≤100 lines, beginning with the canonical `@`-import to the shared base. This validates the addendum shape end-to-end on a real critic before Plan 05 bulks the same trim across the remaining 5 critics.

Purpose: De-risk the bulk trim. Plan 05 has 5 trims to do; if the base prompt is missing a section or the post-write verify directive needs a tweak, we surface it here on one critic instead of six. Per RESEARCH §Wave-0 Gaps and VALIDATION §Wave 0 Requirements: "trim 1 critic to ≤100 lines as a pilot — likely smallest one, gsd-critic-strategy.md at 256 lines."

Reviews-applied (this revision):
- B4 / plan-C-003: this plan's `<strategy_specific_checklist>` XML tag-name choice is consumed by Plan 06's `workflows/critique.md` Task prompt for gsd-critic-strategy. Plan 06 now has `depends_on: [1, 2, 3, 4, 5]` and lands in wave 4 (was wave 3) so the tag name is finalized BEFORE the workflow references it. This plan stays at wave 2 (no change here) — it's the EARLIER plan in the cascade.
- S4 / strategy-W-004 (planner-noted, not actionable here): during this pilot trim, the executor SHOULD run a single pre-trim vs post-trim parity comparison on the strategy critic and inspect `missingCritical` and `extraFindings` outputs by hand (~$0.60). Catches systematic false-pass/false-fail before 5 wasted parity runs. This is a planner-suggested pilot validation; the executor decides whether to perform it.
- H9 (downstream from Plan 01 Task 2): the no-base-shadowing test now has a Jaccard content-overlap sub-test in addition to the tag-name scan. This plan's trim must avoid both kinds of violations.

Output: 1 trimmed critic file. Both Wave-0 static guard tests (`critic-line-budget` and `critic-no-base-shadowing`) gain at least one passing critic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/users/dan-halem/gsd-slim-and-integrate/REQUIREMENTS.md
@.planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-RESEARCH.md
@.planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-PATTERNS.md
@.planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-REVIEWS.md
@.planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-02-SUMMARY.md
@agents/_shared/critic-base.md
@agents/gsd-critic-strategy.md
@agents/gsd-planner.md
@tests/critic-line-budget.test.cjs
@tests/critic-no-base-shadowing.test.cjs

<interfaces>
Target post-trim shape (per PATTERNS §agents/gsd-critic-{...}.md and RESEARCH §Code-Example-1):

```
 ---
name: gsd-critic-strategy
description: <existing description verbatim>
tools: <existing tool list verbatim>
color: <existing color verbatim>
 ---

@~/.claude/get-shit-done/agents/_shared/critic-base.md

<lens>
**Primary lane:** <strategy lens summary, 1–2 lines>
**Finding ID prefix:** `strategy-`
**Output file:** `{phase_dir}/CRITIQUE-strategy.md`. Frontmatter `critique_type: strategy`.
**Primary input:** <what the strategy critic reviews — milestone-level docs, ROADMAP, etc.>
</lens>

<strategy_specific_checklist>
### Critical-tier (strategy-only)
- [ ] <strategy-unique items>
### Warning-tier (strategy-only)
- [ ] <strategy-unique warnings>
</strategy_specific_checklist>

<strategy_calibration_examples>
GOOD: <one specific GOOD example with file:line>
BAD: <one specific BAD example — REJECT per base finding-format rules>
</strategy_calibration_examples>
```

Whitelist enforced by `tests/critic-no-base-shadowing.test.cjs` (Plan 01 Task 2):
- `<lens>`
- `tag.endsWith('_specific_checklist')` → `<strategy_specific_checklist>`
- `tag.endsWith('_calibration_examples')` → `<strategy_calibration_examples>`

Any OTHER XML tag in this addendum that ALSO appears in `critic-base.md` is a violation (sub-test 1).
**H9 sub-test 2:** any 5-line window in this addendum's body that has ≥80% Jaccard overlap with any 5-line window in critic-base.md is also a violation. Avoid copying base prose verbatim.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trim agents/gsd-critic-strategy.md to ≤100-line lens addendum</name>
  <files>agents/gsd-critic-strategy.md</files>
  <read_first>
    - agents/gsd-critic-strategy.md (full file, 256 lines — identify which sections duplicate base vs which carry the strategy-specific lens)
    - agents/_shared/critic-base.md (Plan 02 output — anything in this list MUST be removed from the addendum AND the H9 Jaccard scan must not flag the surviving content)
    - .planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-PATTERNS.md §agents/gsd-critic-{plan,...}.md (lines 110–158 — exact post-trim opening shape)
    - .planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-RESEARCH.md §Code-Example-1 (lines 458–501 — line-count budget breakdown)
    - tests/critic-line-budget.test.cjs (the @-import regex constraint)
    - tests/critic-no-base-shadowing.test.cjs (the whitelist + Jaccard sub-test)
  </read_first>
  <behavior>
    - YAML frontmatter PRESERVED verbatim (name, description, tools, color).
    - First non-frontmatter line is exactly `@~/.claude/get-shit-done/agents/_shared/critic-base.md`.
    - Body contains exactly three XML sections in this order: `<lens>`, `<strategy_specific_checklist>`, `<strategy_calibration_examples>`.
    - Total line count ≤ 100 (CRIT-03).
    - No XML tag from `agents/_shared/critic-base.md` is re-defined (CRIT-05 sub-test 1).
    - No 5-line window in the body has ≥80% Jaccard overlap with any 5-line window in critic-base.md (CRIT-05 sub-test 2 / H9). Practical implication: don't copy base prose verbatim — write the strategy-specific content in lens-specific phrasing.
    - Surviving lens content represents what is UNIQUE to the strategy critic (milestone-level scope, multi-phase coupling, capability decomposition, cross-phase dependency tracking).
  </behavior>
  <action>
**Step A — Audit current file for keep vs drop.**

Read `agents/gsd-critic-strategy.md` (256 lines). Categorize each section:

- KEEP (strategy-specific): items in any `<checklist>` block that explicitly reference milestone scope, multi-phase dependencies, capability decomposition, ROADMAP-level concerns, or cross-phase signal.
- DROP (now owned by base): `<role>` framing, severity definitions, finding card schema, cross-flag percentage rules, output contract, success criteria, evidence requirements.
- TRANSFORM: any "GOOD: ... BAD: ..." calibration examples → wrap into `<strategy_calibration_examples>`.

**Step B — Rewrite the file using the Write tool.**

Replace the entire file with this template, populated with the keep-list content from Step A. Preserve the existing YAML frontmatter exactly:

```markdown
 ---
<existing frontmatter — preserve verbatim>
 ---

@~/.claude/get-shit-done/agents/_shared/critic-base.md

<lens>
**Primary lane:** <1–2 line summary — e.g., "Milestone-level scope correctness, multi-phase capability decomposition, cross-phase dependency tracking, and roadmap-level capability gaps.">

**Finding ID prefix:** `strategy-`

**Output file:** `{phase_dir}/CRITIQUE-strategy.md`. Frontmatter `critique_type: strategy`.

**Primary input:** ROADMAP.md, milestone-level PROJECT.md, cross-phase SUMMARY.md files in `.planning/phases/`, and any cross-phase dependency manifests.
</lens>

<strategy_specific_checklist>
### Critical-tier (strategy-only)
- [ ] <strategy-only critical items extracted from existing checklist>
- [ ] <next>
- [ ] <next>

### Warning-tier (strategy-only)
- [ ] <strategy-only warning items>
- [ ] <next>
</strategy_specific_checklist>

<strategy_calibration_examples>
GOOD: <one concrete example with file:line>

BAD: <one concrete BAD example — REJECT per base finding-format rules>
</strategy_calibration_examples>
```

**Step C — Verify line count.**

After writing, confirm `wc -l agents/gsd-critic-strategy.md` returns ≤ 100. Aim for 60–90.

**Step D — Verify no base-shadowing (BOTH sub-tests).**

Run `node --test tests/critic-no-base-shadowing.test.cjs`. Inspect the failure list:
- Sub-test 1 (tag-name scan): no line mentioning `gsd-critic-strategy.md re-defines`.
- Sub-test 2 (H9 Jaccard scan): no line mentioning `gsd-critic-strategy.md window L... ≥80% Jaccard match`.

If a Jaccard violation appears, REWORD the offending content in lens-specific phrasing (the offending window matched base too closely; that's a sign the executor copied base prose instead of writing strategy-specific lens content).

**Step E (optional but recommended per S4) — Pilot parity comparison.**

If desired, run a single pre-trim vs post-trim parity comparison on the strategy critic against its Phase 1 baseline (~$0.60). This catches systematic false-pass/false-fail before the bulk Plan 07 parity runs:

```bash
node integration/test-fixtures/baselines/_capture.cjs --critic gsd-critic-strategy --n 1 --mode compare --phase phase-2-critic-pilot 2>&1 | tail -30
```

Inspect `missingCritical` and `extraFindings` by hand. If anything looks systematically wrong, restore the dropped checklist item and re-trim. Document the pilot result in the SUMMARY.
  </action>
  <verify>
    <automated>wc -l agents/gsd-critic-strategy.md; head -10 agents/gsd-critic-strategy.md; grep -n "@~/.claude/get-shit-done/agents/_shared/critic-base.md" agents/gsd-critic-strategy.md; node --test tests/critic-line-budget.test.cjs --test-name-pattern='@-import' 2>&1 | tail -5; node --test tests/critic-no-base-shadowing.test.cjs 2>&1 | tee /tmp/no-shadow.out; ! grep -q "gsd-critic-strategy.md re-defines" /tmp/no-shadow.out && ! grep -q "gsd-critic-strategy.md window L.*Jaccard" /tmp/no-shadow.out</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l agents/gsd-critic-strategy.md` returns a value ≤ 100 (target: 60–90).
    - Line 1 of the file is `---` (frontmatter delimiter).
    - The first non-blank line AFTER the closing `---` is exactly `@~/.claude/get-shit-done/agents/_shared/critic-base.md`.
    - `grep -c "@~/.claude/get-shit-done/agents/_shared/critic-base.md" agents/gsd-critic-strategy.md` returns 1.
    - `grep -c "<lens>" agents/gsd-critic-strategy.md` returns 1.
    - `grep -c "<strategy_specific_checklist>" agents/gsd-critic-strategy.md` returns 1.
    - `grep -c "<strategy_calibration_examples>" agents/gsd-critic-strategy.md` returns 1.
    - `grep -c "<role>\|<severity_rubric>\|<finding_format>\|<cross_flag_rules>\|<evidence_requirements>\|<output_contract>\|<success_criteria>\|<context_loading>" agents/gsd-critic-strategy.md` returns 0.
    - `! grep -q "gsd-critic-strategy.md re-defines" /tmp/no-shadow.out` (sub-test 1 clean).
    - `! grep -q "gsd-critic-strategy.md window L.*Jaccard" /tmp/no-shadow.out` (H9 sub-test 2 clean).
    - The @-import reachability sub-test of `critic-line-budget.test.cjs` reports no failure for `gsd-critic-strategy.md`.
  </acceptance_criteria>
  <done>
    Pilot critic trimmed to spec. Plan 05 can confidently apply the same shape to the remaining 5 critics. The Wave-0 line-budget test now has at least one critic passing the per-file cap. H9 Jaccard sub-test passes (no copied base prose).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| existing critic content → trimmed addendum | Risk: dropping a strategy-specific finding rule that is NOT in base, leaving the strategy lens weaker than baseline. Mitigated by careful keep/drop audit (Step A) + optional pilot parity (Step E per S4) + Plan 07's CRIT-10 parity test. |
| addendum tag set → no-base-shadowing whitelist | Risk: introducing a new tag (e.g., `<phase_specific_strategy>`) that doesn't match the whitelist suffixes. Mitigated by sticking to the three known containers. |
| addendum prose → H9 Jaccard scan | Risk: copying base prose verbatim into the addendum body, triggering 5-line-window Jaccard overlap. Mitigated by writing strategy-specific phrasing rather than copying. |
| addendum tag name → workflows/critique.md (Plan 06) | B4: this plan's `<strategy_specific_checklist>` choice is consumed by Plan 06's workflow Task prompt. Plan 06 depends on this plan (wave 4 cascade). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-A | Tampering | dropping a load-bearing strategy checklist item during trim | mitigate | Plan 07 CRIT-10 parity test (N=5 median, ≥85% finding overlap, no missing critical) catches behavioral regressions. Optional Step E pilot parity (S4) catches systematic issues earlier. If parity FAILS, restore the dropped item to the addendum. |
| T-02-B | Spoofing | leading @-import line typo silently breaks resolution | mitigate | Plan 01 Task 1's @-import-reachability regex sub-test enforces the exact path; this plan's verify command runs it. |
| T-02-C | Information Disclosure | preserved frontmatter contains nothing sensitive | accept | Frontmatter is `name`, `description`, `tools`, `color` — no credentials. |
| T-02-D | Tampering | H9 Jaccard violation from copied base prose | mitigate | Acceptance check `! grep -q "gsd-critic-strategy.md window L.*Jaccard"` enforces zero violations on the new content-overlap sub-test. |
</threat_model>

<verification>
1. `wc -l agents/gsd-critic-strategy.md` returns ≤ 100.
2. The first non-frontmatter line is `@~/.claude/get-shit-done/agents/_shared/critic-base.md`.
3. `node --test tests/critic-line-budget.test.cjs --test-name-pattern='@-import'` exits 0 (or, if all 6 critics needed for the test, the strategy line in the failure output is gone).
4. `node --test tests/critic-no-base-shadowing.test.cjs` 2>&1 contains NO `gsd-critic-strategy.md re-defines` line AND NO `gsd-critic-strategy.md window L.*Jaccard` line.
5. `grep -c "<role>\|<severity_rubric>\|<finding_format>" agents/gsd-critic-strategy.md` returns 0.
</verification>

<success_criteria>
1. `agents/gsd-critic-strategy.md` is ≤ 100 lines, begins (post-frontmatter) with the exact `@`-import to base, contains only the three whitelisted lens-specific tag containers.
2. Both Wave-0 static guards report ZERO violations for `gsd-critic-strategy.md` specifically (other un-trimmed critics still violate; that's expected and Plan 05's job).
3. Plan 05 has a verified template — its 5 trims can be a mechanical application of the same shape.
4. (Optional per S4) Pilot parity result documented in SUMMARY — informs whether the trim shape is parity-safe before Plan 05's bulk apply.
5. The `<strategy_specific_checklist>` tag name finalized here is the SAME tag name Plan 06 references in `workflows/critique.md` (B4 cascade).
</success_criteria>

<output>
After completion, create `.planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-04-SUMMARY.md` documenting:
- Final line count (target: 60–90).
- The exact list of `<strategy_specific_checklist>` items that survived the trim — Plan 05 needs this as a reference when authoring the parallel sections for the other 5 critics.
- Any base-shadowing violations encountered (sub-test 1 OR H9 sub-test 2) + how they were resolved (informs Plan 05's executor).
- A note on whether the base prompt needed any adjustment to support the strategy lens cleanly (if yes, this is a load-bearing finding for Plan 05 — re-adjusting base mid-Wave-0 means re-running the spike pair).
- Optional pilot parity result (S4) — `missingCritical` count, `overlap` value, hand-inspection conclusion.
</output>
</content>
