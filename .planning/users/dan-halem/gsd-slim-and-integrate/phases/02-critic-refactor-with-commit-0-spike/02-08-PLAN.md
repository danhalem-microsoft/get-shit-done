---
phase: 02-critic-refactor-with-commit-0-spike
plan: 08
type: execute
wave: 5
depends_on: [1, 2, 3, 4, 5, 6, 7]
files_modified:
  - CHANGELOG.md
autonomous: false
requirements: [XCUT-03]
must_haves:
  truths:
    - "User can run `bazel test //integration/... //tests/... --test_tag_filters=phase-2-critic` and the entire scoped suite passes — all static guards (line-budget, no-base-shadowing, install-shared-dir, critic-aggregate-shape, critique-workflow-structure, walltime-ledger-schema) AND all live tests (critic-spike-passes, critic-batch-walltime, critic-fault-injection, critic-parity)."
    - "User can run `git tag --list 'gsd-slim-phase-2-critic'` and observe the tag exists, pointing at the commit where the full Phase 2 suite passed."
    - "User can read `CHANGELOG.md` and find a Phase 2 entry summarizing: (a) shared base prompt + 6 trimmed critic addendums (1731 → ≤700 lines), (b) parallel critic batch via `/gsd-review --critique`, (c) `gsd-tools.cjs critic-aggregate` subcommand for disk-based aggregation, (d) bad-subagent-name fault-injection test pattern for orchestrator skip-and-continue policy (no production-code changes), (e) re-install required for users with v1.36.x to pick up `agents/_shared/critic-base.md`."
  artifacts:
    - path: CHANGELOG.md
      provides: "Phase 2 entry with the bullet list above. Format follows existing CHANGELOG.md convention."
    - path: (git tag) gsd-slim-phase-2-critic
      provides: "Phase exit gate per ROADMAP §Phase 2 success criterion 5 + XCUT-01 cross-cutting requirement. Tag created ONLY after the full Bazel suite for phase-2-critic passes."
  key_links:
    - from: bazel test command
      to: all Phase 2 BUILD.bazel entries
      via: "--test_tag_filters=phase-2-critic resolves to: critic-line-budget, critic-no-base-shadowing, install-shared-dir, critic-aggregate-shape, critique-workflow-structure, walltime-ledger-schema, critic-spike-passes, critic-batch-walltime, critic-fault-injection, critic-parity, gsd-lifecycle (multi-tagged)"
      pattern: "phase-2-critic"
    - from: git tag gsd-slim-phase-2-critic
      to: HEAD commit on main after the suite passes
      via: "git tag -a gsd-slim-phase-2-critic -m '...'"
      pattern: "gsd-slim-phase-2-critic"
---

<objective>
Close Phase 2 with a single command (`bazel test //integration/... //tests/... --test_tag_filters=phase-2-critic`) running both the static and live test suites GREEN, then create the `gsd-slim-phase-2-critic` git tag and write the CHANGELOG entry. This is the phase exit gate per ROADMAP §Phase 2 success criterion 5 and XCUT-01.

Purpose: The phase is not "done" until both static and live suites pass with the phase tag. The git tag is the canonical record of phase completion that Phase 3 (and downstream phases) depend on per `Depends on: Phase 2 (gsd-slim-phase-2-critic git tag)` in ROADMAP.

Output: Bazel suite GREEN; git tag created; CHANGELOG entry committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/users/dan-halem/gsd-slim-and-integrate/REQUIREMENTS.md
@.planning/users/dan-halem/gsd-slim-and-integrate/ROADMAP.md
@.planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-RESEARCH.md
@.planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-07-SUMMARY.md
@CHANGELOG.md
@integration/BUILD.bazel
@tests/BUILD.bazel
@.planning/users/dan-halem/gsd-slim-and-integrate/phases/01-cull-with-wave-0-test-infrastructure/01-09-SUMMARY.md

<interfaces>
<!-- Phase 1 ROADMAP §Phase 1 plans/01-09 reference: same XCUT-01 git-tag pattern -->

<!-- Bazel suite scope (per RESEARCH §Validation Architecture): -->
bazel test //integration/... //tests/... --test_tag_filters=phase-2-critic

<!-- Expected target list (cross-check Plans 01–07 BUILD.bazel entries): -->
//tests:critic-line-budget
//tests:critic-no-base-shadowing
//tests:install-shared-dir
//tests:critic-aggregate-shape
//tests:critique-workflow-structure
//tests:walltime-ledger-schema
//integration:critic-spike-passes
//integration:critic-batch-walltime
//integration:critic-fault-injection
//integration:critic-parity
//integration:gsd-lifecycle (multi-tagged with phase-1-cull AND phase-2-critic per Plan 07)

<!-- ROADMAP §Phase 2 success criterion 5 — exit gate language -->
"phase exits with git tag `gsd-slim-phase-2-critic` only after both static and live test suites pass"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full Phase 2 Bazel suite + author CHANGELOG entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (full file — match existing entry style: heading format, bullet style, version section convention)
    - integration/BUILD.bazel (verify ALL Phase 2 entries are present)
    - tests/BUILD.bazel (verify ALL Phase 2 entries are present)
    - .planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-07-SUMMARY.md (parity results, fault-injection mechanism shipped, total cost)
    - .planning/users/dan-halem/gsd-slim-and-integrate/phases/01-cull-with-wave-0-test-infrastructure/01-09-SUMMARY.md (precedent — how Phase 1's CHANGELOG entry was structured)
  </read_first>
  <action>
**Step A — Run the full Phase 2 suite.**

```bash
bazel test //integration/... //tests/... --test_tag_filters=phase-2-critic 2>&1 | tee /tmp/phase-2-suite.log
```

Expected outcome: ALL targets PASSED. Capture the run summary (target count + pass/fail counts) into the SUMMARY.

If any target FAILS:
- Read the failure log carefully. Most likely failures:
  - parity FAIL on a specific critic → the trim dropped a critical-tier check; restore it in `agents/gsd-critic-{lens}.md` and re-run that critic's parity test (Plan 05 SUMMARY's keep-list is the reference).
  - walltime FAIL → spawn-timestamp budget tripped; re-run with `--test_arg=--verbose` to see actual delta; if persistent, it's a real Claude Code scheduler regression — escalate to user.
  - fault-injection FAIL → the orchestrator did not log the missing critic info finding; verify Plan 06's workflow Step 5 exactly matches the regex `\[INFO\][\s\S]*<lens>[\s\S]*did not produce CRITIQUE`.
  - line-budget / no-base-shadowing FAIL → re-run Plan 05 trim on the offending critic.
- If a fix is small (single-file edit), apply it and re-run the suite. If the fix is architectural, halt and escalate.

**Step B — Run the static-only sanity check too.**

```bash
node --test tests/critic-line-budget.test.cjs tests/critic-no-base-shadowing.test.cjs tests/install-shared-dir.test.cjs tests/critic-aggregate-shape.test.cjs tests/critique-workflow-structure.test.cjs tests/walltime-ledger-schema.test.cjs tests/cull-no-orphan-references.test.cjs tests/gsd-tools-dispatcher-reachable.test.cjs
```

All must exit 0. (This catches anything Bazel might have masked via stale caches.)

**Step C — Author CHANGELOG.md entry.**

Use the Edit tool. Insert a new section under the [Unreleased] block (or whatever the current convention is per the existing CHANGELOG):

```markdown
## Phase 2 — Critic refactor (with commit-0 spike)

- **Shared critic base prompt + 6 trimmed addendums.** Extracted role framing, severity rubric, finding-format schema (now with explicit `category` field), cross-flag rules, evidence requirements, and post-write verify directive into `agents/_shared/critic-base.md` (≤250 lines). Each of `agents/gsd-critic-{plan,code,scope,verify,discuss,strategy}.md` now opens with `@~/.claude/get-shit-done/agents/_shared/critic-base.md` and contains only lens-specific content (≤100 lines each). Total critic surface: 1,731 → ≤700 lines.
- **Parallel critic batch** via `/gsd-review --critique <phase>` — `get-shit-done/workflows/critique.md` (previously 0 bytes) emits all 6 `Task(subagent_type="gsd-critic-{lens}")` calls in a single contiguous block. Spawn-timestamp delta < 2s catches the [#7406 "claims parallel, executes serial"](https://github.com/anthropics/claude-code/issues/7406) failure mode.
- **Disk-based aggregation** via new `gsd-tools.cjs critic-aggregate` subcommand (`get-shit-done/bin/lib/critic-aggregate.cjs`). The orchestrator runs `gsd-sdk query critic-aggregate --phase $PHASE_ARG` instead of trusting parent-Task text summaries — mitigates [anthropics/claude-code#29181](https://github.com/anthropics/claude-code/issues/29181).
- **Skip-and-continue policy** when 1-of-N critic fails — orchestrator logs the missing critic as an info-severity finding (`category: missing-critic-output`) and aggregates the surviving critics. Exercised by `integration/critic-fault-injection.test.cjs` via the debug-only `--inject-fault <lens>` flag.  *(Or: alternative mechanism, per Plan 07 SUMMARY — adapt this bullet if the executor chose Option-b file-mutation.)*
- **Behavior parity** verified via `integration/critic-parity.test.cjs` — N=5 median per critic, ≥85% finding overlap by severity-bucketed key against Phase 1 baselines, no missing critical findings.
- **Walltime ledger** at `integration/test-fixtures/walltime-ledger.jsonl` now contains entries with `phase: "phase-2-critic"` (one per live test). Schema enforced by `tests/walltime-ledger-schema.test.cjs`.
- **Re-install required** for users on prior versions to pick up `agents/_shared/critic-base.md` — the install manifest now tracks shared agent fragments (per `bin/install.js` extension).

**Phase 2 git tag:** `gsd-slim-phase-2-critic` (created after `bazel test //integration/... //tests/... --test_tag_filters=phase-2-critic` passed).
```

Adapt bullet wording based on what actually shipped — Plan 07 may have used a different fault-injection mechanism than the planner-time `--inject-fault` decision, in which case adjust bullet 4. The critical detail is that the user can read CHANGELOG and learn what changed in this phase.
  </action>
  <verify>
    <automated>git tag --list 'gsd-slim-phase-2-critic' && grep -c "Phase 2" CHANGELOG.md && grep -c "gsd-slim-phase-2-critic" CHANGELOG.md && grep -c "agents/_shared/critic-base.md" CHANGELOG.md && grep -c "critic-aggregate" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - The Bazel suite invocation `bazel test //integration/... //tests/... --test_tag_filters=phase-2-critic` exits 0.
    - The static-only sanity check (Step B `node --test ...`) exits 0.
    - `grep -c "Phase 2" CHANGELOG.md` returns ≥ 1 (new section added).
    - `grep -c "agents/_shared/critic-base.md" CHANGELOG.md` returns ≥ 1.
    - `grep -c "critic-aggregate" CHANGELOG.md` returns ≥ 1.
    - `grep -c "1,731\|1731" CHANGELOG.md` returns ≥ 1 (the line-count baseline is documented).
    - `grep -c "gsd-slim-phase-2-critic" CHANGELOG.md` returns ≥ 1.
    - The CHANGELOG entry references at least 5 of the 7 bullets above (variability allowed for the fault-injection mechanism actually shipped).
  </acceptance_criteria>
  <done>
    Phase 2 suite green; CHANGELOG entry written; Task 2 (git tag + commit) ready.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 2 exit gate — verify suite GREEN, then create git tag gsd-slim-phase-2-critic</name>
  <files>(git tag — no file change beyond Task 1's CHANGELOG.md)</files>
  <read_first>
    - Output of `bazel test //integration/... //tests/... --test_tag_filters=phase-2-critic` (must show all targets PASSED)
    - .planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-07-SUMMARY.md (parity results — confirm ≥85% overlap, no missing critical, for all 6 critics)
    - tail of `integration/test-fixtures/walltime-ledger.jsonl` (≥4 new `phase-2-critic` entries from Plan 07)
    - .planning/users/dan-halem/gsd-slim-and-integrate/phases/01-cull-with-wave-0-test-infrastructure/01-09-SUMMARY.md (precedent for the Phase 1 git tag)
    - $HOME/.claude/CLAUDE.md (the MODULE.bazel.lock global rule — verify the suite did NOT regenerate the lockfile; if it did, `git checkout HEAD -- MODULE.bazel.lock` BEFORE creating the tag)
  </read_first>
  <action>
This is a CHECKPOINT — the human must visually confirm the Bazel suite GREEN before the git tag is created. The tag is the canonical phase-completion record; once created and pushed, downstream phases (3+) consume it.

**Step A — Pre-tag checks (executor runs these and shows the user):**

```bash
# 1. Bazel suite green
bazel test //integration/... //tests/... --test_tag_filters=phase-2-critic 2>&1 | tail -20

# 2. Walltime ledger has Phase 2 entries
grep -c '"phase":"phase-2-critic"' integration/test-fixtures/walltime-ledger.jsonl

# 3. CHANGELOG entry exists
grep -A 3 "Phase 2 — Critic refactor" CHANGELOG.md

# 4. MODULE.bazel.lock not modified (per global CLAUDE.md rule)
git diff MODULE.bazel.lock | head -5

# 5. Critic surface within budget
wc -l agents/_shared/critic-base.md agents/gsd-critic-*.md | tail -1
```

**Step B — Commit any pending Phase 2 changes (CHANGELOG.md from Task 1 + any plan SUMMARYs not yet committed).**

```bash
git status
gsd-sdk query commit "docs(phase-2): close phase 2 with summary + CHANGELOG" \
  CHANGELOG.md \
  .planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/*.md \
  -- # any other files staged
```

**Step C — Create the annotated tag at the commit just made.**

```bash
git tag -a gsd-slim-phase-2-critic -m "Phase 2 — Critic refactor (commit-0 spike + parallel batch + parity)

- Shared base prompt + 6 lens addendums (1731 → ≤700 lines)
- Parallel critic batch via /gsd-review --critique
- Disk-based aggregation via gsd-tools.cjs critic-aggregate (mitigates anthropics/claude-code#29181)
- Behavior parity verified vs Phase 1 baselines (≥85% overlap, no missing critical, N=5 median)
- Walltime ledger captures phase-2-critic entries

Suite passed: bazel test //integration/... //tests/... --test_tag_filters=phase-2-critic
"
```

**Step D — Verify the tag exists and points at the right commit.**

```bash
git show --stat gsd-slim-phase-2-critic | head -20
```

Do NOT push the tag without explicit user confirmation (`git push origin gsd-slim-phase-2-critic`). The push is a separate operation — leave it to the user unless they ask.
  </action>
  <what-built>
- Bazel scoped suite GREEN: `bazel test //integration/... //tests/... --test_tag_filters=phase-2-critic` exits 0.
- CHANGELOG.md Phase 2 entry committed.
- git tag `gsd-slim-phase-2-critic` created, annotated, pointing at the just-committed phase-closure commit.
  </what-built>
  <how-to-verify>
1. Suite output (Step A.1) shows ALL targets PASSED, no FAILED, no FLAKY.
2. Walltime ledger has ≥4 phase-2-critic entries (Step A.2).
3. CHANGELOG.md has the Phase 2 section (Step A.3).
4. MODULE.bazel.lock NOT modified (Step A.4 produces empty diff).
5. Critic line-count total ≤700 (Step A.5).
6. Tag exists: `git tag --list 'gsd-slim-phase-2-critic'` returns the tag name.
7. Tag annotation contains the bullet summary (Step D output).
  </how-to-verify>
  <resume-signal>Type "tag created — Phase 2 closed; ready for Phase 3 planning" or "tag NOT created — issue X must be resolved before exit".</resume-signal>
  <verify>
    <automated>git tag --list 'gsd-slim-phase-2-critic' | grep -c '^gsd-slim-phase-2-critic$' && git diff --quiet MODULE.bazel.lock && grep -c "Phase 2" CHANGELOG.md && grep -c '"phase":"phase-2-critic"' integration/test-fixtures/walltime-ledger.jsonl</automated>
  </verify>
  <acceptance_criteria>
    - `git tag --list 'gsd-slim-phase-2-critic'` returns exactly `gsd-slim-phase-2-critic` (one match).
    - `git show gsd-slim-phase-2-critic` exits 0 and shows the annotated tag message.
    - `git diff --quiet MODULE.bazel.lock` exits 0 (lockfile unchanged per global CLAUDE.md rule).
    - `grep -c '"phase":"phase-2-critic"' integration/test-fixtures/walltime-ledger.jsonl` returns ≥ 4.
    - `wc -l agents/_shared/critic-base.md agents/gsd-critic-*.md | tail -1` returns total ≤ 700.
    - Bazel suite from Step A.1 shows zero FAILED targets.
  </acceptance_criteria>
  <done>
    Phase 2 closed. The `gsd-slim-phase-2-critic` git tag is the canonical record. Phase 3 may begin (per ROADMAP §Phase 3 Depends on: Phase 2 (gsd-slim-phase-2-critic git tag)).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bazel test orchestrator → API key + cost budget | Live tests consume real cost; `phase-2-critic` filter must NOT silently include unrelated nightly suites. |
| `git tag` creation → ROADMAP dependency contract | Phase 3+ depend on this tag; creating it without GREEN suite would break the contract. |
| MODULE.bazel.lock side-effect from `bazel build` | Per global CLAUDE.md rule, the lockfile must not be staged on a passing-suite commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-A | Repudiation | tag created without GREEN suite | mitigate | Checkpoint forces human visual confirmation; acceptance check `bazel test ... | tail -20` must show all PASSED. |
| T-02-B | Tampering | MODULE.bazel.lock regenerated as side effect | mitigate | Acceptance check `git diff --quiet MODULE.bazel.lock`; if it fails, executor runs `git checkout HEAD -- MODULE.bazel.lock` per global CLAUDE.md rule. |
| T-02-C | Spoofing | tag name typo (e.g., `gsd-slim-phase-2-criticism`) | mitigate | Exact-match grep `'^gsd-slim-phase-2-critic$'` in acceptance check. |
| T-02-D | Information Disclosure | tag annotation leaks something sensitive | accept | Annotation only references file paths + bullet summary — no secrets. |
| T-02-E | Denial of Service | bazel suite exceeds budget | accept | The user explicitly runs this on their machine with their API key; budget is the user's call. Cost surfacing is in CHANGELOG bullet. |
</threat_model>

<verification>
1. `bazel test //integration/... //tests/... --test_tag_filters=phase-2-critic` exits 0 with all targets PASSED.
2. `git tag --list 'gsd-slim-phase-2-critic'` returns the tag.
3. `CHANGELOG.md` has the Phase 2 entry.
4. `MODULE.bazel.lock` unchanged.
5. Walltime ledger has ≥ 4 `phase-2-critic` entries.
6. `wc -l agents/_shared/critic-base.md agents/gsd-critic-*.md | tail -1` returns total ≤ 700.
</verification>

<success_criteria>
1. Phase 2 exit gate met per ROADMAP §Phase 2 success criterion 5: both static and live test suites for `phase-2-critic` pass.
2. Git tag `gsd-slim-phase-2-critic` exists and is annotated.
3. CHANGELOG.md communicates the change to users (re-install required note included).
4. The phase's contract with Phase 3+ is now ratified by the tag.
</success_criteria>

<output>
After completion, create `.planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-08-SUMMARY.md` documenting:
- Bazel suite output (target count + pass/fail/skipped counts).
- Total walltime ledger entries gained during Phase 2 (across all 7 prior plans + this one).
- Total Phase 2 live-test cost (USD) across all live runs.
- The exact git tag SHA (commit hash the tag points at).
- Confirmation MODULE.bazel.lock was unchanged.
- A "Phase 3 ready" or "Phase 3 BLOCKED on issue X" line for the orchestrator's next step.
</output>
