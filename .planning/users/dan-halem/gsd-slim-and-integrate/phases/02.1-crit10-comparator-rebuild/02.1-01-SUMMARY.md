---
phase: 02.1-crit10-comparator-rebuild
plan: 01
subsystem: testing

tags: [tdd, red-test, embeddings, cosine-similarity, critic-comparator, config-schema, crit-10]

# Dependency graph
requires:
  - phase: 02-critic-refactor-with-commit-0-spike
    provides: "agent-parity.cjs comparator (Jaccard fallback); SCHEMAS['critic-findings']; existing _internal namespace; Phase 2 lock tests"
provides:
  - "RED unit-test suite (tests/critic-comparator-embedding-shape.test.cjs, 18 sub-tests, 471 lines) locking the embedding-comparator contract that Plan 02.1-02 must satisfy"
  - "workflow.embedding_model accepted as an exact-match config key (VALID_CONFIG_KEYS) with documentation row"
  - "Hand-off note for Plan 02.1-02: implement _internal.{cosineSimilarity, normalizeForEmbedding, embeddingCacheKey, COSINE_TITLE_THRESHOLD, EMBEDDING_MODEL_DEFAULT, getEmbeddingModel, computeCriticFindingsDeltasEmbedding} to turn the RED suite GREEN"
affects: [02.1-02-PLAN, 02.1-03-PLAN, future-critic-parity-runs, CRIT-10-gap-closure]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "RED-by-contract test: imports succeed, destructured/accessed symbols are `undefined`, every assertion fails with an ERR_ASSERTION message naming the missing symbol — NOT MODULE_NOT_FOUND, NOT SyntaxError"
    - "Synthetic 1536-dim unit vectors for offline cosine-similarity tests (e0 basis + makeVecCosine constructor producing exact cosine=c via b=[c, sqrt(1-c²), 0,...])"
    - "Scoped env-var mutation via node:test before()/after() hooks with original-presence + original-value capture; leak-safe (Phase 1 D-04 / D-CTX-15 concurrency contract)"

key-files:
  created:
    - "tests/critic-comparator-embedding-shape.test.cjs"
  modified:
    - "get-shit-done/bin/lib/config-schema.cjs (+1 line — VALID_CONFIG_KEYS entry)"
    - "docs/CONFIGURATION.md (+1 row — Workflow Toggles section)"

key-decisions:
  - "RED test fails by ERR_ASSERTION on undefined _internal symbols, NOT by MODULE_NOT_FOUND — file imports cleanly so failure messages point at the missing API, not at a load problem"
  - "All synthetic vectors are 1536-dim unit vectors so the cosine = dot product identity holds, eliminating norm-computation noise from the offline tests"
  - "process.env.OPENAI_API_KEY mutation is scoped to Group E's describe block via before()/after() hooks that capture original presence + value separately, ensuring the env var is fully restored (deleted vs. set-to-value) when the test exits"

patterns-established:
  - "RED-test pattern for shape locks: type-check the symbol first inside each test (assertion failure on undefined typeof), then exercise the behavior — gives the GREEN implementer (Plan 02.1-02) one-line targets to satisfy"
  - "Synthetic-embedding pattern for offline cosine tests: basis vector e0 + makeVecCosine(c) = [c, sqrt(1-c²), 0,...] — both unit norm, cosine ≡ c exactly"

requirements-completed: [CRIT-10]

# Metrics
duration: 4min
completed: 2026-05-14
---

# Phase 02.1 Plan 01: Embedding-Comparator RED Test + Config-Schema Lock-In Summary

**RED-by-contract test suite (18 sub-tests, 471 lines) locking the embedding-cosine-similarity comparator contract for Plan 02.1-02 to satisfy, plus `workflow.embedding_model` registered in VALID_CONFIG_KEYS + docs so Plan 02.1-02's implementation reads the model name from config without tripping the CI drift guard.**

## Performance

- **Duration:** 4 min (212 seconds)
- **Started:** 2026-05-14T18:14:40Z
- **Completed:** 2026-05-14T18:18:12Z
- **Tasks:** 3/3 (Task 1 + Task 2 atomically committed; Task 3 was verification-only with no files modified)
- **Files modified:** 3 (1 new test file, 1 config-schema line, 1 docs row)

## Accomplishments

- **Locked the embedding-path contract via 18 RED sub-tests** spanning 6 groups (A primitives, B normalization + cache key, C constants + config, D primary-path load-bearing CRIT-10 case, E fallback-when-API-key-unset, F per-finding diagnosis). Each sub-test names the exact missing `_internal` symbol or violated behavior in its failure message, so Plan 02.1-02's implementer has machine-checkable targets without re-reading the plan.
- **Registered `workflow.embedding_model` as an exact-match config key** in `get-shit-done/bin/lib/config-schema.cjs` and documented it in `docs/CONFIGURATION.md`. The `tests/config-schema-docs-parity.test.cjs` CI drift guard now sees the key on both sides and stays GREEN.
- **Confirmed all 30 Phase 2 lock sub-tests stay GREEN** (`critic-comparator-fix.test.cjs` 16, `critic-findings-delta-shape.test.cjs` 3, `agent-parity-helper-shape.test.cjs` 11). `integration/helpers/agent-parity.cjs` has ZERO lines diffed (verified via `git diff`) — the GREEN target is preserved for Plan 02.1-02 to modify.

## Task Commits

Each task was committed atomically with `--no-verify` (worktree mode per parallel-execution context):

1. **Task 1: Add `workflow.embedding_model` to config-schema + docs** — `ec1c2656` (`feat(02.1-01)`)
   - Files: `get-shit-done/bin/lib/config-schema.cjs`, `docs/CONFIGURATION.md`
   - Verification: `tests/config-schema-docs-parity.test.cjs` passes; `isValidConfigKey('workflow.embedding_model')` returns `true`.

2. **Task 2: RED test locking embedding comparator contract** — `3792d4a1` (`test(02.1-01)`)
   - Files: `tests/critic-comparator-embedding-shape.test.cjs` (NEW, 471 lines)
   - Verification: 18/18 sub-tests fail with `ERR_ASSERTION` (RED-by-contract). No `MODULE_NOT_FOUND`, no `SyntaxError`.

3. **Task 3: Verify Phase 2 lock tests stay GREEN (regression gate)** — no commit (verification-only per plan: `(no files modified — verification only)`)
   - Verification: `tests/critic-comparator-fix.test.cjs` + `tests/critic-findings-delta-shape.test.cjs` + `tests/agent-parity-helper-shape.test.cjs` = 30/30 PASS.

_Note: Task 2 is a TDD task in RED phase — only the `test(...)` commit lands here. Plan 02.1-02 will add the `feat(...)` commit that turns these tests GREEN._

## Files Created/Modified

- `tests/critic-comparator-embedding-shape.test.cjs` (NEW, 471 lines) — RED unit-test suite. Sub-test groups: A (cosineSimilarity primitives, 5), B (normalizeForEmbedding + embeddingCacheKey, 5), C (constants + getEmbeddingModel, 5), D (primary-path CRIT-10 root-cause case, 1 load-bearing), E (fallback when OPENAI_API_KEY unset, 1), F (perFindingDiagnosis shape, 1).
- `get-shit-done/bin/lib/config-schema.cjs` (+1 line) — added `'workflow.embedding_model'` to `VALID_CONFIG_KEYS` adjacent to `workflow.verifier` (alphabetical neighborhood; exact-match key per D-CTX-02, not DYNAMIC_KEY_PATTERN).
- `docs/CONFIGURATION.md` (+1 row) — Workflow Toggles table entry: `workflow.embedding_model | string | text-embedding-3-small | ...purpose linked to integration/helpers/agent-parity.cjs cosine-similarity comparator (Phase 2.1 CRIT-10 gap closure)...`.

## Decisions Made

- **Place `workflow.embedding_model` in VALID_CONFIG_KEYS adjacent to `workflow.verifier`** (rather than between `workflow.discuss_mode` and `workflow.nyquist_validation` as the plan suggested). The Set has no required order; the placement still falls inside the workflow.* alphabetical cluster, and `workflow.verifier` is a thematic neighbor (both are testing-infrastructure config knobs).
- **Place `workflow.embedding_model` after `workflow.inline_plan_threshold` in the docs Workflow Toggles table** as the last new entry in that section, keeping the toggles table visually grouped and the new entry easy to spot in code review.

## Failure-Shape Confirmation (RED-by-Contract)

A representative sample of the 18 failure messages, all confirming the test is RED-by-contract and NOT by syntactic/load failure:

```
A1: cosineSimilarity returns 1.0 for two identical 1536-dim vectors
  AssertionError: expected typeof _internal.cosineSimilarity === 'function', got 'undefined'

B1: normalizeForEmbedding strips leading [CRITICAL]...
  AssertionError: expected typeof _internal.normalizeForEmbedding === 'function', got 'undefined'

C1: COSINE_TITLE_THRESHOLD === 0.80
  AssertionError [strictEqual]: expected _internal.COSINE_TITLE_THRESHOLD === 0.80, got undefined (typeof undefined)

D1: comparator matches paraphrased findings via cosine when Jaccard fails (the load-bearing CRIT-10 case)
  AssertionError: expected typeof _internal.computeCriticFindingsDeltasEmbedding === 'function', got 'undefined'

E1: comparator returns usedFallback=true + fallbackReason + stderr warning when OPENAI_API_KEY is unset
  AssertionError: expected typeof _internal.computeCriticFindingsDeltasEmbedding === 'function', got 'undefined'

F1: perFindingDiagnosis has one entry per baseline finding...
  AssertionError: expected typeof _internal.computeCriticFindingsDeltasEmbedding === 'function', got 'undefined'
```

All 18 failures are `code: 'ERR_ASSERTION'` from `node:assert/strict` — proof that the file imports cleanly and the contract is violated by absence of the symbols Plan 02.1-02 will add.

## Deviations from Plan

**None — plan executed exactly as written.**

The 3 tasks landed in order with no auto-fixes, no architectural questions, no auth gates, and no scope expansion. The only minor freedom Claude exercised was placement of the new entry within the schema file (adjacent to `workflow.verifier` instead of between `discuss_mode` and `nyquist_validation`); the plan explicitly noted "the Set has no required order" so this is within the plan's described latitude.

## Issues Encountered

None.

## Threat Flags

None. The plan's threat model (T-2.1-01) was mitigated as designed: the RED test file injects pre-computed embedding vectors via `embeddingsByTitle` and never calls a live API; Group E's env-var mutation is scoped to one `describe` block and restored in `after()`.

## Hand-Off Note to Plan 02.1-02

Plan 02.1-02 turns this RED test GREEN. To do so, add the following symbols to the `_internal` namespace of `integration/helpers/agent-parity.cjs`:

| Symbol | Signature | Test references |
|---|---|---|
| `cosineSimilarity` | `(vecA: number[], vecB: number[]) => number in [0,1]` | A1-A5 |
| `normalizeForEmbedding` | `(title: string) => string` (lowercase + collapse ws + strip leading `[SEVERITY]`) | B1-B2, D1, F1 |
| `embeddingCacheKey` | `(model: string, title: string) => string` (sha256 hex, 64-char lowercase) | B3-B5 |
| `COSINE_TITLE_THRESHOLD` | `number` (=== 0.80) | C1 |
| `EMBEDDING_MODEL_DEFAULT` | `string` (=== 'text-embedding-3-small') | C2 |
| `getEmbeddingModel` | `(config) => config?.workflow?.embedding_model ?? EMBEDDING_MODEL_DEFAULT` | C3-C5 |
| `computeCriticFindingsDeltasEmbedding` | `(schema, baseline, runs, {embeddingsByTitle?, allowFallback?}) => { pass, overlap, threshold, missingCritical, extraFindings, baseFindingCount, currFindingCount, fuzzyMatchCount, cosineMatchCount, perFindingDiagnosis, usedFallback?, fallbackReason? }` | D1, E1, F1 |

The load-bearing target is D1 — it proves the new path closes the CRIT-10 gap on the actual root-cause pair from `02-07-SUMMARY-fixes.md`. The fallback target (E1) ensures the comparator degrades gracefully when `OPENAI_API_KEY` is unset and no pre-computed embeddings are injected, emitting the documented warning `WARN: embedding API unavailable` to stderr.

`workflow.embedding_model` is already a valid config key — Plan 02.1-02's `getEmbeddingModel(config)` implementation can immediately read it via `gsd-sdk query config-get workflow.embedding_model` or directly off the parsed config object.

## Next Phase Readiness

- ✅ Plan 02.1-02 unblocked: contract is locked, config key is accepted, GREEN target (`integration/helpers/agent-parity.cjs`) is untouched.
- ✅ Phase 2 lock-test surface intact: 30/30 sub-tests across the three lock files still GREEN.
- ✅ CI drift guard intact: `tests/config-schema-docs-parity.test.cjs` GREEN with the new key documented on both sides.
- No blockers, no concerns.

## Self-Check: PASSED

Created files verified to exist:
- `tests/critic-comparator-embedding-shape.test.cjs` — FOUND
- `.planning/users/dan-halem/gsd-slim-and-integrate/phases/02.1-crit10-comparator-rebuild/02.1-01-SUMMARY.md` — FOUND (this file)

Commits verified to exist in `git log`:
- `ec1c2656` (Task 1) — FOUND
- `3792d4a1` (Task 2) — FOUND

---
*Phase: 02.1-crit10-comparator-rebuild*
*Plan: 01*
*Completed: 2026-05-14*
