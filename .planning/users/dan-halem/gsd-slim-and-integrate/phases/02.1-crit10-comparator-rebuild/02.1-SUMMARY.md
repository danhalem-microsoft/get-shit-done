---
phase: "02.1-crit10-comparator-rebuild"
plan: "03"
subsystem: integration/critic-parity (embedding-cosine comparator verification)
tags: [phase-2.1, wave-3, CRIT-10, gap_closure, gaps_found, MR-002]
requirements: [CRIT-10]
provides:
  - tests/walltime-ledger-schema.test.cjs PHASE_RE accepting decimal phase tags (XCUT-03)
  - N=5 parity measurement across 6 trimmed critics under Azure embedding cosine
  - VERIFICATION.md with per-critic outcome (1/6 PASS, 5/6 sub-threshold)
  - 30 walltime-ledger entries tagged phase-2.1-crit10
affects:
  - Plan 02-08 (Phase 2 exit gate) — CAN run; phase-2-critic Bazel suite is independent of the parity finding
  - Phase 2.2 (potential) — H8 variance investigation needed to distinguish trim-regression vs LLM nondeterminism
metrics:
  ledger_entries_added: 30
  test_duration_ms: 1351631
  test_duration_min: 22.5
  live_spend_usd_estimated: 9
  embedding_api_spend_usd_estimated: 0.006
  parity_pass_count: 1
  parity_fail_count: 5
status: complete-with-gaps
completed: "2026-05-14"
---

# Phase 2.1 Plan 03 — N=5 Parity Verification with Azure Embedding Comparator

This plan was reshaped in flight per **MR-002 (scope creep in autonomous planning)** correction: original 5-task scope was reduced to 3 (Task 1.5 + Task 3 + Task 4 + Task 5; Task 2 H8 variance DROPPED as unauthorized).

## What shipped

| Commit | What |
|--------|------|
| `9bce2f98` | Task 1.5: walltime-ledger PHASE_RE accepts decimal phase tags (XCUT-03) |
| (this) | Task 3 result captured in VERIFICATION.md + 30 ledger entries |
| (this) | Task 4: VERIFICATION.md (per-critic table + diagnosis + criteria mapping) |
| (this) | Task 5: this SUMMARY.md |

## Live test outcome

Ran `integration/critic-parity.test.cjs` end-to-end with Azure OpenAI embedding cosine similarity as the comparator (per Plan 02.1-02's implementation + the Plan 02.1-02 follow-up Azure-support patch at commit `24ada869`).

| Critic | Overlap | Status |
|--------|---------|--------|
| critic-plan | ≥0.85 | ✅ |
| critic-code | 0.25 | ❌ |
| critic-scope | 0.00 | ❌ |
| critic-verify | 0.29 | ❌ |
| critic-discuss | 0.00 | ❌ |
| critic-strategy | 0.33 | ❌ |

**1 of 6 passes the 0.85 threshold.** This is a REAL finding — embedding cosine solved the synonym/paraphrase blindness of Jaccard but did NOT close the deeper gap. See VERIFICATION.md "Diagnosis" for the two non-exclusive hypotheses (trim-induced behavior drift vs LLM nondeterminism at finding-set level).

## What this DOES verify

- The Azure-mode embedding comparator works end-to-end. The 1/6 pass and the non-zero overlaps for 3 of the 5 fails (0.25, 0.29, 0.33) confirm the comparator is producing real signal, not trivial-pass or always-zero behavior.
- The PHASE_RE schema update (commit `9bce2f98`) accepts the new `phase-2.1-crit10` tag — schema test stays GREEN against the 136-entry ledger.
- The bridging pattern from `~/.claude/agents` to the worktree is solid; teardown verified with md5sum match.
- The fallback path (Azure → Jaccard with stderr WARN) is structurally tested by Plan 02.1-02's 24 unit sub-tests.

## What this DOES NOT verify

- That the trimmed critics produce findings that are SEMANTICALLY EQUIVALENT to the Phase 1 baselines. The 5/6 sub-threshold says we have an open question about this, not an answer.
- The choice of 0.85 as the right cosine threshold. Without H8 variance data, we don't know whether 0.85 is too strict, just right, or too loose for the SAME-critic-twice-against-baseline distribution.

## Scope reduction note (MR-002)

Original Plan 02.1-03 had 5 tasks. Task 2 ("H8 variance estimate, 3 runs per critic, informational only") was DROPPED because it had not been explicitly authorized by the user. The user picked an AskUserQuestion option labeled "Embedding-based comparator (Recommended)" with `~$8 N=5 verification` as the cost figure — the orchestrator had subsequently inflated CONTEXT.md to include H8 plus a $20 budget cap and called it "pre-authorized." The user correction triggered MR-002: option-label scope IS the boundary; cost is informational; "pre-authorized" framing must trace to verbatim user text.

With Plan 02.1-03's parity result now showing 5/6 sub-threshold, H8 is retroactively load-bearing for diagnosis. The right next move is either:
(a) Open a focused Phase 2.2 ("CRIT-10 H8 variance investigation") with explicit authorization for the H8 scope, OR
(b) Accept embedding-cosine at the empirical floor (~0.30 per the worst case) as the documented threshold for current measurement methodology, OR
(c) Investigate whether Plan 02-05's bulk trim caused real behavior drift (read each lens addendum carefully against its Phase 1 prose; look for missing lens-distinctive cues).

Path (b) is the cheapest; path (a) is the most rigorous; path (c) is the most ambitious. User decides.

## Deviations

- **Task 2 dropped** per MR-002 (documented above).
- **Plan ran inline in orchestrator shell**, NOT in a worktree, because of two harness issues discovered during this session: (1) Agent subprocesses with `isolation: worktree` don't inherit orchestrator-sourced env vars, blocking Azure mode; (2) the harness's worktree-base resolution picked an unrelated chore commit (`55193ac6`) instead of orchestrator HEAD. Both are documented in `.continue-here.md`. The inline path was cleaner given the constraints.

## Phase 2.1 result for the project

Phase 2.1 ships **complete-with-gaps**: the embedding comparator MECHANISM is correct + locked + tested with mocks; live verification reveals a deeper measurement-methodology question. Plan 02-08 (Phase 2 exit gate) is NOT blocked by this — it can run with the embedding path on tap; the `phase-2-critic` Bazel tag covers the static-test surface (line-budget, no-base-shadowing, fault-injection) which is all green.

The parity test stays in the suite as a real signal, marked `gaps_found` per Phase 2.1 contract.

## Self-Check: PASSED

Files written:
- `.planning/users/dan-halem/gsd-slim-and-integrate/phases/02.1-crit10-comparator-rebuild/02.1-VERIFICATION.md` — created
- `.planning/users/dan-halem/gsd-slim-and-integrate/phases/02.1-crit10-comparator-rebuild/02.1-SUMMARY.md` — this file
- `integration/test-fixtures/walltime-ledger.jsonl` — +30 entries tagged `phase-2.1-crit10`

Bridging torn down: `bash /tmp/gsd-bridging-teardown.sh` succeeded; md5sum check verified host critic files match repo state. Symlinks removed; backup files in `/tmp` deleted.

## TDD Gate Compliance

Plan frontmatter does not specify `type: tdd`. The plan ran live verification against the comparator implementation shipped in Plan 02.1-02 (which DID have type: tdd discipline — RED tests in Plan 02.1-01 turned GREEN in Plan 02.1-02). No new RED→GREEN cycle was required for this verification plan.
