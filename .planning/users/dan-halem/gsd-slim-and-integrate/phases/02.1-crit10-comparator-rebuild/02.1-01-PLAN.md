---
phase: 02.1-crit10-comparator-rebuild
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/critic-comparator-embedding-shape.test.cjs
  - get-shit-done/bin/lib/config-schema.cjs
  - docs/CONFIGURATION.md
autonomous: true
requirements: [CRIT-10]
gap_closure: true
must_haves:
  truths:
    - "User can run `node --test tests/critic-comparator-embedding-shape.test.cjs` and observe ALL sub-tests fail with errors that prove the embedding code path is the missing piece (NOT missing-import errors, NOT bag-of-words drift). Each failure message names the expected exported symbol or behavior, so executor Plan 02.1-02 has a precise GREEN target. (Per D-CTX-09: this test locks the embedding-path JSON contract, fallback behavior, threshold constant, and model-name-from-config.)"
    - "User can run `node --test tests/critic-comparator-fix.test.cjs` (16 sub-tests, Phase 2 lock) and observe ALL 16 still PASS — the new RED tests do NOT require modifying agent-parity.cjs yet, so the Phase 2 lock-test surface remains untouched. (Per D-CTX-07: Phase 2 lock tests STAY GREEN.)"
    - "User can run `node --test tests/critic-findings-delta-shape.test.cjs` (Plan 02-03 B6 lock) and observe ALL sub-tests still PASS — adding `workflow.embedding_model` to config-schema does NOT touch any code path this test exercises."
    - "User can run `node --test tests/agent-parity-helper-shape.test.cjs` (Plan 02-01 TEST-02 contract) and observe ALL sub-tests still PASS — the public surface (`runAgentParity`, `SCHEMAS`, `loadBaseline`, `saveBaseline`, `pickMedianByDuration`, `BASELINES_DIR`) is unchanged by this plan."
    - "User can run `node --test tests/config-schema-docs-parity.test.cjs` and observe it PASSES — `workflow.embedding_model` is in `VALID_CONFIG_KEYS` AND documented in `docs/CONFIGURATION.md` (CI drift guard contract per config-schema.cjs line 7-12)."
  artifacts:
    - path: tests/critic-comparator-embedding-shape.test.cjs
      provides: "RED unit-test suite locking the embedding-path contract for Plan 02.1-02 to implement. Sub-tests assert: (1) `_internal.cosineSimilarity(vecA, vecB)` returns 1.0 for identical 1536-dim vectors and ~0 for orthogonal vectors; (2) `_internal.normalizeForEmbedding(title)` lowercases + collapses whitespace + strips leading severity markers like `[CRITICAL]`; (3) `_internal.embeddingCacheKey(model, title)` returns `sha256(model + ':' + normalized)` — deterministic, changes when EITHER input changes; (4) `_internal.COSINE_TITLE_THRESHOLD === 0.80`; (5) `_internal.EMBEDDING_MODEL_DEFAULT === 'text-embedding-3-small'` (the DEFAULT — not a hardcoded usage); (6) `_internal.getEmbeddingModel(config)` returns `config.workflow.embedding_model` when set, else the default; (7) `_internal.computeCriticFindingsDeltasEmbedding(schema, baseline, runs, {embeddingsByTitle})` exists and accepts a pre-computed embeddings map (injectable for offline unit testing — NO live API call from unit tests); (8) when `embeddingsByTitle` shows cosine ≥ 0.80 between a baseline and candidate title, the pair matches even when their Jaccard would be <0.5; (9) when `embeddingsByTitle` is empty AND `OPENAI_API_KEY` is unset, calling the comparator with `allowFallback:true` returns a result with `usedFallback: true` AND `fallbackReason: 'OPENAI_API_KEY unset'` AND emits a stderr warning matching `/WARN: embedding API unavailable/`; (10) the comparator's return shape extends Phase 2's (still has `pass`, `overlap`, `threshold`, `missingCritical`, `extraFindings`, `baseFindingCount`, `currFindingCount`, `fuzzyMatchCount`) and ADDS `cosineMatchCount: number`, `perFindingDiagnosis: Array<{baselineId, bestCandidateId, cosine}>` (D-CTX-12)."
      min_lines: 200
      exports: ["test runner via node:test"]
    - path: get-shit-done/bin/lib/config-schema.cjs
      provides: "Added `'workflow.embedding_model'` to the `VALID_CONFIG_KEYS` Set so `gsd-sdk query config-set workflow.embedding_model text-embedding-3-small` is accepted. Insert alphabetically near other `workflow.*` entries; do NOT use a dynamic pattern (this is an exact-match key per D-CTX-02)."
      contains: "workflow.embedding_model"
    - path: docs/CONFIGURATION.md
      provides: "Documentation entry for `workflow.embedding_model` (string, default `text-embedding-3-small`, used by `integration/helpers/agent-parity.cjs` cosine-similarity comparator). Required so `tests/config-schema-docs-parity.test.cjs` (the CI drift guard) stays GREEN."
      contains: "workflow.embedding_model"
  key_links:
    - from: tests/critic-comparator-embedding-shape.test.cjs
      to: integration/helpers/agent-parity.cjs (NOT YET MODIFIED — this is the GREEN target for Plan 02.1-02)
      via: "require('../integration/helpers/agent-parity.cjs')._internal — destructured imports for `cosineSimilarity`, `normalizeForEmbedding`, `embeddingCacheKey`, `COSINE_TITLE_THRESHOLD`, `EMBEDDING_MODEL_DEFAULT`, `getEmbeddingModel`, `computeCriticFindingsDeltasEmbedding`. These symbols DO NOT EXIST YET — the test fails by `require()` returning a module where the destructured names are `undefined`. That undefined-symbol failure IS the RED signal."
      pattern: "_internal\\.(cosineSimilarity|normalizeForEmbedding|embeddingCacheKey|COSINE_TITLE_THRESHOLD|EMBEDDING_MODEL_DEFAULT|getEmbeddingModel|computeCriticFindingsDeltasEmbedding)"
    - from: get-shit-done/bin/lib/config-schema.cjs
      to: docs/CONFIGURATION.md
      via: "tests/config-schema-docs-parity.test.cjs CI drift guard — every key in VALID_CONFIG_KEYS must appear in CONFIGURATION.md and vice versa."
      pattern: "workflow\\.embedding_model"
threat_model:
  trust_boundaries: []
  threats:
    - id: T-2.1-01
      category: "Tampering"
      component: "tests/critic-comparator-embedding-shape.test.cjs"
      disposition: mitigate
      mitigation: "Test injects pre-computed embedding vectors via the `embeddingsByTitle` parameter — NO live API call from unit tests. This means an attacker cannot use the test to exfiltrate `OPENAI_API_KEY` by tricking the test runner into making outbound requests. The fallback-path sub-test deliberately UNSETS the env var inside the test scope (via `delete process.env.OPENAI_API_KEY` in a `before()` hook with `after()` restore) — no network call attempted in either branch."
threat_model_disposition_check: "Every threat above has explicit disposition (mitigate)."
---

<objective>
Author the RED unit-test suite that locks the embedding-comparator contract Plan 02.1-02 must implement, and add `workflow.embedding_model` to the config-schema/docs so Plan 02.1-02's implementation can read the model name from config without tripping the schema-docs parity test.

This plan is RED-ONLY — it produces failing tests that prove the embedding code path is the missing piece. Plan 02.1-02 then turns those tests GREEN.

Purpose: Without the lock test landed first, Plan 02.1-02's implementer has no machine-checkable contract to build against — and the bag-of-words comparator from Phase 2 would silently remain the default. The RED test forces the new code path to exist with the exact shape the parity test expects.

Per CONTEXT.md D-CTX-09: "New test: `tests/critic-comparator-embedding-shape.test.cjs` — locks the embedding-path JSON contract, fallback behavior, threshold constant, model name from config."

Per CONTEXT.md D-CTX-07: existing Phase 2 lock tests (3 named files) MUST stay GREEN. This plan adds a NEW test file and a NEW config-schema entry; it touches NEITHER `integration/helpers/agent-parity.cjs` NOR any of the 3 lock-test files.

Output: One new test file (200+ lines, ~10 sub-tests, all RED), one config-schema entry, one CONFIGURATION.md entry. Phase 2 lock test surface remains 100% intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/users/dan-halem/gsd-slim-and-integrate/PROJECT.md
@.planning/users/dan-halem/gsd-slim-and-integrate/ROADMAP.md
@.planning/users/dan-halem/gsd-slim-and-integrate/STATE.md
@.planning/users/dan-halem/gsd-slim-and-integrate/REQUIREMENTS.md
@.planning/users/dan-halem/gsd-slim-and-integrate/phases/02.1-crit10-comparator-rebuild/02.1-CONTEXT.md
@.planning/users/dan-halem/gsd-slim-and-integrate/phases/02-critic-refactor-with-commit-0-spike/02-07-SUMMARY-fixes.md
@integration/helpers/agent-parity.cjs
@tests/critic-comparator-fix.test.cjs
@tests/critic-findings-delta-shape.test.cjs
@tests/agent-parity-helper-shape.test.cjs
@get-shit-done/bin/lib/config-schema.cjs

<interfaces>
<!-- Key contracts for Plan 02.1-02 to satisfy. Plan 02.1-01 locks these via RED tests; Plan 02.1-02 turns them GREEN. -->

Existing public surface (from `integration/helpers/agent-parity.cjs` — MUST NOT change):

```javascript
module.exports = {
  runAgentParity,    // unchanged
  SCHEMAS,           // unchanged (still has 'critic-findings' kind with threshold: 0.85)
  BASELINES_DIR,
  loadBaseline,
  saveBaseline,
  pickMedianByDuration,
  _internal: {
    // EXISTING (Phase 2 — keep):
    extractFindingsFromText,
    extractCategoryFromTitle,
    normalizeSeverity,
    titleWordBag,
    jaccardSimilarity,
    computeCriticFindingsDeltas,   // Phase 2 comparator — stays as the FALLBACK
    FUZZY_TITLE_THRESHOLD,         // = 0.7 (Jaccard threshold, fallback only)
    // NEW (Plan 02.1-02 will add — Plan 02.1-01 tests REQUIRE these exports):
    cosineSimilarity,                       // (vecA: number[], vecB: number[]) => number in [0,1]
    normalizeForEmbedding,                  // (title: string) => string  (lowercase + collapse ws + strip leading [SEVERITY])
    embeddingCacheKey,                      // (model: string, title: string) => string  (sha256 hex of `${model}:${normalized}`)
    COSINE_TITLE_THRESHOLD,                 // === 0.80
    EMBEDDING_MODEL_DEFAULT,                // === 'text-embedding-3-small'
    getEmbeddingModel,                      // (config) => config?.workflow?.embedding_model ?? EMBEDDING_MODEL_DEFAULT
    computeCriticFindingsDeltasEmbedding,   // (schema, baseline, runs, {embeddingsByTitle, allowFallback}) => { pass, overlap, threshold, missingCritical, extraFindings, baseFindingCount, currFindingCount, fuzzyMatchCount, cosineMatchCount, perFindingDiagnosis, usedFallback?, fallbackReason? }
  },
};
```

Test runtime model — the unit test MUST NOT hit the OpenAI API:

```javascript
// Inside tests/critic-comparator-embedding-shape.test.cjs, the comparator test passes
// a pre-built embeddings map keyed by the NORMALIZED title:
const embeddingsByTitle = new Map([
  [normalizeForEmbedding('No input validation on req.body.email'),  vec1],
  [normalizeForEmbedding('Missing input validation on email parameter'), vec2],
]);
// vec1 and vec2 are synthetic 1536-dim vectors crafted to have cosine ≈ 0.87
// (above the 0.80 threshold). The test asserts the comparator matches the pair.
```

Schema threshold reinterpretation (locked in Plan 02.1-02, not here):
- `SCHEMAS['critic-findings'].threshold` remains `0.85` (no schema-shape change).
- The comparator interprets `threshold` as the OVERLAP gate (>=85% of baseline findings must match by EITHER exact bucket, fuzzy Jaccard, OR cosine similarity ≥ 0.80).
- The 0.80 cosine threshold for INDIVIDUAL title matches is separate from the 0.85 OVERLAP threshold. This separation is documented in code via the `COSINE_TITLE_THRESHOLD` constant.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add workflow.embedding_model to config-schema + docs</name>
  <files>get-shit-done/bin/lib/config-schema.cjs, docs/CONFIGURATION.md</files>
  <behavior>
    - `'workflow.embedding_model'` is present in `VALID_CONFIG_KEYS` (exact-match Set in config-schema.cjs).
    - `docs/CONFIGURATION.md` documents the key (purpose, type, default, example), satisfying the CI drift guard at `tests/config-schema-docs-parity.test.cjs`.
    - `node --test tests/config-schema-docs-parity.test.cjs` PASSES after both edits.
    - `isValidConfigKey('workflow.embedding_model')` returns `true`.
  </behavior>
  <action>
    1. Add `'workflow.embedding_model'` to the `VALID_CONFIG_KEYS` Set in `get-shit-done/bin/lib/config-schema.cjs`. Place it adjacent to other `workflow.*` keys (alphabetical neighborhood — between `workflow.discuss_mode` and `workflow.nyquist_validation` works; the Set has no required order, but the schema file is human-readable). Use exact-match, NOT a DYNAMIC_KEY_PATTERN entry — per D-CTX-02 this is a fixed key, not a parameterized one.

    2. Add a documented entry in `docs/CONFIGURATION.md` under whichever section currently documents the other `workflow.*` keys (most likely the "Workflow Settings" section — confirm by grep `workflow.tdd_mode` first). The entry must include:
       - Key name: `workflow.embedding_model`
       - Type: `string`
       - Default: `text-embedding-3-small`
       - Purpose: "Embedding model used by `integration/helpers/agent-parity.cjs` for cosine-similarity-based critic finding comparison (Phase 2.1 — CRIT-10 gap closure). Used by the comparator's primary path when `OPENAI_API_KEY` is set; ignored on the Jaccard fallback path."
       - Example: ``gsd-sdk query config-set workflow.embedding_model text-embedding-3-small``
       - Reference link: cite `.planning/users/dan-halem/gsd-slim-and-integrate/phases/02.1-crit10-comparator-rebuild/02.1-CONTEXT.md` D-CTX-02.

    3. Implementation per D-CTX-02 / D-CTX-15: no other files touched. No code in `agent-parity.cjs` is modified by this task.
  </action>
  <verify>
    <automated>node --test tests/config-schema-docs-parity.test.cjs &amp;&amp; node -e "const {isValidConfigKey} = require('./get-shit-done/bin/lib/config-schema.cjs'); if (!isValidConfigKey('workflow.embedding_model')) { console.error('FAIL: workflow.embedding_model rejected by isValidConfigKey'); process.exit(1); } console.log('PASS: workflow.embedding_model accepted');"</automated>
  </verify>
  <done>
    `node --test tests/config-schema-docs-parity.test.cjs` exits 0. `isValidConfigKey('workflow.embedding_model')` returns `true`. `grep -F "workflow.embedding_model" docs/CONFIGURATION.md` finds the documentation entry. `grep -F "workflow.embedding_model" get-shit-done/bin/lib/config-schema.cjs` finds the schema entry.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author RED test suite locking the embedding comparator contract</name>
  <files>tests/critic-comparator-embedding-shape.test.cjs</files>
  <behavior>
    Each `test()` block fails with a `node:assert`-shaped error message naming the missing symbol or behavior, NOT a `Cannot find module` or `SyntaxError`. This guarantees the failures are RED-by-contract (the file imports cleanly; the contract is violated by absence of symbols).

    Sub-tests (each is its own `test('...')` block):

    **Group A — primitives:**
    1. `cosineSimilarity returns 1.0 for two identical 1536-dim vectors`
    2. `cosineSimilarity returns ~0 (within 1e-9) for two orthogonal vectors` — use [1,0,...] and [0,1,0,...] (shrink to 1536 dims with zero-padding)
    3. `cosineSimilarity is symmetric: cos(a,b) === cos(b,a)` for arbitrary vectors
    4. `cosineSimilarity handles zero-vector input by returning 0` (defensive — avoids NaN from divide-by-zero norm)
    5. `cosineSimilarity rejects mismatched-length vectors with a thrown Error` (assert via `assert.throws`)

    **Group B — normalization + cache key:**
    6. `normalizeForEmbedding('  [CRITICAL] SQL Injection ') === 'sql injection'` — strips leading `[SEVERITY]` markers, lowercases, collapses whitespace
    7. `normalizeForEmbedding('[high]  Missing  validation') === 'missing validation'` — also handles `[high]` (case-insensitive severity-marker pattern)
    8. `embeddingCacheKey('text-embedding-3-small', 'foo') !== embeddingCacheKey('text-embedding-ada-002', 'foo')` — model name participates
    9. `embeddingCacheKey('text-embedding-3-small', 'foo') !== embeddingCacheKey('text-embedding-3-small', 'bar')` — title participates
    10. `embeddingCacheKey('m', 't') === embeddingCacheKey('m', 't')` — deterministic; assert key is a 64-char lowercase hex string

    **Group C — constants + config:**
    11. `COSINE_TITLE_THRESHOLD === 0.80`
    12. `EMBEDDING_MODEL_DEFAULT === 'text-embedding-3-small'`
    13. `getEmbeddingModel({ workflow: { embedding_model: 'custom-model' } }) === 'custom-model'`
    14. `getEmbeddingModel({ workflow: {} }) === 'text-embedding-3-small'`
    15. `getEmbeddingModel(null) === 'text-embedding-3-small'` — defensive

    **Group D — comparator primary path (embedding match rescues low-Jaccard paraphrase):**
    16. The CRIT-10 root-cause case: baseline finding title "No input validation on req.body.email" + candidate "Missing input validation on email parameter" (Jaccard 0.333, below 0.7 fuzzy threshold). Pass pre-computed embeddings such that their cosine === 0.87 (synthesize two 1536-dim vectors with that exact dot product — see suggested construction below). Assert: `result.cosineMatchCount === 1`, `result.fuzzyMatchCount === 0`, `result.overlap === 1.0`, `result.pass === true`. **This is the load-bearing test** — it proves the new path closes the CRIT-10 gap.

    **Group E — fallback path:**
    17. Call `computeCriticFindingsDeltasEmbedding(schema, baseline, runs, { allowFallback: true })` with `embeddingsByTitle: undefined` AND `OPENAI_API_KEY` unset (use `before/after` hooks to delete + restore `process.env.OPENAI_API_KEY`). Assert: `result.usedFallback === true`, `result.fallbackReason === 'OPENAI_API_KEY unset'`, AND the comparator still returns a valid result shape (no thrown error). Capture `process.stderr.write` output via a one-shot monkey-patch and assert it includes `/WARN: embedding API unavailable/`.

    **Group F — diagnosis output:**
    18. Construct a 4-baseline scenario where 3 findings match (cosine ≥0.80) and 1 baseline finding has NO close candidate (best cosine 0.45). Assert: `result.perFindingDiagnosis.length === 4` (one entry per baseline), the entry for the unmatched finding has `bestCandidateId` set to whichever candidate scored highest and `cosine === 0.45`, the entries for matched findings have `cosine >= 0.80`.

    Suggested synthetic-vector construction for sub-test 16:
    ```javascript
    // Two unit vectors with cosine === 0.87:
    //   a = [1, 0, 0, ..., 0]  (1536 dims, all zeros except index 0)
    //   b = [0.87, sqrt(1 - 0.87^2), 0, ..., 0]
    function makeVecCosine(cosine, dim = 1536) {
      const v = new Array(dim).fill(0);
      v[0] = cosine;
      v[1] = Math.sqrt(1 - cosine * cosine);
      return v;
    }
    const vecA = (() => { const v = new Array(1536).fill(0); v[0] = 1; return v; })();
    const vecB = makeVecCosine(0.87);
    // cosineSimilarity(vecA, vecB) === 0.87 exactly (within floating-point epsilon).
    ```
  </behavior>
  <action>
    Write `tests/critic-comparator-embedding-shape.test.cjs` as a `node --test`-compatible CommonJS file. Pattern source: copy the imports + describe/test structure from `tests/critic-comparator-fix.test.cjs` (the 16-subtest sibling file from Phase 2 — same `node:test` + `node:assert/strict` style).

    Header docblock must explain (1) this is a RED test for Phase 2.1, (2) the GREEN target is Plan 02.1-02's `computeCriticFindingsDeltasEmbedding`, (3) the test never hits the live API (synthetic vectors only), (4) the fallback sub-test deliberately unsets `OPENAI_API_KEY` in scope.

    All assertions use `assert.strictEqual` / `assert.deepStrictEqual` / `assert.match` / `assert.throws` from `node:assert/strict` (matches the Phase 2 lock-test convention).

    Each sub-test must reference the `_internal` export shape exactly as documented in `<interfaces>` above. When a sub-test asserts a symbol exists, the failure message should read like `expected typeof _internal.cosineSimilarity === 'function', got 'undefined'` — explicit enough that the Plan 02.1-02 implementer can act on it without re-reading this plan.

    No live API calls. No network access. No filesystem writes (the test does NOT touch `integration/test-fixtures/baselines/embeddings/<critic>/...`; that's Plan 02.1-02's job).

    Concurrency safety (per D-CTX-15 / Phase 1 D-04): no `process.chdir`, no shared module-state mutation. The `process.env.OPENAI_API_KEY` mutation in Group E uses `before()`/`after()` hooks scoped to ONE `describe()` block AND restores the original value (or `delete`s it if it wasn't set originally) in `after()` — guaranteeing isolation from other tests.

    Length target: ~200-300 lines. The file is heavy on synthetic-vector construction helpers; that's expected.
  </action>
  <verify>
    <automated>node --test tests/critic-comparator-embedding-shape.test.cjs 2>&amp;1 | tee /tmp/red-output.txt; if grep -E "Cannot find module|SyntaxError" /tmp/red-output.txt &gt; /dev/null; then echo "FAIL: test errors are syntactic, not RED-by-contract"; exit 1; fi; if ! grep -E "fail [1-9]" /tmp/red-output.txt &gt; /dev/null; then echo "FAIL: expected test failures (this is a RED test) but none were reported"; exit 1; fi; echo "PASS: RED tests report contract-shaped failures only"</automated>
  </verify>
  <done>
    `node --test tests/critic-comparator-embedding-shape.test.cjs` reports failed tests (this is intentional — it's RED). The failure messages name missing `_internal` symbols (e.g., `cosineSimilarity`, `computeCriticFindingsDeltasEmbedding`, `COSINE_TITLE_THRESHOLD`). NO failures are `Cannot find module` or `SyntaxError`. The file imports `require('../integration/helpers/agent-parity.cjs')` cleanly. Sub-test count is ≥ 10. The fallback sub-test (Group E) restores `process.env.OPENAI_API_KEY` correctly (assert via a `node -e` check after the test run that the env var state is unchanged from before).
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify Phase 2 lock tests still pass (regression gate)</name>
  <files>(no files modified — verification only)</files>
  <action>
    Run all three Phase 2 lock-test files and confirm they remain GREEN. Per D-CTX-07, these tests are the contract surface that this plan must NOT regress. Since this plan only adds files (config-schema entry, docs entry, new test file), they SHOULD pass — this task makes that explicit and machine-checkable.

    Per the gap-closure mode contract: if any of these tests fail, the plan is broken and Plan 02.1-02 cannot proceed.
  </action>
  <verify>
    <automated>node --test tests/critic-comparator-fix.test.cjs tests/critic-findings-delta-shape.test.cjs tests/agent-parity-helper-shape.test.cjs 2>&amp;1 | tee /tmp/phase2-locks.txt; if grep -E "^# fail [1-9]" /tmp/phase2-locks.txt &gt; /dev/null; then echo "FAIL: Phase 2 lock test regression detected"; exit 1; fi; echo "PASS: all 3 Phase 2 lock-test files remain GREEN"</automated>
  </verify>
  <done>
    All three Phase 2 lock-test files pass (combined sub-test count ≥ 30). No regression — the only files changed by this plan are `tests/critic-comparator-embedding-shape.test.cjs` (NEW), `get-shit-done/bin/lib/config-schema.cjs` (one-line addition), and `docs/CONFIGURATION.md` (one entry addition).
  </done>
</task>

</tasks>

<verification>
1. `node --test tests/critic-comparator-embedding-shape.test.cjs` reports test failures naming missing `_internal` symbols (intentional RED state).
2. `node --test tests/critic-comparator-fix.test.cjs tests/critic-findings-delta-shape.test.cjs tests/agent-parity-helper-shape.test.cjs tests/config-schema-docs-parity.test.cjs` all PASS.
3. `isValidConfigKey('workflow.embedding_model')` returns `true`.
4. `git diff --stat` shows exactly 3 files changed: `tests/critic-comparator-embedding-shape.test.cjs` (NEW), `get-shit-done/bin/lib/config-schema.cjs` (+1 line), `docs/CONFIGURATION.md` (+entry).
5. `integration/helpers/agent-parity.cjs` is UNCHANGED in this plan (Plan 02.1-02 modifies it).
</verification>

<success_criteria>
- [ ] `tests/critic-comparator-embedding-shape.test.cjs` exists with ≥10 sub-tests, all RED-by-contract (no syntactic errors).
- [ ] `workflow.embedding_model` is in `VALID_CONFIG_KEYS` and documented in `docs/CONFIGURATION.md`.
- [ ] `tests/config-schema-docs-parity.test.cjs` passes (CI drift guard satisfied).
- [ ] All 3 Phase 2 lock-test files (critic-comparator-fix, critic-findings-delta-shape, agent-parity-helper-shape) remain GREEN.
- [ ] No modifications to `integration/helpers/agent-parity.cjs` — that file is Plan 02.1-02's GREEN target.
- [ ] The test file's fallback sub-test correctly restores `process.env.OPENAI_API_KEY` in its `after()` hook (no leakage).
</success_criteria>

<output>
After completion, create `.planning/users/dan-halem/gsd-slim-and-integrate/phases/02.1-crit10-comparator-rebuild/02.1-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Summary must explicitly call out:
- Sub-test count in the new RED test file.
- Failure message shape (proves RED-by-contract, not by missing-module).
- Confirmation that Phase 2 lock tests stay GREEN.
- Confirmation that `workflow.embedding_model` is now an accepted config key.
- Hand-off note: Plan 02.1-02 will turn these RED tests GREEN by implementing the documented `_internal` symbols on `integration/helpers/agent-parity.cjs`.
</output>
