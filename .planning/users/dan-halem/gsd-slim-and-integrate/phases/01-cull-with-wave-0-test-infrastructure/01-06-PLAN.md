---
phase: 01-cull-with-wave-0-test-infrastructure
plan: 06
type: execute
wave: 1
depends_on: [01, 04]
files_modified:
  - bin/install.js
  - get-shit-done/bin/lib/model-profiles.cjs
  - get-shit-done/bin/lib/intel.cjs
  - get-shit-done/bin/lib/docs.cjs
  - get-shit-done/bin/lib/init.cjs
  - agents/gsd-verifier.md
  - agents/gsd-pattern-mapper.md
  - agents/gsd-roadmapper.md
  - agents/gsd-planner.md
  - get-shit-done/workflows/diagnose-issues.md
  - get-shit-done/workflows/plan-phase.md
  - get-shit-done/workflows/execute-plan.md
  - get-shit-done/workflows/verify-work.md
  - docs/AGENTS.md
  - docs/COMMANDS.md
autonomous: true
requirements_addressed: [TEST-01, CULL-06]
must_haves:
  truths:
    - "User can grep all surviving files (agents, commands, workflows, templates, tests, integration, docs, bin/install.js, get-shit-done/bin/lib/*.cjs) and find ZERO references to any deleted command or agent name."
    - "After this plan, tests/cull-no-orphan-references.test.cjs (Plan 01) is GREEN."
    - "CLI modules (bin/install.js, get-shit-done/bin/lib/{model-profiles,intel,docs,init}.cjs) no longer hardcode deleted agent names — runtime errors prevented."
    - "Surviving agent prompts and workflows no longer suggest deleted commands or agents to users."
  artifacts:
    - path: "bin/install.js"
      provides: "CODEX_AGENT_SANDBOX map cleaned of 3 deleted agent entries; any other deleted-name string references removed"
    - path: "get-shit-done/bin/lib/model-profiles.cjs"
      provides: "MODEL_PROFILES map with 8 deleted-agent rows removed (gsd-debugger, gsd-codebase-mapper, gsd-nyquist-auditor, gsd-ui-researcher, gsd-ui-checker, gsd-ui-auditor, gsd-doc-writer, gsd-doc-verifier)"
    - path: "Multiple agent prompts + workflow markdown + docs"
      provides: "Reference-rot scrubbed of all deleted command/agent slash-mentions and @-references"
  key_links:
    - from: "Plan 01 orphan-reference test"
      to: "all surviving files"
      via: "After Plan 06 commits, npm test → tests/cull-no-orphan-references.test.cjs goes GREEN (modulo deleted-file paths still on disk; Plan 08 finishes the cull)"
      pattern: "no orphan references"
    - from: "Plan 04 baselines"
      to: "this plan"
      via: "depends_on: [04] — baseline capture MUST land before any agent file is touched"
      pattern: "depends_on contains 04"
---

<objective>
Scrub all surviving prompts, workflows, CLI modules, and docs of references to deleted command/agent names. This is the **single largest reference-rot fix commit** in Phase 1 — it must land BEFORE any deletion (Plan 08) AND AFTER baseline capture (Plan 04). Per RESEARCH.md §3 and the recommendation in §3.2 ("single 'reference-rot fix' commit BEFORE any deletion commit"), this consolidates ALL non-self-references to deleted names in one place.

CRITICAL ordering invariants (from RESEARCH.md and CONTEXT.md `<decisions>` step 7):
1. Plan 04 (baseline capture) MUST be committed first — `depends_on: [04]` is HARD.
2. After this plan commits, the orphan-reference test (Plan 01) is "structurally GREEN" — it would pass IF the deleted command/agent FILES were also removed. They are not yet (Plan 08); the orphan test will be RED on the file existence but GREEN on the prose-references-to-them. This is a transient state.
3. Plan 07 (consolidated commands + stubs) must also land before Plan 08 (deletions) because the deprecation stubs `@`-include `/gsd-review` workflows.
4. CRITICAL: this plan does NOT delete any files. It only EDITS surviving files. The 49 commands + 17 agents are still on disk; their deletion is Plan 08.

Purpose: TEST-01 (orphan-reference scan goes GREEN modulo file existence) + CULL-06 (full GSD spine completes without referencing deleted names) + RESEARCH.md §3.1 (CRITICAL — CLI module reference-rot causes runtime errors, not just stale prose).

Output: ~10-15 files modified across 4 categories: CLI modules (5), surviving agents (3-4), workflows (3-4), docs (2-3). Single commit titled per task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/users/dan-halem/gsd-slim-and-integrate/phases/01-cull-with-wave-0-test-infrastructure/01-CONTEXT.md
@.planning/users/dan-halem/gsd-slim-and-integrate/phases/01-cull-with-wave-0-test-infrastructure/01-RESEARCH.md
@.planning/users/dan-halem/gsd-slim-and-integrate/phases/01-cull-with-wave-0-test-infrastructure/01-PATTERNS.md
@tests/fixtures/cull-deletion-list.cjs
@tests/cull-no-orphan-references.test.cjs

<interfaces>
Deletion list (from tests/fixtures/cull-deletion-list.cjs created in Plan 01):
- 49 deleted commands
- 17 deleted agents
- 9 consolidated commands (still mentioned in deprecation stubs + migration tables, NOT scrubbed from those allow-listed files)

Known reference-rot sites per RESEARCH.md §3 (each cited with line numbers from VERIFIED inspection):

CRITICAL (CLI runtime — RESEARCH.md §3.1):
- bin/install.js lines 30-42: CODEX_AGENT_SANDBOX references gsd-debugger, gsd-codebase-mapper, gsd-nyquist-auditor (3 entries to delete)
- bin/install.js further down (claudeToCopilotTools etc.): grep for 'gsd-(debugger|codebase|nyquist|doc-|ui-|domain-|eval-|framework|ai-research|intel-update)' to find all sites
- get-shit-done/bin/lib/model-profiles.cjs lines 16-27: 8 deleted-agent rows (gsd-debugger, gsd-codebase-mapper, gsd-nyquist-auditor, gsd-ui-researcher, gsd-ui-checker, gsd-ui-auditor, gsd-doc-writer, gsd-doc-verifier)
- get-shit-done/bin/lib/intel.cjs line 319: 'spawn gsd-intel-updater agent' (delete the agent reference)
- get-shit-done/bin/lib/docs.cjs lines 15, 251: GSD_MARKER + doc_writer_model field (gsd-doc-writer references)
- get-shit-done/bin/lib/init.cjs line 922: mapper_model: resolveModelInternal(cwd, 'gsd-codebase-mapper')

HIGH (surviving agent prompts — RESEARCH.md §3.2):
- agents/gsd-verifier.md: mentions /gsd-debug for fault investigation
- agents/gsd-pattern-mapper.md: may reference gsd-codebase-mapper
- agents/gsd-roadmapper.md: may reference deleted commands

MEDIUM (workflow files — RESEARCH.md §3.3):
- get-shit-done/workflows/diagnose-issues.md, plan-phase.md, execute-plan.md, verify-work.md — grep for /gsd-debug, /gsd-graphify, /gsd-stats, /gsd-add-tests, gsd-debugger

LOW (docs — RESEARCH.md §3.4):
- docs/AGENTS.md, docs/COMMANDS.md, docs/ARCHITECTURE.md — surface deleted names
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scrub CLI modules of deleted-agent name references (CRITICAL — runtime risk)</name>
  <files>bin/install.js, get-shit-done/bin/lib/model-profiles.cjs, get-shit-done/bin/lib/intel.cjs, get-shit-done/bin/lib/docs.cjs, get-shit-done/bin/lib/init.cjs</files>
  <read_first>
    - bin/install.js (lines 26-46 — CODEX_AGENT_SANDBOX map literal)
    - get-shit-done/bin/lib/model-profiles.cjs (lines 9-35 — full MODEL_PROFILES map)
    - get-shit-done/bin/lib/intel.cjs (lines 314-321 — intelUpdate function)
    - get-shit-done/bin/lib/docs.cjs (lines 1-50, 240-260 — find both reference sites + understand if module is dead code)
    - get-shit-done/bin/lib/init.cjs (lines 900-950 — context around line 922 mapper_model field)
    - .planning/users/dan-halem/gsd-slim-and-integrate/phases/01-cull-with-wave-0-test-infrastructure/01-RESEARCH.md (lines 988-1003 — §3.1 CLI reference-rot table with exact line numbers)
    - .planning/users/dan-halem/gsd-slim-and-integrate/phases/01-cull-with-wave-0-test-infrastructure/01-PATTERNS.md (lines 832-865 — modified-file pattern notes)
    - tests/fixtures/cull-deletion-list.cjs (the source-of-truth deletion list)
  </read_first>
  <action>

**Sub-task 1a — Scrub bin/install.js.**

In `bin/install.js`, find the `CODEX_AGENT_SANDBOX` map literal (lines 26-46 per RESEARCH.md §3.1). Remove the 3 entries for deleted agents:
- `gsd-debugger`
- `gsd-codebase-mapper`
- `gsd-nyquist-auditor`

Then run a comprehensive scrub for ALL deleted agents in the rest of the file. Use this exact discovery command and remove every match:

```bash
grep -nE 'gsd-(debugger|debug-session-manager|doc-(writer|classifier|synthesizer|verifier)|domain-researcher|eval-auditor|eval-planner|framework-selector|ai-researcher|ui-(auditor|checker|researcher)|codebase-mapper|intel-updater|nyquist-auditor)' bin/install.js
```

For each match: if the line is a map entry, delete the line (preserving trailing-comma syntax for the surrounding map); if the line is in a comment that lists removed/deleted agents, leave it (those comments document history).

**Sub-task 1b — Scrub get-shit-done/bin/lib/model-profiles.cjs.**

In `get-shit-done/bin/lib/model-profiles.cjs`, find the `MODEL_PROFILES` map literal (lines 9-34 per RESEARCH.md §3.1) and DELETE rows for these 8 deleted agents:
- gsd-debugger (line 16)
- gsd-codebase-mapper (line 17)
- gsd-nyquist-auditor (line 21)
- gsd-ui-researcher (line 23)
- gsd-ui-checker (line 24)
- gsd-ui-auditor (line 25)
- gsd-doc-writer (line 26)
- gsd-doc-verifier (line 27)

Verify `VALID_PROFILES` derivation at line 35 still resolves (it derives from `MODEL_PROFILES['gsd-planner']` per PATTERNS.md line 63 — `gsd-planner` survives, so safe). The companion docstring at lines 1-8 referencing `references/model-profiles.md` may need updating if the doc lists removed agents — check the referenced file and align.

**Sub-task 1c — Scrub get-shit-done/bin/lib/intel.cjs.**

In `get-shit-done/bin/lib/intel.cjs` line 319 (per RESEARCH.md §3.1), the message string is:

```
'Run gsd-tools intel update or spawn gsd-intel-updater agent for full refresh'
```

Replace with:

```
'Run gsd-tools intel update for full refresh'
```

Inspect the function (`intelUpdate()`, lines 314-321) — it returns `{ action, message }`. If callers branch on `action === 'spawn_agent'`, simplify the function to always return `{ action: 'manual', message: '...' }` and grep for any caller using the old `'spawn_agent'` action — fix those too.

**Sub-task 1d — Scrub get-shit-done/bin/lib/docs.cjs (TRIAGE first).**

In `get-shit-done/bin/lib/docs.cjs` (per RESEARCH.md §3.1 — VERIFY DEAD-CODE STATUS):

First, run a discovery grep:
```bash
grep -rn "require.*['\"]\.\./lib/docs['\"]\\|require.*['\"]./docs['\"]" get-shit-done/bin/ bin/ 2>/dev/null
```

If NO callers exist (the entire docs.cjs codepath is dead — likely, since gsd-doc-* agents are all deleted), then:
1. Delete `get-shit-done/bin/lib/docs.cjs` entirely.
2. Remove the corresponding row from `docs/INVENTORY.md` if listed.

If callers DO exist:
1. Replace `GSD_MARKER` at line 15 (currently `'<!-- generated-by: gsd-doc-writer -->'`) with `'<!-- generated-by: gsd-tools -->'` (drop the deleted-agent reference).
2. Remove the `doc_writer_model:` field at line 251 from the returned config object.
3. Verify no caller reads `doc_writer_model`; if any caller does, remove that consumer too.

**Sub-task 1e — Scrub get-shit-done/bin/lib/init.cjs.**

In `get-shit-done/bin/lib/init.cjs` line 922 (per RESEARCH.md §3.1):

Remove the line:
```javascript
mapper_model: resolveModelInternal(cwd, 'gsd-codebase-mapper'),
```

Then `grep -rn 'mapper_model' get-shit-done/ bin/ 2>/dev/null` to verify no callers reference the removed field. If callers exist, remove or rewire them.

**Implementation rules across all 5 sub-tasks:**
- All edits are MECHANICAL string-replacement / line-deletion. NO surrounding logic should be modified.
- After all edits, run `node -c <file>` on each modified `.js` / `.cjs` file to verify syntactic validity.
- After all edits, run `node bin/install.js --dry-run` if the installer supports dry-run (check). If it doesn't, do at minimum `node -e "require('./bin/install.js')"` to verify the file loads without immediate errors.
- DO NOT add new files. DO NOT change function signatures except in 1c (intelUpdate simplification, which is allowed because the deleted-agent branch is being removed).
- Do NOT touch any deleted-COMMAND name references in CLI modules (CLI modules don't reference command files; only agent files). The orphan test will catch any miss.
  </action>
  <verify>
    <automated>set -e; for f in bin/install.js get-shit-done/bin/lib/model-profiles.cjs get-shit-done/bin/lib/intel.cjs get-shit-done/bin/lib/init.cjs; do node -c "$f" || { echo "FAIL parse: $f"; exit 1; }; done; if [ -f get-shit-done/bin/lib/docs.cjs ]; then node -c get-shit-done/bin/lib/docs.cjs || { echo "FAIL parse docs.cjs"; exit 1; }; fi; echo "--- CLI grep for deleted agents ---"; matches=$(grep -nE 'gsd-(debugger|debug-session-manager|doc-(writer|classifier|synthesizer|verifier)|domain-researcher|eval-auditor|eval-planner|framework-selector|ai-researcher|ui-(auditor|checker|researcher)|codebase-mapper|intel-updater|nyquist-auditor)' bin/install.js get-shit-done/bin/lib/*.cjs 2>/dev/null | grep -v '^#\|//.*removed\|//.*deleted' || true); if [ -n "$matches" ]; then echo "FAIL: deleted-agent references remain in CLI:"; echo "$matches"; exit 1; fi; echo "OK: CLI modules scrubbed"</automated>
  </verify>
  <acceptance_criteria>
    - `bin/install.js`, `get-shit-done/bin/lib/model-profiles.cjs`, `get-shit-done/bin/lib/intel.cjs`, `get-shit-done/bin/lib/init.cjs` all parse (`node -c`).
    - `get-shit-done/bin/lib/docs.cjs` is either DELETED or has both reference-sites scrubbed (line 15 marker + line 251 field).
    - `grep -E 'gsd-(debugger|debug-session-manager|doc-(writer|classifier|synthesizer|verifier)|domain-researcher|eval-auditor|eval-planner|framework-selector|ai-researcher|ui-(auditor|checker|researcher)|codebase-mapper|intel-updater|nyquist-auditor)' bin/install.js get-shit-done/bin/lib/*.cjs` (excluding comment lines documenting removals) returns ZERO matches.
    - `node -e "require('./bin/install.js')"` does not throw (loads without error).
    - `MODEL_PROFILES` in model-profiles.cjs no longer contains any of the 8 deleted-agent keys.
    - Caller-side references to `mapper_model` (init.cjs) and `'spawn_agent'` action (intel.cjs) are also cleaned (no stale callers).
  </acceptance_criteria>
  <done>All 5 CLI modules are scrubbed of deleted-agent name references; CLI loads without runtime errors.</done>
</task>

<task type="auto">
  <name>Task 2: Scrub surviving agents, workflows, and docs of deleted command/agent slash-mentions</name>
  <files>agents/gsd-verifier.md, agents/gsd-pattern-mapper.md, agents/gsd-roadmapper.md, agents/gsd-planner.md, get-shit-done/workflows/diagnose-issues.md, get-shit-done/workflows/plan-phase.md, get-shit-done/workflows/execute-plan.md, get-shit-done/workflows/verify-work.md, docs/AGENTS.md, docs/COMMANDS.md</files>
  <read_first>
    - tests/fixtures/cull-deletion-list.cjs
    - .planning/users/dan-halem/gsd-slim-and-integrate/phases/01-cull-with-wave-0-test-infrastructure/01-RESEARCH.md (lines 1005-1056 — §3.2/§3.3/§3.4 known reference-rot sites + discovery scan command)
    - All files listed in `<files>` (read each before editing)
  </read_first>
  <action>

**Sub-task 2a — Run the comprehensive discovery scan to find ALL surviving-file references to deleted names.**

Run this exact command (from RESEARCH.md §3.2 lines 1015-1022, expanded):

```bash
# Deleted commands — slash-mentions
for name in audit-fix audit-uat forensics health stats scan intel map-codebase graphify ai-integration-phase ui-phase ui-review eval-review spike sketch spike-wrap-up sketch-wrap-up debug explore note plant-seed add-backlog thread review-backlog audit-milestone plan-milestone-gaps milestone-summary archive-project restore-project ship undo inbox review manager autonomous fast 'do' next session-report spec-phase import ultraplan-phase list-phase-assumptions docs-update ingest-docs from-gsd2 add-tests analyze-dependencies cleanup; do
  grep -lE "/gsd-${name}\\b" agents/*.md commands/gsd/*.md get-shit-done/workflows/*.md get-shit-done/templates/*.md docs/*.md 2>/dev/null | grep -vE "/gsd-${name}\\.md$" | grep -vE "(secure-phase|validate-phase|code-review|code-review-fix|critique|plan-review-convergence|help)\\.md"
done

# Deleted agents — name mentions
for agent in gsd-debugger gsd-debug-session-manager gsd-doc-writer gsd-doc-classifier gsd-doc-synthesizer gsd-doc-verifier gsd-domain-researcher gsd-eval-auditor gsd-eval-planner gsd-framework-selector gsd-ai-researcher gsd-ui-auditor gsd-ui-checker gsd-ui-researcher gsd-codebase-mapper gsd-intel-updater gsd-nyquist-auditor; do
  grep -l "${agent}" agents/*.md commands/gsd/*.md get-shit-done/workflows/*.md get-shit-done/templates/*.md docs/*.md 2>/dev/null | grep -vE "${agent}\\.md$"
done
```

Capture the output. The unique set of files is the work list for this task. Add any files surfaced by the scan to `<files>` (the listed files above are the EXPECTED set per RESEARCH.md §3.2/3.3 — actual scan may find more).

**Sub-task 2b — For each found file, scrub references with appropriate replacements.**

Apply these replacement rules:

| Old reference | Replacement |
|---------------|-------------|
| `/gsd-debug` (slash-mention) | "investigate the issue manually (debugger agent retired)" |
| `/gsd-graphify` | (delete the suggestion entirely; no replacement) |
| `/gsd-stats` | "git log" |
| `/gsd-map-codebase` | "use Grep + Read directly" |
| `/gsd-add-tests` | "write tests as part of the implementation task (TDD layer arrives in Phase 4)" |
| `/gsd-explore` | "read STATE.md or run /gsd-progress" |
| `/gsd-audit-fix`, `/gsd-audit-uat`, `/gsd-forensics`, `/gsd-health`, `/gsd-scan`, `/gsd-intel` | (delete the suggestion entirely) |
| `gsd-debugger` (agent ref) | (delete the suggestion; replace with main-thread reasoning if context demands a substitute) |
| `gsd-codebase-mapper` (agent ref) | "gsd-pattern-mapper" if the surrounding context is about code-pattern analysis; otherwise delete |
| `gsd-nyquist-auditor` (agent ref) | (delete; the test-coverage gap detection lands in Phase 4 TDD layer) |
| `gsd-doc-*` agents | (delete the suggestion entirely; doc subsystem retired) |
| `gsd-ui-*` agents | (delete the suggestion entirely; UI subsystem retired) |
| `gsd-intel-updater` | (delete the suggestion entirely) |

**Sub-task 2c — Verify orphan-reference test goes structurally GREEN on prose (the deleted FILES are still on disk, but no surviving file should mention them).**

Run `npm test` and capture the orphan-reference test output. The test will still report the deleted-FILE existence (because Plan 08 hasn't deleted them yet), but it should report ZERO findings of "slash-mention of deleted command" or "@-ref to deleted agent" in surviving files (excluding the allow-listed files).

Implementation rules:
- This is a SINGLE commit. Do NOT split per file. The commit title must be: `chore(cull): scrub reference-rot from surviving prompts, workflows, and CLI modules`.
- DO NOT touch any deletion-stub file (`commands/gsd/secure-phase.md`, etc. — these are in the orphan test ALLOW_LIST and will be re-authored in Plan 07).
- DO NOT touch `commands/gsd/help.md` or `CHANGELOG.md` (allow-listed; Plan 09 populates them).
- DO NOT touch `tests/fixtures/cull-deletion-list.cjs` (it's the source of truth).
- DO NOT delete any command or agent files (that's Plan 08).
- After this plan commits, the orphan-reference test SHOULD report findings ONLY for: (a) `agents/gsd-{deleted-name}.md` files that still exist on disk (caught by no scan context — those are the FILES, not REFERENCES), and (b) any surviving file that references a deleted name we missed. (b) MUST be zero; if any (b) findings appear, fix them in the same commit.
- It is acceptable for the orphan-reference test to still be RED at the end of this plan IF the only findings are file-existence on the deleted command/agent files themselves. (The orphan test scans content, not file existence — so once the prose is clean, even if the deleted files remain, the test goes GREEN. Verify this empirically.)
  </action>
  <verify>
    <automated>set -e; cmd_pat='(audit-fix|audit-uat|forensics|health|stats|scan|intel|map-codebase|graphify|ai-integration-phase|ui-phase|ui-review|eval-review|spike|sketch|spike-wrap-up|sketch-wrap-up|debug|explore|note|plant-seed|add-backlog|thread|review-backlog|audit-milestone|plan-milestone-gaps|milestone-summary|archive-project|restore-project|ship|undo|inbox|review|manager|autonomous|fast|do|next|session-report|spec-phase|import|ultraplan-phase|list-phase-assumptions|docs-update|ingest-docs|from-gsd2|add-tests|analyze-dependencies|cleanup)'; agent_pat='(gsd-debugger|gsd-debug-session-manager|gsd-doc-writer|gsd-doc-classifier|gsd-doc-synthesizer|gsd-doc-verifier|gsd-domain-researcher|gsd-eval-auditor|gsd-eval-planner|gsd-framework-selector|gsd-ai-researcher|gsd-ui-auditor|gsd-ui-checker|gsd-ui-researcher|gsd-codebase-mapper|gsd-intel-updater|gsd-nyquist-auditor)'; cmd_findings=$(grep -rEn "/gsd-${cmd_pat}\\b" agents/ commands/gsd/ get-shit-done/workflows/ get-shit-done/templates/ docs/ 2>/dev/null | grep -vE "(secure-phase|validate-phase|code-review|code-review-fix|critique|plan-review-convergence|help)\.md|cull-deletion-list|CHANGELOG|cull-no-orphan-references" || true); agent_findings=$(grep -rEn "${agent_pat}" agents/ commands/gsd/ get-shit-done/workflows/ get-shit-done/templates/ docs/ 2>/dev/null | grep -vE "(secure-phase|validate-phase|code-review|code-review-fix|critique|plan-review-convergence|help)\.md|cull-deletion-list|CHANGELOG|cull-no-orphan-references" || true); if [ -n "$cmd_findings" ]; then echo "FAIL: deleted-command refs remain (excluding allow-listed):"; echo "$cmd_findings" | head -30; exit 1; fi; if [ -n "$agent_findings" ]; then echo "FAIL: deleted-agent refs remain (excluding allow-listed) — note: agents/gsd-X.md FILES still exist; this scan looks for cross-references INTO surviving files only"; echo "$agent_findings" | head -30; exit 1; fi; echo "OK: surviving prose scrubbed"; node --test tests/cull-no-orphan-references.test.cjs 2>&1 | grep -E "fail|pass" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The discovery scan finds ZERO slash-mentions of deleted commands in surviving files (excluding allow-listed: cull-deletion-list, deprecation stubs, help.md, CHANGELOG.md, the orphan-test itself).
    - The discovery scan finds ZERO agent-name mentions of deleted agents in surviving files (excluding allow-listed AND excluding the deleted agents' own files which still exist on disk).
    - Surviving files modified in this plan still parse / load (markdown is well-formed; agents files have valid frontmatter).
    - All known reference-rot sites from RESEARCH.md §3.1/3.2/3.3 are addressed.
    - Single git commit with title: `chore(cull): scrub reference-rot from surviving prompts, workflows, and CLI modules`.
    - No deleted command or agent FILES are removed in this plan (Plan 08's job).
  </acceptance_criteria>
  <done>All surviving prose, workflows, and CLI modules are clean of references to deleted names; the orphan-reference test (Plan 01) reports findings ONLY against files that will be deleted in Plan 08.</done>
</task>

</tasks>

<test_inventory>
This plan does NOT introduce new test files. It clears the way for tests/cull-no-orphan-references.test.cjs (Plan 01) to go GREEN once Plan 08 deletes the residual command/agent files.

| Test consumed | REQ-IDs verified |
|---|---|
| `tests/cull-no-orphan-references.test.cjs` | TEST-01 (this plan removes the scan findings against surviving files) |
| Manual: `node bin/install.js` smoke load | CULL-06 (CLI modules don't error at load) |
</test_inventory>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edits → CLI modules | Mechanical string deletions; risk is breaking surrounding logic |
| Edits → agent prompts | Wording changes; risk is altering agent behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | Tampering (silent runtime breakage) | CLI module edits leave dangling refs | mitigate | Per-file `node -c` parse check + `node -e "require(...)"` smoke load + comprehensive discovery grep verify; test/cull-no-orphan-references.test.cjs as the rolling gate. |
| T-01-06-02 | Repudiation | A reference is missed and Plan 08 silently deletes a file users still link to | mitigate | The orphan-reference test (Plan 01) IS the gate: Plan 08 cannot land if it leaves orphan refs. This plan ensures all non-self-references are clean BEFORE Plan 08 runs. |
| T-01-06-03 | Denial of service | An agent prompt edit changes agent behavior unexpectedly | mitigate | The pre-scrub baselines (Plan 04) lock the contract. Phase 6 will run parity against Plan 04 baselines; if this plan's scrubs altered agent semantics, Phase 6 catches it then. (Phase 1 itself does not run parity — capture-only.) Acceptable lag because the changes are mechanical reference removals, not semantic edits. |
| T-01-06-04 | Information disclosure | Deleted-agent name remains in some niche file (e.g., a test fixture) | mitigate | Discovery scan covers `agents/`, `commands/gsd/`, `get-shit-done/workflows/`, `get-shit-done/templates/`, `docs/`, `bin/install.js`, `get-shit-done/bin/lib/`, plus test/cull-no-orphan-references.test.cjs scans `tests/`, `integration/`, and `CHANGELOG.md`. Combined coverage = 10 scan roots; a missed site would have to be outside all 10. |
</threat_model>

<verification>
- All 5 CLI modules parse (`node -c`).
- `node -e "require('./bin/install.js')"` does not throw.
- Discovery grep for deleted commands/agents in surviving prose returns ZERO matches (excluding allow-listed files).
- `npm test` runs; `tests/cull-no-orphan-references.test.cjs` reports findings ONLY against deleted-FILE existence (not prose mentions in surviving files).
- Single commit with locked title.
</verification>

<success_criteria>
- [ ] CLI modules: `bin/install.js` + `get-shit-done/bin/lib/{model-profiles,intel,init}.cjs` are scrubbed; `docs.cjs` is either deleted or scrubbed.
- [ ] Surviving agent prompts: gsd-verifier, gsd-pattern-mapper, gsd-roadmapper, gsd-planner (and any others surfaced by discovery scan) no longer mention deleted commands or agents.
- [ ] Workflow files: diagnose-issues.md, plan-phase.md, execute-plan.md, verify-work.md (and any others surfaced) cleaned.
- [ ] docs/AGENTS.md, docs/COMMANDS.md cleaned.
- [ ] Single commit titled `chore(cull): scrub reference-rot from surviving prompts, workflows, and CLI modules`.
- [ ] CLI loads without error.
- [ ] Orphan-reference test reports findings ONLY against deleted-file existence (which Plan 08 will fix).
</success_criteria>

<output>
Create `.planning/users/dan-halem/gsd-slim-and-integrate/phases/01-cull-with-wave-0-test-infrastructure/01-06-SUMMARY.md`:
- Total files modified (count)
- CLI modules cleaned: 4 or 5 (depending on docs.cjs disposition)
- Agent prompts cleaned: count + names
- Workflow files cleaned: count + names
- Docs cleaned: count + names
- Orphan-reference test status: prose-clean (FILE-RED until Plan 08)
- Risk-pinning: any reference deemed too tightly coupled to its surrounding logic to scrub safely → flag for Plan 08 review.
</output>
