---
phase: 01-cull-with-wave-0-test-infrastructure
plan: 10
type: execute
wave: 2
gap_closure: true
depends_on: [09]
files_modified:
  - get-shit-done/workflows/help.md
  - tests/cull-no-orphan-references.test.cjs
autonomous: true
requirements_addressed: [TEST-01, CULL-06]
gaps_addressed: [CR-01]
must_haves:
  truths:
    - "CR-01 closed: get-shit-done/workflows/help.md no longer contains live-command Usage documentation for /gsd-map-codebase, /gsd-list-phase-assumptions, /gsd-do — only the migration table at the bottom mentions these names."
    - "User can run `node --test tests/cull-no-orphan-references.test.cjs` and the test passes (existing GREEN behavior preserved)."
    - "User can run a new sub-test that scans every file in ALLOW_LIST for `Usage: /gsd-<deleted-command>` patterns and the sub-test fails if any allow-listed file contains a Usage line for a deleted command (other than inside a tracked migration-table fenced region)."
    - "User can run `grep -E '^\\*\\*`\\/gsd-map-codebase`\\*\\*$|^\\*\\*`\\/gsd-list-phase-assumptions <number>`\\*\\*$|^\\*\\*`\\/gsd-do <description>`\\*\\*$' get-shit-done/workflows/help.md` and the grep returns zero matches (live-command headers gone from body)."
  artifacts:
    - path: "get-shit-done/workflows/help.md"
      provides: "Help workflow body with three live-command sections (lines 51-59 /gsd-map-codebase, 85-92 /gsd-list-phase-assumptions, 123-133 /gsd-do) stripped; migration table at bottom unchanged"
      contains: "Migration table rows for /gsd-map-codebase, /gsd-list-phase-assumptions, /gsd-do remain (per CULL-07); body sections gone"
    - path: "tests/cull-no-orphan-references.test.cjs"
      provides: "Tightened orphan scan that flags `Usage: /gsd-<deleted-command>` patterns even inside ALLOW_LIST files (separate sub-test, does not weaken existing scan)"
  key_links:
    - from: "tests/cull-no-orphan-references.test.cjs"
      to: "tests/fixtures/cull-deletion-list.cjs"
      via: "consumes deletedCommands minus slashMentionExcludes for the new Usage-pattern check inside ALLOW_LIST files"
      pattern: "deletedCommands.*ALLOW_LIST"
    - from: "get-shit-done/workflows/help.md"
      to: "Migration table rows (lines ~683, 712, 718)"
      via: "still present; body sections removed only"
      pattern: "Removed.*gsd-map-codebase|Removed.*gsd-list-phase-assumptions|Removed.*gsd-do"
---

<objective>
Close BLOCKER CR-01 from `01-VERIFICATION.md` and `01-REVIEW.md`: `get-shit-done/workflows/help.md` body advertises three deleted commands as if they are live (full Usage sections at lines 51-59, 85-92, 123-133), contradicting the migration table at the bottom of the same file. The orphan-reference test ALLOW_LISTs the entire file (line 65 of the test), so this drift ships silently. Strip the three body sections AND tighten the orphan test to flag `Usage: /gsd-<deleted-command>` patterns even inside allow-listed files.

Purpose: Close the CR-01 gap that the verifier confirmed (`01-VERIFICATION.md` Truth 1 PARTIAL, "Anti-Patterns Found" lines 173-175). Without this fix, users running `/gsd-help` post-cull see live documentation pointing to commands that no longer resolve — the exact user-visible regression Phase 1 was supposed to prevent.

Output: Modified `get-shit-done/workflows/help.md` with three body sections removed; modified `tests/cull-no-orphan-references.test.cjs` with a new sub-test that catches the same class of bug if it recurs in any other ALLOW_LIST file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/users/dan-halem/gsd-slim-and-integrate/PROJECT.md
@.planning/users/dan-halem/gsd-slim-and-integrate/ROADMAP.md
@.planning/users/dan-halem/gsd-slim-and-integrate/STATE.md
@.planning/users/dan-halem/gsd-slim-and-integrate/phases/01-cull-with-wave-0-test-infrastructure/01-CONTEXT.md
@.planning/users/dan-halem/gsd-slim-and-integrate/phases/01-cull-with-wave-0-test-infrastructure/01-VERIFICATION.md
@.planning/users/dan-halem/gsd-slim-and-integrate/phases/01-cull-with-wave-0-test-infrastructure/01-REVIEW.md
@tests/fixtures/cull-deletion-list.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten orphan-reference test to catch Usage: /gsd-&lt;deleted&gt; inside ALLOW_LIST files</name>
  <files>tests/cull-no-orphan-references.test.cjs</files>
  <read_first>
    - tests/cull-no-orphan-references.test.cjs (the file being modified — read in full to understand existing test structure, ALLOW_LIST shape, deletedCommands import, slashMentionExcludes carve-out)
    - tests/fixtures/cull-deletion-list.cjs (source of truth for `deletedCommands` array and `slashMentionExcludes` — the new sub-test consumes the same fixture)
    - .planning/users/dan-halem/gsd-slim-and-integrate/phases/01-cull-with-wave-0-test-infrastructure/01-VERIFICATION.md (re-read CR-01 section to understand the precise pattern to flag)
  </read_first>
  <behavior>
    - Test 1 (RED): Given current help.md body (which contains `Usage: \`/gsd-map-codebase\`` at line 59), the new sub-test FAILS naming `get-shit-done/workflows/help.md:59` and the deleted command `/gsd-map-codebase`. This proves the test catches the violation BEFORE Task 2 strips the body sections.
    - Test 2 (GREEN after Task 2): Given the post-strip help.md (Task 2 output), the new sub-test PASSES — no `Usage: /gsd-<deleted-command>` patterns remain in any ALLOW_LIST file's body.
    - Test 3 (regression catch): If a contributor re-introduces `Usage: \`/gsd-do …\`` inside any ALLOW_LIST file (e.g., commands/gsd/help.md, CHANGELOG.md), the sub-test fails with file:line and command name. This is the durable guard.
    - Existing 1/1 GREEN orphan scan continues to pass (the new sub-test is additive — does NOT relax the existing scan).
  </behavior>
  <action>
    Add a new `test()` block to `tests/cull-no-orphan-references.test.cjs` immediately after the existing top-level test. Concrete implementation:

    1. Import / reuse the existing `deletedCommands` and `slashMentionExcludes` from `require('./fixtures/cull-deletion-list.cjs')` (same require as the existing scan).

    2. Iterate over the existing `ALLOW_LIST` Set. For each `relPath` in ALLOW_LIST, read the file via `fs.readFileSync(path.join(ROOT, relPath), 'utf8')`.

    3. For each command name in `deletedCommands` minus `slashMentionExcludes`, build a regex that matches a Usage block referencing the deleted command. Use this exact pattern:
       ```js
       const usageRe = new RegExp(`Usage:\\s*\`?/gsd-${cmd}\\b`, 'g');
       ```
       This matches `Usage: /gsd-map-codebase`, `Usage: \`/gsd-map-codebase\``, `Usage: /gsd-list-phase-assumptions 3`, etc.

    4. For each match, push a finding string `${relPath}:${lineNumber} (allow-listed file contains live Usage block for deleted command): ${matchedLine.trim()}` into a findings array.

    5. After scanning all ALLOW_LIST files, call `assert.deepStrictEqual(findings, [], …)` with a message that explains: ALLOW_LIST is intended for migration-table prose, not live command docs. Bodies of allow-listed files MUST NOT contain `Usage: /gsd-<deleted>` patterns. The migration-table rows themselves use `Removed`/`/gsd-review --…` syntax, not `Usage:` syntax — so this regex does not collide with legitimate migration-table prose.

    6. Wrap in `test('ALLOW_LIST files have no live Usage blocks for deleted commands (CR-01 guard)', () => { … })`.

    7. Per CONTEXT.md D-04 (per-test concurrency contract): use only `fs.readFileSync` (read-only), own-scope local variables, NO `process.chdir`, NO mutation of shared state, NO inter-test state. The existing test follows this pattern; the new sub-test must too.

    8. Do NOT modify the existing scan. Do NOT remove any entry from ALLOW_LIST. Do NOT change `slashMentionExcludes` or `deletedCommands` — this is a tightening, not a relaxation.

    Run the test BEFORE Task 2 ships to confirm RED on `/gsd-map-codebase`, `/gsd-list-phase-assumptions`, `/gsd-do` — proving the test catches the bug. Then Task 2 strips the body sections and the test goes GREEN.
  </action>
  <verify>
    <automated>cd /home/danhalem/personal/get-shit-done && node --test tests/cull-no-orphan-references.test.cjs 2>&amp;1 | tee /tmp/cull-orphan-out.txt; grep -E 'fail 1$|fail 2$|fail 3$' /tmp/cull-orphan-out.txt &amp;&amp; echo "RED OK (test catches CR-01 before help.md fix)" || echo "RED MISSING — sub-test did not detect CR-01 violation; fix the regex"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test('ALLOW_LIST files have no live Usage blocks for deleted commands" tests/cull-no-orphan-references.test.cjs` returns >= 1 (new test block exists).
    - `grep -c "deletedCommands\\.filter\\|deletedCommands.*minus\\|slashMentionExcludes" tests/cull-no-orphan-references.test.cjs | grep -v '^#'` returns >= 1 within the new test block (confirms it consumes the fixture's `slashMentionExcludes` carve-out, honoring D-01).
    - `grep -cE "Usage:\\\\s\\*\\\\\`\\?/gsd-" tests/cull-no-orphan-references.test.cjs` returns >= 1 (the regex pattern is present).
    - Running `node --test tests/cull-no-orphan-references.test.cjs` BEFORE Task 2 outputs at least 3 failure findings — one each for `/gsd-map-codebase`, `/gsd-list-phase-assumptions`, `/gsd-do` in `get-shit-done/workflows/help.md`. Capture this output to confirm RED.
    - The pre-existing top-level test (orphan-reference scan) remains as-is and still passes (`# pass 1` for that block; the new block is the one expected to fail at this point in time).
  </acceptance_criteria>
  <done>
    The new sub-test exists and FAILS on the current help.md body (RED), naming the three deleted commands and their line numbers in get-shit-done/workflows/help.md. The pre-existing orphan-reference test continues to pass. Concurrency contract per D-04 honored. Task 2 will turn the new test GREEN by stripping the body sections.
  </done>
</task>

<task type="auto">
  <name>Task 2: Strip three live-command sections from help.md body</name>
  <files>get-shit-done/workflows/help.md</files>
  <read_first>
    - get-shit-done/workflows/help.md (the file being modified — read in full to locate exact start/end of each section to strip; the line numbers below come from the verification, but the executor must re-confirm against the actual current file before editing)
    - tests/cull-no-orphan-references.test.cjs (the new sub-test from Task 1 — confirms which command names must disappear from the body)
    - .planning/users/dan-halem/gsd-slim-and-integrate/phases/01-cull-with-wave-0-test-infrastructure/01-REVIEW.md (re-read CR-01 fix prescription)
  </read_first>
  <action>
    Strip three live-command body sections from `get-shit-done/workflows/help.md`. Concrete edits:

    1. **Section 1 — `/gsd-map-codebase`:** Remove the entire block currently at approximately lines 51-59. The block starts with the line `**\`/gsd-map-codebase\`**` (a bold, backtick-wrapped header) and ends with the `Usage: \`/gsd-map-codebase\`` line (the line right before the next section header `### Phase Planning`). Remove the trailing blank line so `### Phase Planning` butts directly against the previous content (`Usage: \`/gsd-new-project\`` line ~49). Concretely: delete lines starting at the `**\`/gsd-map-codebase\`**` line through (and including) the blank line preceding `### Phase Planning`.

    2. **Section 2 — `/gsd-list-phase-assumptions`:** Remove the block at approximately lines 85-92. The block starts with `**\`/gsd-list-phase-assumptions <number>\`**` and ends with `Usage: \`/gsd-list-phase-assumptions 3\``. Also remove the trailing blank line so the next section (`**\`/gsd-plan-phase <number>\`**`) follows the previous block (the `/gsd-research-phase` block ending at `Usage: \`/gsd-research-phase 3\``).

    3. **Section 3 — `/gsd-do`:** Remove the entire `### Smart Router` heading PLUS the block at approximately lines 121-133. The block consists of:
       - Line ~121: `### Smart Router`
       - Line ~122: blank line
       - Lines ~123-133: `**\`/gsd-do <description>\`**` header + description bullet points + three `Usage:` examples
       - The trailing blank line preceding `### Quick Mode`
       Result: After the `### Execution` block ending at `Usage: \`/gsd-execute-phase 5 --wave 2\``, the file flows directly to `### Quick Mode`. The `### Smart Router` heading is removed because `/gsd-do` was its only entry — leaving an empty heading would create a different orphan.

    4. **Preserve untouched:** The migration table at the bottom of the file (around lines 683, 712, 718 per the verification report) stays as-is. Migration table rows should be the only place in the file where the strings `gsd-map-codebase`, `gsd-list-phase-assumptions`, `gsd-do` appear after this edit.

    5. **Per D-01:** Do not touch any reference to the NEW `/gsd-review` (consolidated quality-gate) or `/gsd-phase` (consolidated phase-manipulation). Those references are legitimate.

    6. **Use the Edit tool** with exact `old_string`/`new_string` pairs covering each section. Do NOT regenerate the entire file via Write. Verify after each edit that the surrounding section structure is preserved (e.g., `### Phase Planning` still follows the project-init section; `### Quick Mode` still follows the execution section).

    Rationale: The migration table in this file (the `Removed` rows) is preserved. The body, which the orphan-reference test ALLOW_LISTed in good faith for migration-table prose, no longer contains contradictory live documentation.
  </action>
  <verify>
    <automated>cd /home/danhalem/personal/get-shit-done &amp;&amp; \
      grep -cE '^\*\*`/gsd-map-codebase`\*\*$|^\*\*`/gsd-list-phase-assumptions <number>`\*\*$|^\*\*`/gsd-do <description>`\*\*$' get-shit-done/workflows/help.md | grep -E '^0$' &amp;&amp; \
      grep -cE '^Usage: `/gsd-map-codebase`|^Usage: `/gsd-list-phase-assumptions|^Usage: /gsd-do' get-shit-done/workflows/help.md | grep -E '^0$' &amp;&amp; \
      grep -cE 'Removed.*gsd-map-codebase|Removed.*gsd-list-phase-assumptions|Removed.*gsd-do' get-shit-done/workflows/help.md | grep -vE '^0$' &amp;&amp; \
      node --test tests/cull-no-orphan-references.test.cjs 2>&amp;1 | tail -5 | grep -E '# fail 0' &amp;&amp; \
      echo "ALL CR-01 ACCEPTANCE CHECKS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^\*\*`/gsd-map-codebase`\*\*$' get-shit-done/workflows/help.md` returns 0 (live header gone).
    - `grep -cE '^\*\*`/gsd-list-phase-assumptions <number>`\*\*$' get-shit-done/workflows/help.md` returns 0 (live header gone).
    - `grep -cE '^\*\*`/gsd-do <description>`\*\*$' get-shit-done/workflows/help.md` returns 0 (live header gone).
    - `grep -cE '^Usage: `/gsd-map-codebase`' get-shit-done/workflows/help.md` returns 0 (Usage line gone).
    - `grep -cE '^Usage: `/gsd-list-phase-assumptions ' get-shit-done/workflows/help.md` returns 0 (Usage lines gone, both `Usage: \`/gsd-list-phase-assumptions 3\`` variants).
    - `grep -cE '^Usage: /gsd-do ' get-shit-done/workflows/help.md` returns 0 (all three `Usage: /gsd-do …` examples gone).
    - `grep -cE '^### Smart Router$' get-shit-done/workflows/help.md` returns 0 (orphan heading removed).
    - `grep -cE 'Removed.*gsd-map-codebase|Removed.*gsd-list-phase-assumptions|Removed.*gsd-do' get-shit-done/workflows/help.md | grep -v '^#'` returns >= 3 (migration table rows preserved per CULL-07).
    - `node --test tests/cull-no-orphan-references.test.cjs 2>&amp;1 | grep -E '# fail 0'` returns a match (the new Task-1 sub-test passes; existing scan still passes).
    - `npm test 2>&amp;1 | grep -E 'tests/migration-table-present.test.cjs.*ok|migration-table-present.*pass'` confirms the migration-table presence test (from Plan 09) still passes (we did not break the table).
  </acceptance_criteria>
  <done>
    `get-shit-done/workflows/help.md` body has zero live-command sections for `/gsd-map-codebase`, `/gsd-list-phase-assumptions`, `/gsd-do`. The migration table at the bottom retains the `Removed` rows for those names. The Task-1 sub-test passes GREEN. The pre-existing orphan-reference top-level test still passes. CR-01 closed.
  </done>
</task>

</tasks>

<verification>
- `node --test tests/cull-no-orphan-references.test.cjs` exits 0 (both the existing top-level test and the new CR-01 guard sub-test pass).
- `node --test tests/migration-table-present.test.cjs` exits 0 (Plan 09's migration-table presence test was not regressed by the body strip).
- `grep -cE '^\*\*`/gsd-(map-codebase|list-phase-assumptions|do)` get-shit-done/workflows/help.md` returns 0 (no live-command body sections for deleted commands).
- `grep -cE 'Removed.*gsd-(map-codebase|list-phase-assumptions|do)' get-shit-done/workflows/help.md | grep -v '^#'` returns >= 3 (migration-table rows preserved).
- VERIFICATION.md Truth 1 (orphan-reference test) flips from PARTIAL → VERIFIED on re-verification.
</verification>

<success_criteria>
- CR-01 BLOCKER closed: 01-VERIFICATION.md Truth 1 PARTIAL→VERIFIED on re-run.
- The new sub-test in `tests/cull-no-orphan-references.test.cjs` is a durable guard against future re-introduction of `Usage: /gsd-<deleted>` patterns inside any ALLOW_LIST file.
- The migration table (CULL-07) is unchanged; users running `/gsd-help` see the migration table as the authoritative source of truth for what replaced each deleted command.
</success_criteria>

<output>
After completion, create `.planning/users/dan-halem/gsd-slim-and-integrate/phases/01-cull-with-wave-0-test-infrastructure/01-10-SUMMARY.md` documenting:
- CR-01 closure: which sections of help.md body were stripped (line ranges before edit)
- The new sub-test added to `tests/cull-no-orphan-references.test.cjs` (test name, what it asserts)
- RED-then-GREEN evidence (Task 1 sub-test failed before Task 2; passes after)
- Confirmation that the migration table rows are preserved
</output>
