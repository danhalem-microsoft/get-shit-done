---
phase: 02-module-path-migration
plan: "05"
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - get-shit-done/workflows/*.md
  - agents/*.md
  - get-shit-done/templates/*.md
  - tests/audit-paths.test.cjs
autonomous: true
requirements: [PATH-11, PATH-12, PATH-13]

must_haves:
  truths:
    - "All workflow markdown files use ${planning_root}/... paths instead of hardcoded .planning/"
    - "All agent markdown files use {planning_root}, {phase_dir}, {state_path} placeholder variables instead of literal .planning/ paths"
    - "All template markdown files use {planning_root} placeholders instead of hardcoded .planning/ paths"
    - "tests/audit-paths.test.cjs passes — zero unallowed .planning/ references in entire codebase"
  artifacts:
    - path: "tests/audit-paths.test.cjs"
      provides: "Passing grep audit gate test"
      contains: "audit"
  key_links:
    - from: "get-shit-done/workflows/*.md"
      to: "gsd-tools.cjs init JSON"
      via: "${planning_root} from parsed init output"
      pattern: "\\$\\{?planning_root\\}?"
    - from: "agents/*.md"
      to: "orchestrator spawn prompts"
      via: "{planning_root} placeholder filled by orchestrator"
      pattern: "\\{planning_root\\}"
---

<objective>
Migrate all workflow markdown (~37 files, ~361 refs), agent markdown (~18 files, ~132 refs), and template markdown (~30 files, ~73 refs) from hardcoded `.planning/` paths to resolved/placeholder paths, then activate the grep audit gate test.

Purpose: This completes the full phase migration. After this plan, zero operational code anywhere in the repository references raw `.planning/` paths. The grep audit gate permanently enforces this.
Output: Updated markdown files across three directories + passing audit gate test
</objective>

<execution_context>
@/home/danhalem/.claude/get-shit-done/workflows/execute-plan.md
@/home/danhalem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-module-path-migration/02-CONTEXT.md
@.planning/phases/02-module-path-migration/02-RESEARCH.md
@.planning/phases/02-module-path-migration/02-01-SUMMARY.md
@.planning/phases/02-module-path-migration/02-02-SUMMARY.md
@.planning/phases/02-module-path-migration/02-03-SUMMARY.md
@.planning/phases/02-module-path-migration/02-04-SUMMARY.md

@tests/audit-paths.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate workflow and template markdown files</name>
  <files>get-shit-done/workflows/*.md, get-shit-done/templates/*.md</files>
  <action>
**Workflow markdown migration (~37 files, ~361 references):**

Per CONTEXT.md decisions:
- Workflows already call `gsd-tools.cjs init <command>` and parse JSON output
- Init JSON includes `planning_root` field
- Replace all hardcoded `.planning/STATE.md` with `${planning_root}/STATE.md`
- Replace all hardcoded `.planning/ROADMAP.md` with `${planning_root}/ROADMAP.md`
- Replace all hardcoded `.planning/phases/` with `${planning_root}/phases/`
- Replace all hardcoded `.planning/config.json` with `${planning_root}/config.json`
- Replace all hardcoded `.planning/` with appropriate `${planning_root}/` variant

**Workflow-specific patterns:**

For bash commands in workflows:
```markdown
<!-- BEFORE -->
cat .planning/STATE.md
cat .planning/ROADMAP.md

<!-- AFTER -->
cat "${planning_root}/STATE.md"
cat "${planning_root}/ROADMAP.md"
```

For `<files_to_read>` blocks in workflows:
```markdown
<!-- BEFORE -->
- .planning/STATE.md
- .planning/ROADMAP.md

<!-- AFTER -->
- ${planning_root}/STATE.md
- ${planning_root}/ROADMAP.md
```

For documentation/example references within workflows:
```markdown
<!-- BEFORE -->
Read .planning/STATE.md for current context.

<!-- AFTER -->
Read ${planning_root}/STATE.md for current context.
```

**IMPORTANT:** Per CONTEXT.md decision, documentation references ALSO get updated. Both operational and doc references use the new path structure.

**IMPORTANT:** Always quote bash variable expansions in shell commands: `"${planning_root}/STATE.md"` not `${planning_root}/STATE.md` (Pitfall 8).

**High-ref files to focus on (from grep counts):**
- new-project.md (49 refs) — heaviest, many path constructions
- complete-milestone.md (34 refs)
- new-milestone.md (24 refs)
- map-codebase.md (22 refs)
- execute-phase.md (22 refs)
- help.md (21 refs) — may be mostly documentation
- cleanup.md (19 refs)
- execute-plan.md (17 refs)
- discuss-phase.md (15 refs)

Work through ALL 37 workflow files. Use grep to find every `.planning/` reference and replace systematically.

**Template markdown migration (~30 files, ~73 references):**

Per CONTEXT.md decision, template files use `{planning_root}` placeholder variables:
```markdown
<!-- BEFORE (template content) -->
@.planning/PROJECT.md
See .planning/STATE.md

<!-- AFTER -->
@{planning_root}/PROJECT.md
See {planning_root}/STATE.md
```

High-ref template files:
- phase-prompt.md (19 refs)
- planner-subagent-prompt.md (9 refs)
- state.md (4 refs)
- milestone-archive.md (4 refs)

Template placeholders use `{planning_root}` (curly braces, no $) to distinguish from bash variable syntax. These are filled at template generation time by `cmdTemplateFill` or workflow code.
  </action>
  <verify>
    <automated>grep -rn '\.planning/' get-shit-done/workflows/ get-shit-done/templates/ --include="*.md" 2>/dev/null | grep -v '{planning_root}' | grep -v '${planning_root}' | head -20</automated>
  </verify>
  <done>All workflow markdown files use ${planning_root}/... paths. All template markdown files use {planning_root}/... placeholders. Zero raw .planning/ references remain in workflow or template files.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate agent markdown and activate grep audit gate</name>
  <files>agents/*.md, tests/audit-paths.test.cjs</files>
  <action>
**Agent markdown migration (~18 files, ~132 references):**

Per CONTEXT.md strict isolation decision (PATH-12):
- Agents receive ALL paths via orchestrator `<files_to_read>` blocks
- Agents NEVER construct `.planning/` paths themselves
- Replace literal `.planning/` paths with placeholder variables: `{planning_root}`, `{phase_dir}`, `{state_path}`
- Orchestrators fill these placeholders when constructing the spawn prompt

**Agent-specific patterns:**

For path references in agent instructions:
```markdown
<!-- BEFORE -->
Read .planning/STATE.md for project state.
Write SUMMARY.md to .planning/phases/XX-name/
cat .planning/ROADMAP.md

<!-- AFTER -->
Read {state_path} for project state.
Write SUMMARY.md to {phase_dir}/
cat "{roadmap_path}"
```

For `<files_to_read>` guidance:
```markdown
<!-- BEFORE -->
- .planning/STATE.md
- .planning/ROADMAP.md
- .planning/phases/XX-name/XX-CONTEXT.md

<!-- AFTER (agents receive these from orchestrator, show as placeholders) -->
- {state_path}
- {roadmap_path}
- {context_path}
```

For bash commands in agents:
```markdown
<!-- BEFORE -->
cat .planning/STATE.md 2>/dev/null

<!-- AFTER -->
cat "{state_path}" 2>/dev/null
```

**Standard placeholder variables:**
- `{planning_root}` — base planning directory (e.g., `.planning/users/dan/frontend`)
- `{state_path}` — `{planning_root}/STATE.md`
- `{roadmap_path}` — `{planning_root}/ROADMAP.md`
- `{requirements_path}` — `{planning_root}/REQUIREMENTS.md`
- `{phase_dir}` — `{planning_root}/phases/XX-name/`
- `{config_path}` — `{planning_root}/config.json`
- `{context_path}` — `{phase_dir}/XX-CONTEXT.md`
- `{research_path}` — `{phase_dir}/XX-RESEARCH.md`

**High-ref agent files:**
- gsd-planner.md (18 refs) — this file! Special care with self-references
- gsd-debugger.md (14 refs)
- gsd-project-researcher.md (10 refs)
- gsd-critic-*.md (10 refs each × 6 critics)
- gsd-roadmapper.md (8 refs)

Work through ALL 18 agent files systematically.

**Part B: Activate grep audit gate test**

Update `tests/audit-paths.test.cjs` (created in Plan 01):
1. Remove any skip/todo markers — the test should now be active
2. Ensure the allowlist is correct and comprehensive:
   - `core.cjs` — getPlanningRoot resolver + legacy detector + tryGetPlanningContext resolver
   - `identity.cjs` — user-map.json repo-root path
   - `context.cjs` — context resolution (if any remaining refs)
   - `audit-paths.test.cjs` — the test itself
   - `helpers.cjs` — test helper directory creation (if needed — check if Plan 01 eliminated all refs)
3. Add the test to the main test runner (`scripts/run-tests.cjs`) if not already included
4. Run the test and verify it PASSES — zero violations means the entire Phase 02 migration is complete

**If the audit gate test reveals violations:**
- Fix any remaining `.planning/` references in the violating files
- These are likely edge cases missed by earlier plans
- Common sources: string literals in error messages, comment documentation, assertion strings in tests

**Final validation:**
Run `grep -rn '\.planning/' --include="*.cjs" --include="*.md" .` from repo root and manually verify every remaining hit is on the allowlist.
  </action>
  <verify>
    <automated>node --test tests/audit-paths.test.cjs</automated>
  </verify>
  <done>All agent markdown files use placeholder variables instead of literal .planning/ paths. The grep audit gate test passes — zero unallowed .planning/ references in the entire codebase. PATH-13 is complete.</done>
</task>

</tasks>

<verification>
- `node --test tests/audit-paths.test.cjs` PASSES (this is the definitive gate for the entire phase)
- `node scripts/run-tests.cjs` PASSES (full suite including audit gate)
- `grep -rn '\.planning/' --include="*.cjs" --include="*.md" .` only returns allowlisted files (core.cjs, identity.cjs, context.cjs, audit-paths.test.cjs)
- Workflow files use `${planning_root}` variable syntax
- Agent files use `{planning_root}`, `{phase_dir}`, `{state_path}` placeholder syntax
- Template files use `{planning_root}` placeholder syntax
</verification>

<success_criteria>
1. Zero raw `.planning/` references in any workflow, agent, or template markdown file
2. Workflows use `${planning_root}` bash variable syntax
3. Agents use `{placeholder}` syntax — agents never construct `.planning/` paths
4. Templates use `{planning_root}` placeholder syntax
5. Grep audit gate test passes permanently
6. Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-module-path-migration/02-05-SUMMARY.md`
</output>
