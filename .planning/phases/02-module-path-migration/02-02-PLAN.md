---
phase: 02-module-path-migration
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - get-shit-done/bin/lib/state.cjs
  - get-shit-done/bin/lib/phase.cjs
  - get-shit-done/bin/lib/roadmap.cjs
  - get-shit-done/bin/lib/config.cjs
  - tests/state.test.cjs
  - tests/phase.test.cjs
  - tests/roadmap.test.cjs
  - tests/config.test.cjs
autonomous: true
requirements: [PATH-02, PATH-03, PATH-04, PATH-05]

must_haves:
  truths:
    - "state.cjs resolves all planning paths through getPlanningRoot(cwd)"
    - "phase.cjs resolves all planning paths through getPlanningRoot(cwd)"
    - "roadmap.cjs resolves all planning paths through getPlanningRoot(cwd)"
    - "config.cjs resolves all planning paths through getPlanningRoot(cwd)"
    - "All four modules' test suites pass with multi-user directory structure"
  artifacts:
    - path: "get-shit-done/bin/lib/state.cjs"
      provides: "State operations using resolved paths"
      contains: "getPlanningRoot"
    - path: "get-shit-done/bin/lib/phase.cjs"
      provides: "Phase lifecycle using resolved paths"
      contains: "getPlanningRoot"
    - path: "get-shit-done/bin/lib/roadmap.cjs"
      provides: "Roadmap operations using resolved paths"
      contains: "getPlanningRoot"
    - path: "get-shit-done/bin/lib/config.cjs"
      provides: "Config CRUD using resolved paths"
      contains: "getPlanningRoot"
  key_links:
    - from: "get-shit-done/bin/lib/state.cjs"
      to: "core.cjs getPlanningRoot"
      via: "require('./core.cjs')"
      pattern: "getPlanningRoot\\(cwd\\)"
    - from: "get-shit-done/bin/lib/phase.cjs"
      to: "core.cjs getPlanningRoot"
      via: "require('./core.cjs')"
      pattern: "getPlanningRoot\\(cwd\\)"
---

<objective>
Migrate state.cjs, phase.cjs, roadmap.cjs, and config.cjs from hardcoded `.planning/` paths to use `getPlanningRoot(cwd)`, and update all their tests to use multi-user directory structure.

Purpose: These four modules handle core planning artifact operations (state management, phase lifecycle, roadmap parsing, configuration). They must resolve paths through getPlanningRoot to support multi-user isolation.
Output: Four migrated modules with updated test suites
</objective>

<execution_context>
@/home/danhalem/.claude/get-shit-done/workflows/execute-plan.md
@/home/danhalem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-module-path-migration/02-CONTEXT.md
@.planning/phases/02-module-path-migration/02-RESEARCH.md
@.planning/phases/02-module-path-migration/02-01-SUMMARY.md

@get-shit-done/bin/lib/state.cjs
@get-shit-done/bin/lib/phase.cjs
@get-shit-done/bin/lib/roadmap.cjs
@get-shit-done/bin/lib/config.cjs
@get-shit-done/bin/lib/core.cjs
@tests/state.test.cjs
@tests/phase.test.cjs
@tests/roadmap.test.cjs
@tests/config.test.cjs
@tests/helpers.cjs

<interfaces>
<!-- From core.cjs (migrated in Plan 01): -->
```javascript
const { getPlanningRoot, clearPlanningRootCache } = require('./core.cjs');
// getPlanningRoot(cwd) → '.planning/users/<user>/<project>'
// clearPlanningRootCache() → call in test teardown
```

<!-- From tests/helpers.cjs (updated in Plan 01): -->
```javascript
const { createTempMultiUserProject, cleanup } = require('./helpers.cjs');
// createTempMultiUserProject() → { tmpDir, userSlug, projectName }
// Planning root: `.planning/users/${userSlug}/${projectName}`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate state.cjs and phase.cjs to getPlanningRoot</name>
  <files>get-shit-done/bin/lib/state.cjs, get-shit-done/bin/lib/phase.cjs, tests/state.test.cjs, tests/phase.test.cjs</files>
  <action>
**state.cjs migration (15 `.planning/` references):**

Add `const { getPlanningRoot } = require('./core.cjs');` to the require block (state.cjs likely already requires core.cjs — add getPlanningRoot to the destructured import).

For each function that constructs paths:
- Replace `path.join(cwd, '.planning', ...)` with `path.join(cwd, getPlanningRoot(cwd), ...)`
- Add `const planningRoot = getPlanningRoot(cwd);` at function body top if the function has multiple path constructions

CONTEXT.md says state.cjs shows 0 grep hits in original audit but research found 15 — audit carefully. Some references may be in string literals (output values), template content, or received from callers. Only change path CONSTRUCTION (filesystem operations). String output values like `state_path: '.planning/STATE.md'` should become `state_path: planningRoot + '/STATE.md'`.

**phase.cjs migration (19 `.planning/` references):**

Add `getPlanningRoot` to the destructured import from `./core.cjs`.

phase.cjs has heavy path construction in:
- `cmdPhaseAdd` — phases directory path
- `cmdPhaseInsert` — phases directory path
- `cmdPhaseRemove` — phases directory path
- `cmdPhaseComplete` — phases directory, ROADMAP path
- `cmdPhasesList` — phases directory
- `cmdPhasePlanIndex` — phases directory, plan file paths
- `cmdPhasePrompt` — phases directory, file paths
- `cmdPhaseAssumptions` — phases directory

Pattern: At the top of each function that needs it, add:
```javascript
const planningRoot = getPlanningRoot(cwd);
```
Then replace all `'.planning'` segments.

**Test updates for both modules:**

Update tests/state.test.cjs (80 refs) and tests/phase.test.cjs (129 refs):
1. Replace `createTempProject()` with `createTempMultiUserProject()`
2. Update all `path.join(tmpDir, '.planning', ...)` to use the multi-user planning root
3. Add `clearPlanningRootCache()` in afterEach/cleanup
4. Ensure all test assertions reference the correct multi-user paths

For state.test.cjs: Files like STATE.md, ROADMAP.md are now under `.planning/users/<user>/<project>/`
For phase.test.cjs: The `phases/` directory is now under `.planning/users/<user>/<project>/phases/`
  </action>
  <verify>
    <automated>node --test tests/state.test.cjs && node --test tests/phase.test.cjs</automated>
  </verify>
  <done>state.cjs has zero hardcoded .planning/ paths in path construction. phase.cjs has zero hardcoded .planning/ paths. Both test suites pass with multi-user directory structure.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate roadmap.cjs and config.cjs to getPlanningRoot</name>
  <files>get-shit-done/bin/lib/roadmap.cjs, get-shit-done/bin/lib/config.cjs, tests/roadmap.test.cjs, tests/config.test.cjs</files>
  <action>
**roadmap.cjs migration (4 `.planning/` references):**

Add `getPlanningRoot` to the destructured import from `./core.cjs`.

roadmap.cjs has small scope:
- `path.join(cwd, '.planning', 'ROADMAP.md')` → `path.join(cwd, getPlanningRoot(cwd), 'ROADMAP.md')`
- `path.join(cwd, '.planning', 'phases')` → `path.join(cwd, getPlanningRoot(cwd), 'phases')`

Only 4 references — straightforward mechanical replacement.

**config.cjs migration (7 `.planning/` references):**

config.cjs has its OWN `loadConfig` function OR imports it from core.cjs. Check carefully:
- If config.cjs has its own config-loading logic with `.planning/` paths, migrate those
- core.cjs `loadConfig` was already migrated in Plan 01
- config.cjs likely has `cmdConfigLoad`, `cmdConfigSet`, `cmdConfigGet` etc. that construct `path.join(cwd, '.planning', 'config.json')`

Add `getPlanningRoot` to imports. Replace all hardcoded `.planning/` path segments.

**IMPORTANT for config.cjs:** The config file is now at `<planningRoot>/config.json` instead of `.planning/config.json`. This is a behavior change that affects all config reads/writes. Verify that `loadConfig` in core.cjs (already migrated) and config.cjs functions are consistent.

**Test updates:**

Update tests/roadmap.test.cjs (31 refs) and tests/config.test.cjs (2 refs):
1. Replace `createTempProject()` with `createTempMultiUserProject()`
2. Update path constructions in test setup/assertions
3. Add `clearPlanningRootCache()` in teardown
4. config.test.cjs has only 2 refs — likely minimal changes
  </action>
  <verify>
    <automated>node --test tests/roadmap.test.cjs && node --test tests/config.test.cjs</automated>
  </verify>
  <done>roadmap.cjs and config.cjs have zero hardcoded .planning/ paths. Both test suites pass. Config CRUD operations resolve to user-qualified paths.</done>
</task>

</tasks>

<verification>
- `node --test tests/state.test.cjs` passes
- `node --test tests/phase.test.cjs` passes
- `node --test tests/roadmap.test.cjs` passes
- `node --test tests/config.test.cjs` passes
- `node scripts/run-tests.cjs` passes (full suite, no regressions)
- `grep -n '\.planning' get-shit-done/bin/lib/state.cjs get-shit-done/bin/lib/phase.cjs get-shit-done/bin/lib/roadmap.cjs get-shit-done/bin/lib/config.cjs` returns zero hits
</verification>

<success_criteria>
1. state.cjs, phase.cjs, roadmap.cjs, and config.cjs contain zero hardcoded `.planning/` path references
2. All four modules use `getPlanningRoot(cwd)` for path construction
3. All four test suites pass with multi-user directory structure
4. No regressions in full test suite
</success_criteria>

<output>
After completion, create `.planning/phases/02-module-path-migration/02-02-SUMMARY.md`
</output>
