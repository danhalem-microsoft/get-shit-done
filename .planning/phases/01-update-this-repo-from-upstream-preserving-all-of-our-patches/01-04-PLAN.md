---
phase: 01-update-this-repo-from-upstream-preserving-all-of-our-patches
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - get-shit-done/workflows/*.md
  - get-shit-done/agents/*.md
  - get-shit-done/templates/*.md
  - commands/gsd/*.md
autonomous: true
requirements:
  - MERGE-09
  - MERGE-10

must_haves:
  truths:
    - "All 37 workflow/template/agent markdown files have zero conflict markers"
    - "All workflow init lines use the @file: temp output pattern"
    - "Our custom workflows (switch, archive, restore, taste, new-project enhancements) are preserved"
    - "Upstream's new workflows and agents are integrated"
    - "Dynamic researcher selection code is preserved in researcher files"
    - "Completion gate enforcement is preserved"
  artifacts:
    - path: "get-shit-done/workflows/"
      provides: "All workflow files resolved with both our enhancements and upstream additions"
    - path: "get-shit-done/agents/"
      provides: "All agent files resolved"
    - path: "get-shit-done/templates/"
      provides: "All template files resolved"
  key_links:
    - from: "get-shit-done/workflows/*.md"
      to: "get-shit-done/bin/gsd-tools.cjs"
      via: "gsd-tools.cjs init <command> calls in bash blocks"
      pattern: "gsd-tools\\.cjs.*init"
---

<objective>
Resolve all 37 conflicting workflow, template, agent, and command markdown files (~111 conflict markers). These follow a repeating mechanical pattern once the core libs are resolved.

Purpose: Workflow/template/agent files form the user-facing orchestration layer. They call into gsd-tools.cjs init commands and reference planning paths. With core libs resolved, these resolutions are largely mechanical: adopt upstream's version where both sides made the same structural change (e.g., @file: output), preserve our unique additions (custom workflows, dynamic researcher, completion gates).
Output: All 37 markdown files fully resolved and committed on the feature branch.
</objective>

<execution_context>
@/home/danhalem/.claude/get-shit-done/workflows/execute-plan.md
@/home/danhalem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-update-this-repo-from-upstream-preserving-all-of-our-patches/01-CONTEXT.md
@.planning/phases/01-update-this-repo-from-upstream-preserving-all-of-our-patches/01-RESEARCH.md
@.planning/phases/01-update-this-repo-from-upstream-preserving-all-of-our-patches/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve workflow markdown files (~26 files, ~85 conflict markers)</name>
  <files>get-shit-done/workflows/*.md</files>
  <action>
  Most workflow conflicts follow one repeating pattern (per RESEARCH.md):

  ```markdown
  <!-- Both sides updated the init line with @file: support -->
  INIT=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" init <cmd> "${ARGS}")
  if [[ "$INIT" == @file:* ]]; then INIT=$(cat "${INIT#@file:}"); fi
  ```

  Both sides independently added the `@file:` temp file pattern. Conflicts are mostly cosmetic — the code is functionally equivalent.

  **Resolution approach for each workflow file:**

  1. List conflicting workflow files: `git diff --name-only --diff-filter=U -- 'get-shit-done/workflows/'`
  2. For each file with conflicts:
     a. Open the file and examine each `<<<<<<<` marker
     b. For `@file:` init pattern conflicts: both sides are equivalent — take upstream's version (usually cleaner/newer)
     c. For init field extraction (`Extract from init JSON`): verify our context fields (planning_root, active_user, active_project) are still extracted
     d. For `${planning_root}` variable usage: both sides use this — take upstream's version, verify our variable references work
     e. For new workflow sections added by upstream (read_first, acceptance_criteria, node repair): adopt them
     f. For our unique content (critic auto-fire, dynamic researcher routing, adaptive synthesizer changes): preserve
  3. Stage resolved file: `git add <file>`

  **Special attention for our enhanced workflows:**
  - `plan-phase.md` and `research-phase.md`: preserve dynamic researcher selection routing
  - Any workflow with completion gate enforcement: preserve the critic auto-fire patterns
  - `new-project.md`: preserve our two-step bootstrap pattern
  - `switch-project.md`, `archive-project.md`, `restore-project.md`: these are OUR files — if upstream didn't touch them, no conflict. If they did, preserve our logic.
  - Adaptive synthesizer workflow changes: preserve our modifications

  After resolving all ~26 workflow files:
  - Zero conflict markers across all workflow files
  - `grep -r "^<<<<<<<" get-shit-done/workflows/ | wc -l` returns 0

  Stage all: `git add get-shit-done/workflows/`
  </action>
  <verify>
    <automated>count=$(grep -rl "^<<<<<<<" get-shit-done/workflows/ 2>/dev/null | wc -l); if [ "$count" = "0" ]; then echo "PASS: all workflows resolved"; else echo "FAIL: $count workflow files still have markers"; exit 1; fi</automated>
  </verify>
  <done>All ~26 workflow files have zero conflict markers. Our custom workflows and enhancements preserved. Upstream's new workflow features integrated. Dynamic researcher routing and completion gate enforcement intact.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve agent, template, and command markdown files (~11 files, ~13 conflict markers)</name>
  <files>get-shit-done/agents/*.md, get-shit-done/templates/*.md, commands/gsd/*.md</files>
  <action>
  **Agent files (~6 files, ~9 conflicts):**
  1. List conflicting agents: `git diff --name-only --diff-filter=U -- 'get-shit-done/agents/'`
  2. For each agent file:
     - Conflicts likely involve frontmatter changes (Codex TOML fixes, Gemini fixes per RESEARCH.md) and placeholder syntax
     - Our agents use `{planning_root}`, `{state_path}`, `{phase_dir}` placeholders
     - Upstream may have changed placeholder naming or added new fields
     - Resolution: Keep our placeholder syntax if it's working, adopt upstream's frontmatter fixes (Codex TOML, Gemini)
     - Preserve our unique agents (gsd-critic-*.md) — these are our files, no upstream equivalent
  3. Stage: `git add get-shit-done/agents/`

  **Template files (~3 files, ~3 conflicts):**
  1. List conflicting templates: `git diff --name-only --diff-filter=U -- 'get-shit-done/templates/'`
  2. Resolution: Keep our `{planning_root}` placeholder support. Adopt upstream's template improvements.
  3. Preserve our code-search template integration
  4. Stage: `git add get-shit-done/templates/`

  **Command definition files (~1 file, ~1 conflict):**
  1. List conflicting commands: `git diff --name-only --diff-filter=U -- 'commands/'`
  2. Resolution: merge both sides' command definitions
  3. Preserve our custom command definitions (switch, archive, restore, taste, etc.)
  4. Stage: `git add commands/`

  **Doc file (~1 file, ~1 conflict):**
  1. If any doc-level markdown conflicts: resolve with union approach
  2. Stage any resolved doc files

  After resolving all remaining markdown conflicts:
  - Commit on feature branch:
  ```bash
  git commit -m "resolve: all workflow, agent, template, and command markdown conflicts

  - 26 workflow files resolved (~85 conflict markers)
  - 6 agent files resolved (~9 conflict markers)
  - 3 template files resolved (~3 conflict markers)
  - 1 command file + 1 doc file resolved (~2 conflict markers)
  - Our custom workflows/agents/templates preserved
  - Dynamic researcher routing and completion gates intact
  - Upstream's new features integrated"
  ```
  </action>
  <verify>
    <automated>count=0; for dir in get-shit-done/agents get-shit-done/templates commands; do c=$(grep -rl "^<<<<<<<" "$dir/" 2>/dev/null | wc -l); count=$((count + c)); done; if [ "$count" = "0" ]; then echo "PASS: all agent/template/command files resolved"; else echo "FAIL: $count files still have markers"; exit 1; fi</automated>
  </verify>
  <done>All agent, template, and command markdown files have zero conflict markers. Our unique agents and templates preserved. Upstream's frontmatter fixes and new content integrated. Feature branch committed.</done>
</task>

</tasks>

<verification>
- `grep -rl "^<<<<<<<" get-shit-done/workflows/ get-shit-done/agents/ get-shit-done/templates/ commands/ 2>/dev/null | wc -l` returns 0
- Spot check: `grep "researcher" get-shit-done/workflows/plan-phase.md` — dynamic researcher routing present
- Spot check: `grep "planning_root" get-shit-done/workflows/execute-plan.md` — placeholder present
</verification>

<success_criteria>
All 37 conflicting workflow/template/agent/command markdown files are resolved with zero conflict markers. Our enhancements (dynamic researcher, completion gates, custom workflows, code search) are preserved. Upstream's improvements (read_first, acceptance_criteria, node repair, Codex/Gemini fixes) are integrated. Committed on feature branch.
</success_criteria>

<output>
After completion, create `.planning/phases/01-update-this-repo-from-upstream-preserving-all-of-our-patches/01-04-SUMMARY.md`
</output>
