---
phase: 01-update-this-repo-from-upstream-preserving-all-of-our-patches
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - get-shit-done/bin/lib/init.cjs
  - get-shit-done/bin/lib/state.cjs
  - get-shit-done/bin/lib/phase.cjs
autonomous: true
requirements:
  - MERGE-03
  - MERGE-04
  - MERGE-05

must_haves:
  truths:
    - "init.cjs has zero conflict markers and all 13+ init functions work with multi-user context"
    - "state.cjs has zero conflict markers and uses getPlanningRoot/atomicWriteFileSync"
    - "phase.cjs has zero conflict markers and uses getPlanningRoot for phase operations"
    - "Upstream improvements (atomic writes, locking, manager optimizations) are integrated"
    - "Our multi-user context fields (active_user, active_project, planning_root) are preserved in all init outputs"
  artifacts:
    - path: "get-shit-done/bin/lib/init.cjs"
      provides: "All init command functions with multi-user context + upstream optimizations"
      contains: "planning_root"
    - path: "get-shit-done/bin/lib/state.cjs"
      provides: "State management with multi-user paths + atomic writes"
      contains: "getPlanningRoot"
    - path: "get-shit-done/bin/lib/phase.cjs"
      provides: "Phase operations with multi-user paths"
      contains: "getPlanningRoot"
  key_links:
    - from: "get-shit-done/bin/lib/init.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "require for getPlanningRoot/tryGetPlanningContext"
      pattern: "require.*core\\.cjs.*getPlanningRoot"
    - from: "get-shit-done/bin/lib/state.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "require for getPlanningRoot/atomicWriteFileSync"
      pattern: "require.*core\\.cjs.*getPlanningRoot"
    - from: "get-shit-done/bin/lib/phase.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "require for getPlanningRoot"
      pattern: "require.*core\\.cjs.*getPlanningRoot"
---

<objective>
Resolve the three highest-conflict-count core lib files: init.cjs (22 conflicts), phase.cjs (17 conflicts), and state.cjs (15 conflicts). These files form the heart of the command system.

Purpose: These three files account for 54 of 93 core lib conflict markers. They handle init command outputs (init.cjs), phase lifecycle (phase.cjs), and state persistence (state.cjs). Resolving them clears the majority of core lib work.
Output: Three fully resolved lib files staged for the merge commit.
</objective>

<execution_context>
@/home/danhalem/.claude/get-shit-done/workflows/execute-plan.md
@/home/danhalem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-update-this-repo-from-upstream-preserving-all-of-our-patches/01-CONTEXT.md
@.planning/phases/01-update-this-repo-from-upstream-preserving-all-of-our-patches/01-RESEARCH.md
@.planning/phases/01-update-this-repo-from-upstream-preserving-all-of-our-patches/01-01-SUMMARY.md

<interfaces>
<!-- Pattern established in Plan 01 for core.cjs resolution -->

Import pattern (union of both sides):
```javascript
const { getPlanningRoot, tryGetPlanningContext, planningPaths, atomicWriteFileSync, ... } = require('./core.cjs');
```

Path resolution pattern (use our getPlanningRoot, accept planningPaths as alternative):
```javascript
// EITHER pattern is acceptable after Plan 01's core.cjs resolution:
const planningRoot = getPlanningRoot(cwd);
const statePath = path.join(cwd, planningRoot, 'STATE.md');
// OR:
const paths = planningPaths(cwd);
const statePath = paths.state;
// Both resolve to the same multi-user path.
```

File write pattern (adopt upstream's atomic writes):
```javascript
// BEFORE: fs.writeFileSync(statePath, content, 'utf-8');
// AFTER:  atomicWriteFileSync(statePath, content, 'utf-8');
```

Init output pattern (preserve our context fields):
```javascript
// Every init function MUST include these 3 fields at top:
return {
  active_user: ctx?.active_user || null,
  active_project: ctx?.active_project || null,
  planning_root: ctx?.planning_root || null,
  // ... rest of init-specific fields
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve init.cjs — 22 conflict markers</name>
  <files>get-shit-done/bin/lib/init.cjs</files>
  <action>
  init.cjs has the MOST conflicts (22 markers) in the entire merge. It contains all `cmdInit*` functions that emit JSON for workflow orchestrators.

  **Resolution principles:**
  - Preserve ALL our multi-user context fields (active_user, active_project, planning_root) at the top of every init output
  - Preserve our custom init commands: cmdInitSwitch, cmdInitProjectSetup
  - Adopt upstream's new init commands and manager optimizations
  - Adopt upstream's `@file:` output pattern (we also added this — both sides should agree)
  - Import union: our getPlanningRoot/tryGetPlanningContext + upstream's planningPaths/atomicWriteFileSync
  - Where upstream changed path construction from `planningDir` to `planningPaths`, use our `getPlanningRoot` or the resolved `planningPaths` from core.cjs (both work — Plan 01 made planningPaths delegate to getPlanningRoot)

  **Per-conflict approach:**
  1. Read both sides of each conflict
  2. For context field additions (active_user, active_project, planning_root) — keep ours
  3. For new upstream fields/features — adopt them, add context fields at top
  4. For path construction — use getPlanningRoot pattern (or planningPaths which delegates to it)
  5. For `@file:` temp file output — both sides added this; take whichever is cleaner
  6. For imports — union of both sides
  7. For exports — union of both sides, verify no missing exports

  After resolving all 22 markers:
  - Zero conflict markers remain
  - All 13+ init functions export correctly
  - Every init function includes active_user, active_project, planning_root
  - cmdInitSwitch and cmdInitProjectSetup preserved

  Stage: `git add get-shit-done/bin/lib/init.cjs`
  </action>
  <verify>
    <automated>grep -c "^<<<<<<<\|^>>>>>>>\|^=======" get-shit-done/bin/lib/init.cjs | grep -q "^0$" && grep -q "planning_root" get-shit-done/bin/lib/init.cjs && grep -q "cmdInitSwitch\|cmdInitProjectSetup" get-shit-done/bin/lib/init.cjs && echo "PASS: init.cjs resolved"</automated>
  </verify>
  <done>init.cjs has zero conflict markers. All init functions include multi-user context fields. Our cmdInitSwitch and cmdInitProjectSetup are preserved. Upstream's new init features and optimizations are integrated.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve state.cjs (15 conflicts) and phase.cjs (17 conflicts)</name>
  <files>get-shit-done/bin/lib/state.cjs, get-shit-done/bin/lib/phase.cjs</files>
  <action>
  **state.cjs (15 conflicts):**
  - Upstream nearly doubled this file (721→1415 lines) with atomic writes, locking improvements, and new state management features
  - Our changes were minimal (721→732 lines) — just multi-user path resolution
  - Resolution: Take most of upstream's code (it's largely additive), replace all `planningDir`/`planningPaths` path construction with our `getPlanningRoot` pattern (or use `planningPaths` from core.cjs since it now delegates to ours)
  - Import union: add `atomicWriteFileSync` from core.cjs
  - Replace all `fs.writeFileSync` with `atomicWriteFileSync` where upstream did so
  - Preserve our `getPlanningRoot(cwd)` calls — do NOT replace with upstream's `planningDir(cwd)`
  - Verify exports include all functions from both sides

  **phase.cjs (17 conflicts):**
  - Upstream grew modestly (901→943 lines)
  - Our changes were minimal (901→909 lines) — just multi-user path resolution
  - Resolution similar to state.cjs: adopt upstream's improvements, keep our path resolution
  - Phase operations (create phase, list phases, phase state management) — merge both sides
  - Import union, export union
  - Replace `planningDir` references with `getPlanningRoot`

  For BOTH files, after resolution:
  - Zero conflict markers
  - `getPlanningRoot` used for path construction (or `planningPaths` which delegates to it)
  - `atomicWriteFileSync` used for file writes where upstream adopted it
  - All exports from both sides preserved

  Stage: `git add get-shit-done/bin/lib/state.cjs get-shit-done/bin/lib/phase.cjs`
  </action>
  <verify>
    <automated>grep -c "^<<<<<<<\|^>>>>>>>\|^=======" get-shit-done/bin/lib/state.cjs get-shit-done/bin/lib/phase.cjs | grep -v ":0$" | wc -l | grep -q "^0$" && echo "PASS: state.cjs and phase.cjs resolved"</automated>
  </verify>
  <done>state.cjs has zero conflict markers with atomic writes adopted and multi-user paths preserved. phase.cjs has zero conflict markers with upstream phase improvements and multi-user paths. Both files staged.</done>
</task>

</tasks>

<verification>
- `grep -c "^<<<<<<<" get-shit-done/bin/lib/init.cjs` returns 0
- `grep -c "^<<<<<<<" get-shit-done/bin/lib/state.cjs` returns 0
- `grep -c "^<<<<<<<" get-shit-done/bin/lib/phase.cjs` returns 0
- `grep "planning_root" get-shit-done/bin/lib/init.cjs` finds context fields
- `grep "getPlanningRoot\|planningPaths" get-shit-done/bin/lib/state.cjs` finds path resolution
- Quick test: `node --test tests/core.test.cjs` (smoke test against resolved core libs — may still have failures from unresolved files)
</verification>

<success_criteria>
init.cjs (22 conflicts), state.cjs (15 conflicts), and phase.cjs (17 conflicts) are fully resolved with zero conflict markers. Multi-user context is preserved. Upstream improvements (atomic writes, locking, optimizations) are integrated. All three files are staged. 54 of 93 core lib conflict markers are resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/01-update-this-repo-from-upstream-preserving-all-of-our-patches/01-02-SUMMARY.md`
</output>
