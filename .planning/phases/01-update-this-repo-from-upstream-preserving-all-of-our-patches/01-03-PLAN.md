---
phase: 01-update-this-repo-from-upstream-preserving-all-of-our-patches
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - get-shit-done/bin/lib/commands.cjs
  - get-shit-done/bin/lib/config.cjs
  - get-shit-done/bin/gsd-tools.cjs
  - get-shit-done/bin/lib/roadmap.cjs
  - get-shit-done/bin/lib/milestone.cjs
  - get-shit-done/bin/lib/verify.cjs
  - get-shit-done/bin/lib/template.cjs
  - README.md
  - CHANGELOG.md
  - .gitignore
  - commands/gsd/reapply-patches.md
  - get-shit-done/workflows/set-profile.md
autonomous: true
requirements:
  - MERGE-09
  - MERGE-10
  - MERGE-11

must_haves:
  truths:
    - "All remaining core lib files (commands, config, gsd-tools, roadmap, milestone, verify, template) have zero conflict markers"
    - "Our custom commands (switch, archive, restore, team-status, config-resolve, migrate) are preserved in commands.cjs"
    - "Our 4-tier config (loadConfig with global+project merge) is preserved in config.cjs"
    - "Infrastructure files (.gitignore, README.md, CHANGELOG.md) resolved"
    - "Modify/delete conflicts handled: reapply-patches.md kept, set-profile.md evaluated"
    - "gsd-tools.cjs dispatcher includes all our custom command dispatchers"
  artifacts:
    - path: "get-shit-done/bin/lib/commands.cjs"
      provides: "All commands including our switch/archive/restore/team-status/config-resolve/migrate"
      contains: "cmdTeamStatus"
    - path: "get-shit-done/bin/lib/config.cjs"
      provides: "4-tier config with ENV_KEY_MAP + global/project merge"
      contains: "ENV_KEY_MAP"
    - path: "get-shit-done/bin/gsd-tools.cjs"
      provides: "CLI dispatcher routing all commands including our additions"
      contains: "team-status"
  key_links:
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: "get-shit-done/bin/lib/commands.cjs"
      via: "require and command dispatch"
      pattern: "require.*commands\\.cjs"
    - from: "get-shit-done/bin/lib/commands.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "require for getPlanningRoot"
      pattern: "require.*core\\.cjs"
---

<objective>
Resolve all remaining core lib files (7 files, 39 conflict markers), infrastructure files (3 files, 10 markers), and the 2 modify/delete conflicts. This completes ALL non-workflow, non-test conflict resolution.

Purpose: After this plan, every JavaScript source file is conflict-free. Only markdown workflow/template/agent files and test files remain for Plans 04-05.
Output: All core libs, infrastructure files, and special-case files fully resolved and staged.
</objective>

<execution_context>
@/home/danhalem/.claude/get-shit-done/workflows/execute-plan.md
@/home/danhalem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-update-this-repo-from-upstream-preserving-all-of-our-patches/01-CONTEXT.md
@.planning/phases/01-update-this-repo-from-upstream-preserving-all-of-our-patches/01-RESEARCH.md
@.planning/phases/01-update-this-repo-from-upstream-preserving-all-of-our-patches/01-02-SUMMARY.md

<interfaces>
<!-- Patterns established in Plans 01-02 -->

Import pattern: Union of both sides — always include getPlanningRoot + any upstream additions
Path resolution: getPlanningRoot(cwd) or planningPaths(cwd) (both resolve to multi-user path)
File writes: atomicWriteFileSync where upstream adopted it
Exports: Union of both sides — verify no missing exports

Our custom commands to preserve in commands.cjs:
- cmdTeamStatus, cmdConfigResolve, cmdArchiveProject, cmdRestoreProject
- cmdMigrate (legacy structure migration)
- All existing v1.0 commands

Our custom dispatchers to preserve in gsd-tools.cjs:
- switch, archive-project, restore-project, team-status, config-resolve, migrate
- init project-setup sub-command
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve commands.cjs (10 conflicts) + config.cjs (3 conflicts) + gsd-tools.cjs (5 conflicts)</name>
  <files>get-shit-done/bin/lib/commands.cjs, get-shit-done/bin/lib/config.cjs, get-shit-done/bin/gsd-tools.cjs</files>
  <action>
  **commands.cjs (10 conflicts):**
  - Upstream grew significantly (548→1013 lines) with new commands
  - We grew significantly (548→845 lines) with switch/archive/restore/team-status/config-resolve/migrate
  - Resolution: Union of both command sets. Import union. Export union.
  - Preserve ALL our commands: cmdTeamStatus, cmdConfigResolve, cmdArchiveProject, cmdRestoreProject, cmdMigrate
  - Adopt ALL upstream's new commands (docs, learnings, workstream, UAT, security, profile, intel, etc.)
  - For shared commands modified by both sides (progress, scaffold, history-digest, etc.): merge both changes, preferring our multi-user path resolution + upstream's feature additions
  - For `getPlanningRoot` vs `planningDir` conflicts: use our `getPlanningRoot`

  **config.cjs (3 conflicts):**
  - Upstream grew massively (169→479 lines) with new config features
  - Our changes were minimal (169→170) but we have the 4-tier loadConfig in the separate export
  - Actually our full config.cjs with ENV_KEY_MAP and 4-tier merge is ~170 lines of core logic
  - Resolution: Merge upstream's expanded config features with our ENV_KEY_MAP, global+project config merge, and _sources tracking
  - Keep our loadConfig's two-file merge behavior
  - Adopt upstream's new config keys and --default flag if not already covered
  - Preserve our cmdConfigResolve

  **gsd-tools.cjs (5 conflicts):**
  - Both sides expanded the dispatcher significantly
  - Resolution: Union of all command dispatchers
  - Preserve our custom dispatchers: switch, archive-project, restore-project, team-status, config-resolve, migrate, init project-setup
  - Adopt upstream's new dispatchers: docs, learnings, workstream, UAT, security, profile, intel, etc.
  - For `@file:` temp file output: both sides added this; both should work the same way
  - Import union

  Stage: `git add get-shit-done/bin/lib/commands.cjs get-shit-done/bin/lib/config.cjs get-shit-done/bin/gsd-tools.cjs`
  </action>
  <verify>
    <automated>grep -c "^<<<<<<<\|^>>>>>>>\|^=======" get-shit-done/bin/lib/commands.cjs get-shit-done/bin/lib/config.cjs get-shit-done/bin/gsd-tools.cjs | grep -v ":0$" | wc -l | grep -q "^0$" && grep -q "cmdTeamStatus" get-shit-done/bin/lib/commands.cjs && grep -q "ENV_KEY_MAP" get-shit-done/bin/lib/config.cjs && grep -q "team-status" get-shit-done/bin/gsd-tools.cjs && echo "PASS"</automated>
  </verify>
  <done>commands.cjs, config.cjs, and gsd-tools.cjs have zero conflict markers. All our custom commands and dispatchers preserved. Upstream's new commands integrated. 4-tier config preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve remaining libs (4 files, 12 conflicts) + infrastructure (3 files, 10 conflicts) + modify/delete (2 files)</name>
  <files>get-shit-done/bin/lib/roadmap.cjs, get-shit-done/bin/lib/milestone.cjs, get-shit-done/bin/lib/verify.cjs, get-shit-done/bin/lib/template.cjs, README.md, CHANGELOG.md, .gitignore, commands/gsd/reapply-patches.md, get-shit-done/workflows/set-profile.md</files>
  <action>
  **Remaining core libs (4 files, 12 total conflicts):**
  - roadmap.cjs (4 conflicts): Our changes minimal, upstream grew modestly. Keep our getPlanningRoot, adopt upstream additions.
  - milestone.cjs (3 conflicts): Same pattern — keep our paths, adopt upstream features.
  - verify.cjs (4 conflicts): Keep our cmdValidateConsistency with getPlanningRoot, our cmdValidateHealth with tryGetPlanningContext. Adopt upstream's verification improvements.
  - template.cjs (1 conflict): Keep our {planning_root} placeholder support. Adopt upstream changes.
  - For all: import union, export union, getPlanningRoot for paths.

  **Infrastructure files (3 files, 10 conflicts):**
  - README.md (8 conflicts): Take upstream's README content (it's their project's public face), but preserve any fork-specific sections we added. If we have FORK.md for fork docs, we can largely take upstream's README.
  - CHANGELOG.md (1 conflict): Take upstream's CHANGELOG entirely. Our changes are tracked separately.
  - .gitignore (1 conflict): Union of both sides' ignore patterns. Keep our `.active` and multi-user patterns. Add any upstream additions.

  **Modify/delete conflicts (2 files):**
  - `commands/gsd/reapply-patches.md`: We deleted, upstream modified → **KEEP upstream's version** (per RESEARCH.md — it's part of fork infrastructure, we deleted it incorrectly)
    ```bash
    git checkout upstream/main -- commands/gsd/reapply-patches.md
    git add commands/gsd/reapply-patches.md
    ```
  - `get-shit-done/workflows/set-profile.md`: Upstream deleted, we modified → **Evaluate**: Check if upstream's `settings.md` contains the same functionality. If yes, delete set-profile.md. If our modifications added unique content, merge into settings.md.
    ```bash
    # Check if settings.md exists in upstream and covers set-profile functionality
    git show upstream/main:get-shit-done/workflows/settings.md 2>/dev/null | head -20
    ```
    Based on evaluation, either `git rm get-shit-done/workflows/set-profile.md` or keep it.

  Stage all resolved files.

  **After ALL core libs are resolved, do a quick intermediate commit on the feature branch:**
  ```bash
  git commit -m "resolve: all core lib and infrastructure conflicts

  - 11 core lib files resolved (93 conflict markers)
  - 3 infrastructure files resolved (10 conflict markers)
  - 2 modify/delete conflicts resolved
  - Multi-user path resolution preserved throughout
  - Upstream improvements (atomic writes, locking, security) integrated"
  ```
  </action>
  <verify>
    <automated>for f in get-shit-done/bin/lib/roadmap.cjs get-shit-done/bin/lib/milestone.cjs get-shit-done/bin/lib/verify.cjs get-shit-done/bin/lib/template.cjs README.md CHANGELOG.md .gitignore; do c=$(grep -c "^<<<<<<<" "$f" 2>/dev/null); if [ "$c" != "0" ]; then echo "FAIL: $f has $c markers"; exit 1; fi; done && echo "PASS: all remaining libs and infra resolved"</automated>
  </verify>
  <done>All 7 remaining core lib files have zero conflict markers. Infrastructure files resolved. Modify/delete conflicts handled per RESEARCH.md guidance. Intermediate commit made on feature branch. All 105 non-workflow, non-test conflict markers resolved.</done>
</task>

</tasks>

<verification>
- All 11 core lib files: `for f in get-shit-done/bin/lib/*.cjs get-shit-done/bin/gsd-tools.cjs; do echo "$f: $(grep -c '^<<<<<<<' "$f" 2>/dev/null) markers"; done`
- Infrastructure files: zero markers in README.md, CHANGELOG.md, .gitignore
- Quick smoke: `node -e "require('./get-shit-done/bin/lib/core.cjs')"` — no require crashes
- Quick smoke: `node -e "require('./get-shit-done/bin/lib/commands.cjs')"` — no require crashes
</verification>

<success_criteria>
ALL JavaScript source files (11 core libs + dispatcher) are conflict-free. Infrastructure files resolved. Modify/delete conflicts resolved. Intermediate commit exists on feature branch. Only workflow/template/agent markdown files (37 files) and test files (8 files) remain with conflicts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-update-this-repo-from-upstream-preserving-all-of-our-patches/01-03-SUMMARY.md`
</output>
