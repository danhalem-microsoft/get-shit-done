---
phase: 03-project-lifecycle-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/context.cjs
  - get-shit-done/bin/lib/core.cjs
  - tests/context.test.cjs
  - tests/core.test.cjs
autonomous: true
requirements: [LIFE-04, LIFE-05, LIFE-06]

must_haves:
  truths:
    - "listProjects() returns structured array of project objects with name, phase, progress, last_activity, description"
    - "resolveContext() auto-selects when user has exactly one project"
    - "resolveContext() returns { user, project: null, planning_root: null } when zero projects and no .active"
    - "resolveContext() returns null project when multiple projects and no .active (instead of hard-erroring)"
    - "getPlanningRoot() still hard-errors when resolveContext returns null project"
    - "loadConfig() merges global .planning/config.json + per-project config.json with correct precedence"
    - "loadConfig() tracks source layer per key for Phase 4 config resolve"
  artifacts:
    - path: "get-shit-done/bin/lib/context.cjs"
      provides: "Redesigned listProjects(), modified resolveContext() with auto-select and null return"
      exports: ["readActiveContext", "writeActiveContext", "resolveContext", "listProjects"]
    - path: "get-shit-done/bin/lib/core.cjs"
      provides: "Rebuilt loadConfig() with two-file merge and source tracking"
      exports: ["loadConfig"]
    - path: "tests/context.test.cjs"
      provides: "Tests for listProjects structured return, resolveContext auto-select, resolveContext null return"
    - path: "tests/core.test.cjs"
      provides: "Tests for loadConfig two-file merge, loadConfig source tracking"
  key_links:
    - from: "get-shit-done/bin/lib/core.cjs"
      to: "get-shit-done/bin/lib/context.cjs"
      via: "getPlanningRoot() calling resolveContext()"
      pattern: "resolveContext\\(cwd\\)"
    - from: "get-shit-done/bin/lib/core.cjs"
      to: ".planning/config.json AND ${planning_root}/config.json"
      via: "loadConfig() reading both config files"
      pattern: "safeReadFile.*config\\.json"
---

<objective>
Rebuild core module functions that all downstream lifecycle commands depend on: `listProjects()` structured redesign, `resolveContext()` auto-select + null return, and `loadConfig()` two-file merge with source tracking.

Purpose: These three function changes are the foundation for every Phase 3 command. Switch, archive, progress, and new-project all depend on structured project listing and graceful context resolution. Config layering is needed for per-project config (LIFE-06).

Output: Modified context.cjs, core.cjs, and extended test files
</objective>

<execution_context>
@/home/danhalem/.claude/get-shit-done/workflows/execute-plan.md
@/home/danhalem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-project-lifecycle-commands/03-CONTEXT.md
@.planning/phases/03-project-lifecycle-commands/03-RESEARCH.md

@get-shit-done/bin/lib/context.cjs
@get-shit-done/bin/lib/core.cjs
@tests/context.test.cjs
@tests/core.test.cjs

<interfaces>
<!-- Key types and contracts the executor needs -->

From get-shit-done/bin/lib/context.cjs (CURRENT - will be modified):
```javascript
// readActiveContext(cwd, user) → { project, resolved_path } | null
// writeActiveContext(cwd, user, project) → void (writes .active + ensures gitignore)
// resolveContext(cwd) → { user, project, planning_root } (CURRENTLY hard-errors on no active)
// listProjects(cwd, user) → string (CURRENTLY internal, not exported, returns comma-joined string)
module.exports = {
  readActiveContext,
  writeActiveContext,
  resolveContext,
};
```

From get-shit-done/bin/lib/core.cjs (functions to modify):
```javascript
function _resolvePlanningRootSoft(cwd) {
  const ctx = tryGetPlanningContext(cwd);
  return (ctx && ctx.planning_root) ? ctx.planning_root : '.planning';
}

function loadConfig(cwd) {
  // CURRENTLY reads ONE config file from _resolvePlanningRootSoft(cwd)
  // Needs rebuild for global + per-project merge
}

function getPlanningRoot(cwd) {
  // ... CI/CD check, legacy check ...
  const { resolveContext } = require('./context.cjs');
  const ctx = resolveContext(cwd);
  _planningRootCache[cwd] = ctx.planning_root;
  return ctx.planning_root;
}

function tryGetPlanningContext(cwd) {
  // Soft resolution -- returns nulls instead of hard erroring
  // Does NOT use resolveContext() -- uses resolveIdentity + readActiveContext directly
  // This function does NOT need changes (it already returns nulls gracefully)
}
```

From get-shit-done/bin/lib/identity.cjs:
```javascript
function resolveIdentity(cwd) → { slug, source } | null
```

From get-shit-done/bin/lib/frontmatter.cjs:
```javascript
function extractFrontmatter(content) → object
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redesign listProjects() and modify resolveContext() in context.cjs</name>
  <files>get-shit-done/bin/lib/context.cjs, tests/context.test.cjs</files>
  <behavior>
    - listProjects(cwd, user) returns array of objects: [{ name, current_phase, progress, last_activity, description }]
    - listProjects returns empty array when user directory doesn't exist
    - listProjects filters out _archived directory and .active file
    - listProjects reads STATE.md frontmatter for phase/progress fields
    - listProjects reads PROJECT.md for Core Value line as description
    - listProjects handles missing STATE.md/PROJECT.md gracefully (null fields)
    - resolveContext(cwd) auto-selects when exactly 1 non-archived project exists (no .active needed)
    - resolveContext(cwd) returns { user, project: null, planning_root: null } when 0 projects exist
    - resolveContext(cwd) returns { user, project: null, planning_root: null } when multiple projects exist and no .active file
    - resolveContext still works normally when .active file exists (existing behavior preserved)
    - resolveContext still handles GSD_PROJECT env var (existing behavior preserved)
    - getPlanningRoot(cwd) hard-errors when resolveContext returns null project
  </behavior>
  <action>
    1. **Redesign `listProjects()`** in context.cjs:
       - Change return type from string to array of structured objects
       - Read STATE.md in each project directory, extract frontmatter using `extractFrontmatter()` from frontmatter.cjs
       - Read PROJECT.md, extract "Core Value" line (the line after `**Core Value:**` in the markdown)
       - Get STATE.md mtime for last_activity
       - Progress format: `"X/Y"` from frontmatter `progress.completed_phases` / `progress.total_phases`
       - current_phase from frontmatter or body parsing
       - Filter: `e.isDirectory() && e.name !== '_archived' && !e.name.startsWith('.')`
       - Handle missing files gracefully (null fields)
       - **Export listProjects** from module.exports

    2. **Modify `resolveContext()`** in context.cjs:
       - After identity resolution and user dir bootstrap, BEFORE reading .active:
       - Add auto-select scan: `scanProjects(userDir)` helper that reads user dir, filters directories (excluding `_archived` and dotfiles)
       - If `projects.length === 1` → auto-select: return `{ user, project: projects[0], planning_root: ... }`
       - If `projects.length === 0` → return `{ user, project: null, planning_root: null }` (NOT hard-error)
       - If `projects.length > 1` AND no .active file → return `{ user, project: null, planning_root: null }` (NOT hard-error)
       - Keep GSD_PROJECT env var check BEFORE auto-select (env var has highest priority)
       - Auto-select happens BEFORE .active file read (if exactly 1 project, .active is irrelevant)

    3. **Update `getPlanningRoot()`** in core.cjs:
       - After calling `resolveContext(cwd)`, check if `ctx.planning_root` is null
       - If null: `error('GSD Error: No active project. Run /gsd:new-project to create one, or /gsd:switch to select one.')`
       - This preserves hard-error behavior for commands that require an active project

    4. **Write tests** in context.test.cjs (extend existing file):
       - Test listProjects structured return with multiple projects
       - Test listProjects with empty user dir
       - Test listProjects filters _archived
       - Test listProjects handles missing STATE.md/PROJECT.md
       - Test resolveContext auto-selects single project
       - Test resolveContext returns null for zero projects
       - Test resolveContext returns null for multiple projects without .active
       - Test resolveContext still reads .active when multiple projects exist
       - Test getPlanningRoot errors when resolveContext returns null

    NOTE: The `scanProjects()` helper is a simple dir read + filter — keep it internal to context.cjs. Do NOT confuse it with `listProjects()` which reads metadata from each project. `scanProjects()` returns just directory names, `listProjects()` returns rich metadata objects.
  </action>
  <verify>
    <automated>node --test tests/context.test.cjs</automated>
  </verify>
  <done>
    - listProjects() exported, returns structured array with metadata
    - resolveContext() auto-selects single project without .active file
    - resolveContext() returns null project/planning_root for 0 or multiple projects (no .active)
    - getPlanningRoot() hard-errors when no active project can be resolved
    - All existing context.test.cjs tests still pass (no regressions)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild loadConfig() for global + per-project merge with source tracking</name>
  <files>get-shit-done/bin/lib/core.cjs, tests/core.test.cjs</files>
  <behavior>
    - loadConfig(cwd) reads .planning/config.json (global) AND ${planning_root}/config.json (per-project)
    - Per-project values override global values, global values override defaults
    - loadConfig returns _sources object mapping each key to its source path (or 'default')
    - Depth-to-granularity migration works on whichever file contains the deprecated key
    - loadConfig works when no global config exists (uses defaults only)
    - loadConfig works when no per-project config exists (uses global + defaults only)
    - loadConfig works when no project is active (falls back to global config only, no error)
    - Nested key get() logic (e.g., workflow.plan_check) works across both config files
    - model_overrides merges correctly from either config level
  </behavior>
  <action>
    1. **Rebuild `loadConfig()`** in core.cjs:
       - Read global config: `path.join(cwd, '.planning', 'config.json')` — always at repo root, no resolution needed
       - Read per-project config: use `_resolvePlanningRootSoft(cwd)` — if it returns something other than `.planning`, read `path.join(cwd, planningRoot, 'config.json')`
       - If `_resolvePlanningRootSoft` returns `.planning` (no active project), skip per-project config entirely
       - Merge: `defaults → globalConfig → projectConfig` using per-key precedence
       - Track sources: for each config key, record whether the final value came from `'default'`, the global config path, or the per-project config path
       - Store sources in `result._sources = { key: sourcePath, ... }`
       - Apply the `get()` nested-key logic to BOTH global and project configs (project takes precedence)
       - Depth-to-granularity migration: check both files, migrate in whichever contains `depth`. If both contain it, migrate in per-project (higher precedence). Write back only to the file that contained it.
       - Keep the `parallelization` object-or-boolean normalization
       - Keep `model_overrides` handling (it should come from per-project if set, otherwise global)

    2. **Write tests** in core.test.cjs (extend existing file):
       - Test loadConfig with only global config
       - Test loadConfig with only per-project config
       - Test loadConfig with both configs (per-project overrides global)
       - Test loadConfig with neither config (defaults returned)
       - Test loadConfig._sources tracks correct source per key
       - Test depth migration in global config
       - Test depth migration in per-project config (takes precedence)
       - Test model_overrides from per-project overrides global
       - Test loadConfig when no active project (global + defaults only)

    IMPORTANT: `_resolvePlanningRootSoft(cwd)` returns `.planning` as fallback when no project is active. Use this to detect "no active project" — do NOT read per-project config when planning root is `.planning` (that would try to read `.planning/config.json` twice).

    PITFALL: The global config path is `.planning/config.json`. The per-project config path is `${planningRoot}/config.json`. When `planningRoot === '.planning'`, they would be the SAME file. Guard against this by only reading per-project config when `planningRoot !== '.planning'`.
  </action>
  <verify>
    <automated>node --test tests/core.test.cjs</automated>
  </verify>
  <done>
    - loadConfig() reads both global and per-project configs with correct precedence
    - _sources tracks which layer each value came from
    - Migration logic handles both files correctly
    - No circular dependency issues
    - All existing core.test.cjs tests still pass (no regressions)
  </done>
</task>

</tasks>

<verification>
1. `node --test tests/context.test.cjs` — all tests pass including new ones
2. `node --test tests/core.test.cjs` — all tests pass including new ones
3. `node scripts/run-tests.cjs` — full suite green (no regressions in other modules)
4. Verify context.cjs exports include `listProjects`
5. Verify loadConfig returns `_sources` field
</verification>

<success_criteria>
- listProjects() is exported and returns structured array
- resolveContext() handles 0, 1, and N projects without hard-erroring
- getPlanningRoot() preserves hard-error behavior for commands needing active project
- loadConfig() merges global + per-project with source tracking
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-lifecycle-commands/03-01-SUMMARY.md`
</output>
