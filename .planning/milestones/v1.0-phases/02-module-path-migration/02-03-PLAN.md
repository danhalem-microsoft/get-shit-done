---
phase: 02-module-path-migration
plan: "03"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - get-shit-done/bin/lib/verify.cjs
  - get-shit-done/bin/lib/template.cjs
  - get-shit-done/bin/lib/milestone.cjs
  - get-shit-done/bin/lib/taste.cjs
  - get-shit-done/bin/lib/commands.cjs
  - tests/verify.test.cjs
  - tests/verify-health.test.cjs
  - tests/commands.test.cjs
  - tests/milestone.test.cjs
  - tests/dispatcher.test.cjs
autonomous: true
requirements: [PATH-06, PATH-07, PATH-08, PATH-09]

must_haves:
  truths:
    - "verify.cjs resolves all planning paths through getPlanningRoot(cwd)"
    - "template.cjs resolves all planning paths through getPlanningRoot(cwd)"
    - "milestone.cjs resolves all planning paths through getPlanningRoot(cwd)"
    - "taste.cjs resolves all planning paths through getPlanningRoot(cwd) — function signatures updated to include cwd parameter"
    - "commands.cjs resolves all planning paths through getPlanningRoot(cwd)"
  artifacts:
    - path: "get-shit-done/bin/lib/verify.cjs"
      provides: "Verification suite using resolved paths"
      contains: "getPlanningRoot"
    - path: "get-shit-done/bin/lib/template.cjs"
      provides: "Template operations using resolved paths"
      contains: "getPlanningRoot"
    - path: "get-shit-done/bin/lib/milestone.cjs"
      provides: "Milestone operations using resolved paths"
      contains: "getPlanningRoot"
    - path: "get-shit-done/bin/lib/taste.cjs"
      provides: "Taste operations using resolved paths with cwd parameter"
      contains: "getPlanningRoot"
    - path: "get-shit-done/bin/lib/commands.cjs"
      provides: "Standalone commands using resolved paths"
      contains: "getPlanningRoot"
  key_links:
    - from: "get-shit-done/bin/lib/taste.cjs"
      to: "get-shit-done/bin/gsd-tools.cjs"
      via: "updated function signatures (cwd parameter added)"
      pattern: "loadActiveTasteEntries\\(cwd"
    - from: "get-shit-done/bin/lib/verify.cjs"
      to: "core.cjs getPlanningRoot"
      via: "require('./core.cjs')"
      pattern: "getPlanningRoot\\(cwd\\)"
---

<objective>
Migrate verify.cjs, template.cjs, milestone.cjs, taste.cjs, and commands.cjs from hardcoded `.planning/` paths to `getPlanningRoot(cwd)`, with special attention to taste.cjs function signature changes.

Purpose: These five modules complete the library-level migration. taste.cjs requires a function signature change (adding `cwd` parameter) since its functions currently lack it but need it for getPlanningRoot.
Output: Five migrated modules with updated test suites
</objective>

<execution_context>
@/home/danhalem/.claude/get-shit-done/workflows/execute-plan.md
@/home/danhalem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-module-path-migration/02-CONTEXT.md
@.planning/phases/02-module-path-migration/02-RESEARCH.md
@.planning/phases/02-module-path-migration/02-01-SUMMARY.md

@get-shit-done/bin/lib/verify.cjs
@get-shit-done/bin/lib/template.cjs
@get-shit-done/bin/lib/milestone.cjs
@get-shit-done/bin/lib/taste.cjs
@get-shit-done/bin/lib/commands.cjs
@get-shit-done/bin/lib/core.cjs
@get-shit-done/bin/gsd-tools.cjs
@tests/verify.test.cjs
@tests/verify-health.test.cjs
@tests/commands.test.cjs
@tests/milestone.test.cjs
@tests/dispatcher.test.cjs
@tests/helpers.cjs

<interfaces>
<!-- From core.cjs (migrated in Plan 01): -->
```javascript
const { getPlanningRoot, clearPlanningRootCache } = require('./core.cjs');
// getPlanningRoot(cwd) → '.planning/users/<user>/<project>'
```

<!-- taste.cjs current signatures that need cwd added: -->
```javascript
// BEFORE:
function loadActiveTasteEntries(tastesDir = '.planning/taste/') { ... }
function updateTasteCounters(tasteId, action, tastesDir = '.planning/taste/') { ... }

// AFTER:
function loadActiveTasteEntries(cwd, tastesDir) { ... }
function updateTasteCounters(cwd, tasteId, action, tastesDir) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Migrate verify.cjs and milestone.cjs</name>
  <files>get-shit-done/bin/lib/verify.cjs, get-shit-done/bin/lib/milestone.cjs, tests/verify.test.cjs, tests/verify-health.test.cjs, tests/milestone.test.cjs</files>
  <action>
**verify.cjs migration (6 `.planning/` references):**

Add `getPlanningRoot` to destructured import from `./core.cjs`.

verify.cjs constructs paths in `cmdValidateConsistency` and `cmdValidateHealth`:
- `path.join(cwd, '.planning', 'phases')` → `path.join(cwd, getPlanningRoot(cwd), 'phases')`
- `path.join(cwd, '.planning', 'ROADMAP.md')` → `path.join(cwd, getPlanningRoot(cwd), 'ROADMAP.md')`
- `path.join(cwd, '.planning', 'STATE.md')` → `path.join(cwd, getPlanningRoot(cwd), 'STATE.md')`
- `path.join(cwd, '.planning', 'config.json')` → `path.join(cwd, getPlanningRoot(cwd), 'config.json')`

**milestone.cjs migration (9 `.planning/` references):**

Add `getPlanningRoot` to destructured import from `./core.cjs`.

milestone.cjs constructs paths in `cmdMilestoneComplete` and `cmdRequirementsMarkComplete`:
- `path.join(cwd, '.planning', 'REQUIREMENTS.md')` → use getPlanningRoot
- `path.join(cwd, '.planning', 'ROADMAP.md')` → use getPlanningRoot
- `path.join(cwd, '.planning', 'STATE.md')` → use getPlanningRoot
- `path.join(cwd, '.planning', 'milestones')` → use getPlanningRoot
- `path.join(cwd, '.planning', 'phases')` → use getPlanningRoot

Pattern: `const planningRoot = getPlanningRoot(cwd);` at top of each function body.

**Test updates:**

Update tests/verify.test.cjs (73 refs), tests/verify-health.test.cjs (45 refs), tests/milestone.test.cjs (59 refs):

Concrete sub-steps for each test file:
1. **Replace createTempProject calls:** `const { tmpDir, userSlug, projectName } = createTempMultiUserProject()`. Compute `const planningRoot = '.planning/users/' + userSlug + '/' + projectName;`
2. **Update path.join constructions:** Replace all `path.join(tmpDir, '.planning', ...)` with `path.join(tmpDir, planningRoot, ...)`
3. **Update assertion strings:** Verify output paths in assertions reference multi-user structure
4. **Add clearPlanningRootCache in teardown:** Add `clearPlanningRootCache()` in every afterEach/after/cleanup block
  </action>
  <verify>
    <automated>node --test tests/verify.test.cjs && node --test tests/verify-health.test.cjs && node --test tests/milestone.test.cjs</automated>
  </verify>
  <done>verify.cjs and milestone.cjs have zero hardcoded .planning/ paths. All three test files pass with multi-user directory structure and clearPlanningRootCache in teardown.</done>
</task>

<task type="auto">
  <name>Task 1b: Migrate commands.cjs</name>
  <files>get-shit-done/bin/lib/commands.cjs, tests/commands.test.cjs</files>
  <action>
**commands.cjs migration (12 `.planning/` references):**

Add `getPlanningRoot` to destructured import from `./core.cjs`.

commands.cjs has various standalone commands that build paths. Audit each function for `.planning/` references and replace with `getPlanningRoot(cwd)`. Some may be in `cmdScaffoldProject`, `cmdPhaseScaffold`, or utility functions.

Pattern: `const planningRoot = getPlanningRoot(cwd);` at top of each function body, then replace all `'.planning'` path segments.

**Test updates for commands.test.cjs (51 refs):**

Concrete sub-steps:
1. **Replace createTempProject calls:** `const { tmpDir, userSlug, projectName } = createTempMultiUserProject()`. Compute `const planningRoot = '.planning/users/' + userSlug + '/' + projectName;`
2. **Update path.join constructions:** Replace all `path.join(tmpDir, '.planning', ...)` with `path.join(tmpDir, planningRoot, ...)`
3. **Update assertion strings:** Command output paths must reference multi-user structure
4. **Add clearPlanningRootCache in teardown:** Add `clearPlanningRootCache()` in every afterEach/after/cleanup block
  </action>
  <verify>
    <automated>node --test tests/commands.test.cjs</automated>
  </verify>
  <done>commands.cjs has zero hardcoded .planning/ paths. commands.test.cjs uses multi-user directory structure with clearPlanningRootCache in teardown. All command tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate taste.cjs and template.cjs (with function signature changes)</name>
  <files>get-shit-done/bin/lib/taste.cjs, get-shit-done/bin/lib/template.cjs, get-shit-done/bin/gsd-tools.cjs, tests/dispatcher.test.cjs</files>
  <action>
**taste.cjs migration (5 `.planning/` references — SPECIAL CASE):**

taste.cjs functions currently have default parameter values with `.planning/taste/`:
```javascript
function loadActiveTasteEntries(tastesDir = '.planning/taste/') { ... }
function updateTasteCounters(tasteId, action, tastesDir = '.planning/taste/') { ... }
```

These functions DON'T have a `cwd` parameter — they need one to call `getPlanningRoot()`. Per RESEARCH.md Pitfall 2:

1. Add `cwd` as the first parameter to `loadActiveTasteEntries(cwd, tastesDir)` and `updateTasteCounters(cwd, tasteId, action, tastesDir)`
2. Inside each function, if `tastesDir` is not provided, compute it: `tastesDir = tastesDir || path.join(cwd, getPlanningRoot(cwd), 'taste');`
3. Remove the default parameter value `'.planning/taste/'`
4. Add `getPlanningRoot` to the destructured import from `./core.cjs`

**CRITICAL: Update ALL callers in gsd-tools.cjs dispatcher.** Search for calls to `loadActiveTasteEntries` and `updateTasteCounters` in gsd-tools.cjs and update them to pass `cwd` as the first argument. The dispatcher already has `cwd` available from argument parsing.

Note: This plan modifies gsd-tools.cjs ONLY for taste function call updates. Plan 04 handles the dispatcher's own .planning/ path references.

**template.cjs migration (3 `.planning/` references):**

Add `getPlanningRoot` to destructured import from `./core.cjs`.

template.cjs has `.planning/` in:
1. Template path strings (filesystem paths) — replace with `getPlanningRoot(cwd)` + path
2. Template CONTENT strings (e.g., `@.planning/PROJECT.md` in generated document content) — replace with `{planning_root}/PROJECT.md` placeholder variables per CONTEXT.md decision

For template content: Replace literal `.planning/` paths in template body strings with `{planning_root}` placeholders. The `cmdTemplateFill` or template generation code should accept a `planning_root` variable for substitution.

**Test updates:**

Update tests/dispatcher.test.cjs (12 refs) for taste function signature changes:

Concrete sub-steps:
1. **Replace createTempProject calls** (if any) with `createTempMultiUserProject()`. Compute `planningRoot`.
2. **Update path.join constructions:** Replace `path.join(tmpDir, '.planning', ...)` with multi-user paths
3. **Update assertion strings:** Ensure taste-related assertions reference correct paths
4. **Add clearPlanningRootCache in teardown:** In every afterEach/cleanup block
5. **Update taste function calls in tests:** Pass `cwd` as first argument to `loadActiveTasteEntries(cwd, ...)` and `updateTasteCounters(cwd, ...)`

If taste has its own test file, update it too. Otherwise, taste tests are in dispatcher.test.cjs.
  </action>
  <verify>
    <automated>node --test tests/dispatcher.test.cjs</automated>
  </verify>
  <done>taste.cjs functions have cwd parameter and use getPlanningRoot. template.cjs uses getPlanningRoot and placeholder variables in template content. gsd-tools.cjs taste caller updated. Dispatcher tests pass.</done>
</task>

</tasks>

<verification>
- `node --test tests/verify.test.cjs` passes
- `node --test tests/verify-health.test.cjs` passes
- `node --test tests/milestone.test.cjs` passes
- `node --test tests/commands.test.cjs` passes
- `node --test tests/dispatcher.test.cjs` passes
- `node scripts/run-tests.cjs` passes (full suite)
- `grep -n '\.planning' get-shit-done/bin/lib/verify.cjs get-shit-done/bin/lib/template.cjs get-shit-done/bin/lib/milestone.cjs get-shit-done/bin/lib/taste.cjs get-shit-done/bin/lib/commands.cjs` returns zero hits
</verification>

<success_criteria>
1. All five modules contain zero hardcoded `.planning/` path references
2. taste.cjs function signatures include `cwd` parameter; all callers updated
3. template.cjs uses `{planning_root}` placeholder variables in template content
4. All test suites pass with multi-user directory structure
5. No regressions in full test suite
</success_criteria>

<output>
After completion, create `.planning/phases/02-module-path-migration/02-03-SUMMARY.md`
</output>
