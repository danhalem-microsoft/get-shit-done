'use strict';

// Placeholder for Task 13 (lifecycle E2E - opencode). The Bazel js_test
// rule in tests/e2e/BUILD.bazel is tagged `manual` so it is not executed
// by wildcard test runs. This stub keeps the build graph self-consistent
// until Task 13 replaces the file with the real test.

throw new Error('tests/e2e/lifecycle-opencode.test.cjs is a placeholder; see Task 13 of docs/superpowers/plans/2026-05-26-gsd-copilot-opencode-verify.md');
