'use strict';

// Placeholder for Task 8 (invocation smoke test). The Bazel js_test rule
// in tests/e2e/BUILD.bazel is tagged `manual` so it is not executed by
// wildcard test runs. This stub keeps the build graph self-consistent
// until Task 8 replaces the file with the real test.

throw new Error('tests/e2e/invocation-smoke.test.cjs is a placeholder; see Task 8 of docs/superpowers/plans/2026-05-26-gsd-copilot-opencode-verify.md');
